---
title: "SB-RF: Schrödinger Bridge Rectified Flow for One-Step Robust Speech Enhancement"
title_zh: SB-RF：用于一步鲁棒语音增强的薛定谔桥矫正流
authors: "Caixia Lu, Xueyang Lv, Penglong Hu, Jiaming Xu"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05575v1"
tags: ["query:diff-bridge"]
score: 9.0
evidence: 薛定谔桥整流流用于语音增强
tldr: 生成模型在语音增强中表现优异，但多步推理限制了实际部署。本文提出SB-RF框架，将薛定谔桥理论与整流流相结合，通过熵正则化最优传输构建干净与带噪语音之间的条件桥，并借助速度匹配目标实现单步生成。在VoiceBank-DEMAND基准上，SB-RF达到生成式方法的最优性能；在模拟低信噪比场景下，其展现出强鲁棒性和高效率，为一步式鲁棒语音增强提供了有效方案。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05575v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 853, \"height\": 477, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05575v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 788, \"height\": 457, \"label\": \"Table\"}]"
motivation: 现有生成模型多步推理慢，难以满足实时语音增强需求。
method: 结合薛定谔桥与整流流，构造条件最优传输桥，用速度匹配实现单步生成。
result: VoiceBank-DEMAND上领先生成式方法，低SNR下鲁棒且高效。
conclusion: SB-RF实现一步高质量增强与强鲁棒性，适合实际应用。
---

## 摘要
生成模型在语音增强方面取得了显著成果，但通常存在多步推理的问题。我们提出了SB-RF，一种将矫正流（RF）与薛定谔桥（SB）理论相结合的一步生成框架。SB-RF通过熵正则化最优传输构建了干净语音和带噪语音分布之间的条件桥。通过将SB轨迹与RF的速度匹配目标对齐到最优传输测地线上，SB-RF实现了一步生成的高质量增强。实验表明，SB-RF在VoiceBank-DEMAND基准上达到了生成方法中的领先性能。此外，为了全面评估在挑战性真实场景中的表现，我们在扩展的训练数据集上使用模拟的低信噪比测试集对SB-RF进行了评估。在这些条件下，SB-RF展现出强大且具有竞争力的鲁棒性和高效率，验证了其在实际应用中的潜力。

## Abstract
Generative models have shown impressive results in speech enhancement but often suffer from multi-step inference. We propose SB-RF, a one-step generative framework integrating Rectified Flow (RF) with Schrödinger Bridge (SB) theory. SB-RF constructs a conditional bridge between clean and noisy speech distributions via entropy-regularized optimal transport. By aligning SB trajectories with the optimal transport geodesic through the velocity-matching objective of RF, SB-RF enables high-quality enhancement with one-step generation. Experiments demonstrate that SB-RF achieves leading performance among generative methods on the VoiceBank-DEMAND benchmark. Furthermore, to fully assess performance in challenging real-world scenarios, we evaluate SB-RF on a simulated low signal-to-noise ratio test set using an expanded training dataset. Under these conditions, SB-RF exhibits strong and competitive robustness with high efficiency, validating its potential for real-world applications.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

**研究动机**：尽管基于扩散等生成模型的语音增强方法在音质上优于传统判别式方法，但其多步迭代推理（通常需要 10–200 次函数评估）导致严重的推理延迟，难以满足实时通信或端侧部署的需求。

**核心问题**：如何在保持生成模型高保真度的前提下实现**一步**推理，并在低信噪比（SNR）等挑战场景下保持鲁棒性。

**整体含义**：本文提出 SB-RF 框架，将薛定谔桥（Schrödinger Bridge, SB）与矫正流（Rectified Flow, RF）有机结合，利用熵正则化最优传输构造条件桥，并通过 RF 的速度匹配目标将桥轨迹拉直为最优传输测地线，从而在一步内重建高质量语音。该工作为一步式生成语音增强提供了兼具性能与效率的可行方案。

## 2. 方法论

**核心思想**：标准 RF 使用确定的线性插值 \(x_t = t y + (1-t)x\)，这忽略了语音多模态后验的复杂性。SB 通过最小化关于参考过程的 KL 散度，找到连接干净分布 \(\pi_x\) 与带噪分布 \(\pi_y\) 的最可能路径，其边际分布为自适应的高斯形式。本文将 SB 作为 RF 的先验，再用 RF 的速度匹配目标将该随机路径“拉直”为常速度直线，从而支持一步推理。

**关键技术细节**：

- **SB 边际分布**：假设高斯端点，SB 过程在时刻 \(t\) 的边际分布为：
  \[
  p_t(x_t) = \mathcal{N}_{\mathbb{C}}\big( \mu_x(t),\; \sigma_x^2(t) \mathbf{I} \big),
  \]
  其中均值 \(\mu_x(t) = w_x(t)x + w_y(t)y\)，方差 \(\sigma_x^2(t) = \alpha_t^2 \bar{\sigma}_t^2 / \sigma_T^2\)。权重系数 \(w_x(t)\) 和 \(w_y(t)\) 由噪声调度决定（见公式 (8)）。

- **训练目标**：模型 \(v_\theta(x_t, t, y)\) 预测速度，损失函数为：
  \[
  \mathcal{L}(\theta) = \mathcal{L}_v(\theta) + \lambda_1 \mathcal{L}_{\text{mel}}(\theta) + \lambda_2 \mathcal{L}_{\text{pesq}}(\theta),
  \]
  其中 \(\mathcal{L}_v(\theta) = \mathbb{E}_{t, x, y, x_t}\big[ \lVert (y - x) - v_\theta(x_t, t, y) \rVert_2^2 \big]\) 为 RF 的速度匹配损失；\(\mathcal{L}_{\text{mel}}\) 和 \(\mathcal{L}_{\text{pesq}}\) 分别为多尺度 Mel 谱损失和 PESQ 损失，用于提升感知质量。

- **推断**：使用欧拉 ODE 求解器，从 \(x_T = y\) 开始一步（\(N=1\)）更新：
  \[
  x_{0} = x_T - v_\theta(x_T, T, y) \cdot (T - \epsilon).
  \]

**算法流程**（文字说明）：
1. 训练阶段：对每对干净和带噪 STFT 谱图 \((x, y)\)，采样时刻 \(t \sim \mathcal{U}[\epsilon, T]\)，按 SB 边际分布采样 \(x_t\)；将 \(x_t, t, y\) 输入网络预测速度 \(v_\theta\)；计算组合损失并更新参数。
2. 推断阶段：直接以带噪谱图 \(y\) 作为初始 \(x_T\)，用一步欧拉公式得到干净谱图估计 \(\hat{x}\)，再经逆 STFT 恢复时域波形。

## 3. 实验设计

**数据集与场景**：

- **Track A（标准设置）**：使用 **VoiceBank-DEMAND (VB-DMD)** 基准。训练集 11572 条，SNR 0/5/10/15 dB；测试集 824 条，SNR 2.5/7.5/12.5/17.5 dB。
- **Track B（低 SNR 鲁棒性设置）**：扩展训练集包含 1000 小时干净语音（WenetSpeech4TTS 和 DNS-4 各 500 小时）与 DNS-4、MUSAN 噪声，SNR 在 -10 到 15 dB 随机采样。测试集使用 AISHELL-1 和 LibriSpeech 干净语音，搭配 WHAM! 噪声，SNR 在 -10 到 0 dB。严格保证说话人、语句和噪声片段不重叠。

**Benchmark**：对比以下代表性方法：
- 判别式：MP-SENet。
- 扩散方法：SGMSE+、BBED、SB-VE。
- 流匹配/一步方法：CFM、LARF、COSE、BB-RF（本文消融变体）。
- 本文方法：SB-RF。

**评估指标**：PESQ（感知质量）、ESTOI（可懂度）、SI-SDR（信号失真比）、SI-SIR、SI-SAR、DNSMOS（非侵入式）。

## 4. 资源与算力

文中明确提及：
- 训练硬件：**8 块 NVIDIA RTX A800 GPU**。
- 训练配置：Adam 优化器，初始学习率 0.0001，指数衰减 0.999/epoch；batch size 为 16 per GPU；所有模型**从随机初始化**训练，未说明具体 epoch 数或训练时长。
- 模型规模：NCSN++ 骨干网络，**65.6M 参数**，**265 GMACs** 计算量。

## 5. 实验数量与充分性

**实验数量**：
- 两个主实验表（Table 1: Track A, Table 2: Track B）。
- 消融实验：对比 BB-RF（线性 BB 先验）与 SB-RF（SB 先验）；对 SB-RF 测试了不同 NFE（1/5/10）的性能。
- 指标覆盖广泛（7 项客观指标）。

**充分性评价**：
- **积极**：在标准集和自定义低 SNR 集上均进行了评估；对比了多种类型基线（判别式、扩散、流匹配）；消融设计清晰，验证了 SB 先验优于线性 BB。
- **不足**：
  - 缺少对无噪声或高 SNR 条件下性能的分析。
  - 仅在 STFT 域进行，未与主流时域方法（如 Conv-TasNet）对比。
  - 未评估模型在音乐噪声、混响等更复杂场景下的泛化能力。
  - 由于 Track B 使用自定义数据集和噪声，与其他论文的直接可比性受限。

## 6. 主要结论与发现

- **Track A**：SB-RF 以一步（NFE=1）达到生成方法最优，PESQ 3.39，SI-SDR 19.5 dB，ESTOI 0.88，显著优于 SGMSE+、BBED 等多步扩散方法，也优于 LARF、COSE 等一步方法（+0.37 PESQ 增量）。
- **Track B**：在低 SNR（-10~0 dB）条件下，SB-RF 保持最高 PESQ（2.56）和 ESTOI（0.70）；与判别式 MP-SENet 相比，感知指标更优（+0.47 PESQ），SI-SDR 接近（10.4 vs 10.5 dB）。
- **NFE 分析**：NFE=1 已足够，增加步数对 PESQ 甚至有负面影响（过拟合直线假设），验证了一步推理的最优性。
- **SB 优于 BB**：对比 BB-RF，SB-RF 在所有指标上提升，证明 SB 先验提供了更高效的传输轨迹。

## 7. 优点

1. **理论创新性**：将 SB 与 RF 有机结合，从最优传输角度解决了线性插值对多模态后验建模不足的问题。
2. **实用性**：实现单步推理，NFE=1，大幅降低推理延迟，适合实时/端侧部署。
3. **无需外部预训练模型**：相比 MeanFlowSE（依赖 WavLM-Large），SB-RF 无需预训练编码器，降低了部署负担。
4. **鲁棒性验证充分**：在自定义低 SNR 测试集上展示出强于判别式方法的感知性能。
5. **消融实验清晰**：通过 BB-RF 与 SB-RF 对比，直接证明了所提先验的有效性。

## 8. 不足与局限

1. **实验通用性受限**：仅在 STFT 域验证，未与主流时域方法（如 Conv-TasNet、DPCRN）对比；自定义 Track B 数据集的设置可能影响与其他论文的公平对比。
2. **场景覆盖有限**：未评估音乐噪声、回响、多说话人等情况；未提供对极低 SNR（< -10 dB）的结果。
3. **模型复杂度**：虽然 NFE 低，但骨干网络 NCSN++ 仍有 265 GMACs，对低功耗设备仍较高，需进一步轻量化。
4. **超参数敏感性**：α=0.5, β=0.33 等幅度变换参数为经验设定，缺乏消融；损失权重 λ1=33, λ2=3 未说明搜索过程。
5. **局限性分析缺失**：论文未讨论失败的例子（如高频噪声残留、人声缺损）或对说话人泛化的限制。

（完）
