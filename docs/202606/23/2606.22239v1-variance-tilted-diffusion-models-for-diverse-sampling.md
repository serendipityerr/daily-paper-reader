---
title: Variance-Tilted Diffusion Models for Diverse Sampling
title_zh: 方差倾斜扩散模型用于多样化采样
authors: "Iskander Azangulov, Leo Zhang, Kianoosh Ashouritaklimi"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.22239v1"
tags: ["query:diff-bridge"]
score: 8.0
evidence: 使用Doob h-变换构造用于多样性采样的扩散桥
tldr: 扩散模型独立采样无法保证结果多样性，尤其在需要候选集多样性的下游任务中。本文提出方差倾斜扩散模型，通过引入方差加权批分布来偏好高经验分布的样本集合。利用Doob h-变换导出交互粒子采样器，包含排斥后验去噪均值的交互项和吸引至高特征方差的曲率项。该方法具有明确概率目标，避免了传统启发式排斥项，有效提升采样多样性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22239v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1773, \"height\": 678, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22239v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1773, \"height\": 676, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22239v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1773, \"height\": 678, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22239v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1774, \"height\": 676, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22239v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1773, \"height\": 680, \"label\": \"Figure\"}]"
motivation: 扩散模型独立采样难获多样候选集，需有目标地促进样本分布。
method: 引入方差加权批分布，通过Doob h-变换得到含交互与曲率项的粒子采样器。
result: 交互项排斥后验均值，曲率项引导至特征高方差区，实现透明概率目标。
conclusion: 提供一种具有明确概率解释的多样性采样机制，优于启发式方法。
---

## 摘要
扩散模型通常独立采样，即使下游目标是获得多样化的候选集。我们引入了一种方差加权批分布，该分布在预定义的线性特征映射后倾向于具有较大经验扩散的样本集合。该目标被显式指定，采样器被推导为独立扩散动力学对应的Doob h-变换。所产生的修正具有紧凑形式：一个排斥后验去噪均值的相互作用项，以及一个将粒子移动到更高特征方差区域的曲率项。这产生了一个具有透明概率目标而非启发式排斥漂移的交互粒子采样器。

## Abstract
Diffusion models are typically sampled independently, even when the downstream objective is to obtain a diverse set of candidates. We introduce a variance-weighted batch distribution that favours collections of samples with large empirical spread after a prescribed linear feature map. The target is specified explicitly, and the sampler is derived as the corresponding Doob $h$-transform of independent diffusion dynamics. The resulting correction has a compact form: an interaction term that repels posterior denoised means, together with a curvature term that moves particles to the region of higher feature variance. This yields an interacting-particle sampler with a transparent probabilistic target rather than a heuristic repulsive drift.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，生成一份结构化的中文总结。

---

## 论文总结：方差倾斜扩散模型用于多样化采样

### 1. 核心问题与研究动机
*   **研究背景**：扩散模型在图像生成、蛋白质设计等领域取得了巨大成功。然而，标准的采样方式（独立同分布采样）在需要**多样化候选集**的下游任务（如药物分子设计、机器人运动规划）中效果不佳。
*   **核心问题**：如何在生成过程中明确地鼓励样本的多样性，而不是孤立地生成高概率样本。
*   **整体含义**：现有方法（如粒子引导）通过引入启发式的排斥力来提升多样性，但其修改后的采样分布是隐式且计算上难以控制。本文旨在提出一种“**目标优先**”的方法，即首先定义一个鼓励多样性的显式概率分布，然后精确推导出实现该分布的采样器。

### 2. 方法论
*   **核心思想**：提出一种**方差加权批分布**，该分布倾向于生成一组在线性特征映射后经验方差较大的样本。即，样本在保持原始数据分布 \(p_0\) 的可能性的同时，在特定特征空间（由线性算子 \(A\) 定义）中彼此分散。
*   **关键公式与定义**：
    *   **目标分布**：\[ \pi^A_0(\mathbf{x}) \propto \text{Var}^A_n(\mathbf{x}) \prod_{i=1}^n p_0(\mathbf{x}^{(i)}) \]
        其中经验方差定义为：\[ \text{Var}^A_n(\mathbf{x}) := \frac{1}{n} \sum_{i=1}^n \|A(\mathbf{x}^{(i)} - \bar{\mathbf{x}})\|^2 \]
    *   **Doob h-变换**：为了从该分布采样，利用Doob h-变换对反向扩散过程的得分函数进行修正。修正后的目标得分为：\[ \nabla \log q_t(\mathbf{x}_t) = s_t(\mathbf{x}_t) + \nabla \log h^A_t(\mathbf{x}_t) \]
        其中 \(h^A_t(\mathbf{x}_t)\) 是后验期望的方差。
*   **关键技术细节**：
    *   **封闭形式的Doob修正**：通过Tweedie公式和偏差-方差分解，将 \(h^A_t\) 分解为两个可计算的项：
        \[ h^A_t(\mathbf{x}_t) = \text{Var}^A_n(\mu_t(\mathbf{x}_t)) + \frac{n-1}{n^2} \sum_{i=1}^n \text{Tr}(A\Sigma_t(\mathbf{x}_t^{(i)})A^\top) \]
    *   **指导项分解**：最终的得分修正 \( \nabla \log h^A_t \) 包含两个部分：
        *   **交互项**：通过向量-雅可比积高效计算，用于排斥后验去噪均值 \(\mu_t\)。
        *   **曲率项**：涉及得分函数的二阶导数，可通过有限差分或 Hutchinson 迹估计器计算，用于引导粒子向特征方差更大的区域移动。
    *   **采样算法**：提出了一个离散化的交互粒子采样器（Algorithm 1），在每一步反向扩散中，每个粒子都接收标准得分加上Doob修正项。

### 3. 实验设计
*   **实验场景与数据集**：论文在**图像生成**任务上进行定性评估。采用 Stable Diffusion v1.5 模型，针对多个不同的文本提示（prompt）生成 512x512 的图像。
*   **Benchmark 与对比方法**：
    *   **基线方法**：标准的**无分类器指导**（CFG）采样。
    *   **本文提出方法**：
        1.  **VT (Variance-Tilted)**：仅使用交互项，不使用曲率项。
        2.  **VT + Divergence**：同时包含交互项和估计的曲率项。
    *   **公平性设置**：为公平比较，不同方法的初始噪声保持一致。
*   **定量指标**：未使用任何定量指标，仅进行**定性比较**。

### 4. 资源与算力
论文中没有明确提及所使用的GPU型号、数量或训练时长。仅提到了采样是在 Stable Diffusion v1.5 上进行的，未提供算力细节。

### 5. 实验数量与充分性
*   **实验数量**：实验部分相对有限，主要展示了几个代表性提示词（如“玻璃鸭”、“柯基犬”、“雪地独角兽”等）的生成结果图。未进行消融研究（例如，对不同批大小 \(n\) 或线性算子 \(A\) 进行系统比较），也未在更广泛的数据集或任务上进行验证。
*   **充分性与客观性**：
    *   **不足**：缺乏定量指标（如FID、LPIPS、Vendi Score等）来客观衡量多样性与图像质量之间的权衡。仅依靠视觉比较，结论的客观性不足。
    *   **偏差风险**：对结果的解读可能存在主观偏差。声称“显著更多样化”，但缺乏数据支撑。

### 6. 主要结论与发现
*   提出的“目标优先”和方差倾斜方法能够生成比标准CFG基线**更多样化**的样本集合。
*   该多样化体现在样本覆盖了更广的**姿态、背景和视觉风格**，而不仅仅是生成高概率模式的微小变体。
*   通过Doob h-变换推导出的采样器具有明确的概率目标，为多样化采样提供了一种更透明、理论上可控的替代方案。

### 7. 主要优点
*   **理论创新与方法严谨性**：从定义显式概率目标出发，并利用Doob h-变换给出精确的采样器推导，具有坚实的理论基础，区别于启发式的粒子排斥方法。
*   **封闭形式的解**：将复杂的得分修正分解为可计算的交互项和曲率项，分别对应一阶和二阶信息，结构清晰。
*   **通用性**：提出的框架适用于一般的线性扩散模型（如VE-SDE、VP-SDE）。
*   **明确的可解释性**：通过偏差-方差分解，清晰地揭示了修正项中“排斥后验均值”和“寻找高方差区域”两部分的功能。

### 8. 不足与局限
*   **对图像质量的负面影响**：论文自身承认，在提升多样性的同时，该方法可能对生成图像的质量产生负面影响。
*   **计算成本较高**：特别是包含完整曲率项时，需要估计二阶导数（如通过有限差分或Hutchinson估计），显著增加了采样计算开销。
*   **实验验证不足**：
    *   完全依赖**定性评估**，缺乏与现有SOTA多样化采样方法（如粒子引导）的**定量对比**。
    *   仅测试了 \(A = \mathbf{Id}\) 的简单情况，未探索线性算子 \(A\) 作为特征提取器（如DCT系数、坐标掩码等）的潜力。
    *   缺少关键的**消融实验**，例如分析曲率项、交互项各自的贡献，以及超参数（如 \( \lambda \) ）的影响。
*   **应用范围限制**：论文实验仅聚焦于图像生成，其文本提示和模型选择相对基础，其有效性在更复杂的科学应用（如分子生成）中尚待验证。

（完）
