---
title: Diffusion Approximations to Schrödinger Bridges and the Convergence of Entropic Potentials
title_zh: 薛定谔桥的扩散逼近与熵势的收敛性
authors: "Garrett Mulcahy, Soumik Pal"
date: 2026-09-18
pdf: "https://arxiv.org/pdf/2609.22595v1"
tags: ["query:diff-bridge"]
score: 9.0
evidence: 熵Schrödinger桥与熵势的收敛性
tldr: 论文研究二次代价下欧氏密度间的Monge-Kantorovich最优传输，以及ε-Schrödinger桥和ε熵Brenier映射。通过对耦合取条件期望得到熵Brenier映射，作者证明在边际光滑且对数凹时，其与Brenier映射之差等于ε乘以第一边际score函数的一半，并加L²(μ)中o(ε)误差。该展开不依赖第二边际，且借助McCann插值的加噪版本与Mirror Langevin扩散给出两种逼近。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.22595v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1139, \"height\": 845, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.22595v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1575, \"height\": 566, \"label\": \"Figure\"}]"
motivation: 刻画熵正则最优传输随正则参数ε→0的渐近行为，并连接Schrödinger桥与Brenier映射。
method: 用McCann插值的加噪版本逼近Schrödinger桥，并在额外假设下引入Mirror Langevin扩散与切马尔可夫投影。
result: 在边际光滑、对数凹下，熵Brenier映射与Brenier映射之差为ε/2乘第一边际score加L²(μ)中o(ε)误差。
conclusion: 该展开不依赖第二边际，并揭示两类扩散逼近的内在联系，为熵势收敛提供新工具。
---

## 摘要
考虑在二次代价下，两个欧氏密度 μ 与 ν 之间的 Monge-Kantorovich 最优传输问题。ε-薛定谔桥是正则化参数为 ε 的熵正则化问题的解。在此耦合下，对给定第一坐标的第二坐标取条件期望，便得到 ε-熵 Brenier 映射。当 ε 降至零时，已知熵 Brenier 映射收敛到这两个测度之间的二次代价最优传输映射，即 Brenier 映射。我们证明，在对边缘分布施加某些光滑性与对数凹性约束下，熵 Brenier 映射与 Brenier 映射之差等于 ε 乘以第一边缘分布得分函数的一半，再加上一个在 L^2(μ) 中为 o(ε) 的误差。该展开与第二边缘分布 ν 无关。证明依赖于用 McCann 插值的加噪版本对薛定谔桥进行近似。在额外假设下，我们通过所谓的镜像 Langevin 扩散给出薛定谔桥的第二种近似，这些扩散是 Brenier 映射所生成的 Hessian 流形上的 Langevin 扩散。这两种近似乍看之下相当不同，但被证明密切相关。我们的证明利用了一个随机曲面以及一种称为切向 Markov 投影的新颖随机操作。

## Abstract
Consider the Monge-Kantorovich optimal transport problem between two Euclidean densities $μ$ and $ν$ and quadratic cost. The $\varepsilon$-Schrödinger bridge is the solution to the entropic regularized problem with regularization parameter $\varepsilon$. By taking the conditional expectation of the second coordinate given the first under this coupling, we obtain the $\varepsilon$-entropic Brenier map. As $\varepsilon$ goes down to zero, it is known that the entropic Brenier map converges to the quadratic cost optimal transport map between the two measures, i.e., the Brenier map. We show that, under some smoothness and log-concavity constraints on the marginals, the difference between the entropic Brenier map and the Brenier map is equal to $\varepsilon$ times one-half of the score function of the first marginal plus an error that is $o(\varepsilon)$ in $\mathbf{L}^2(μ)$. This expansion holds irrespective of the second marginal $ν$. The proof relies on an approximation of the Schrödinger bridge by a noisy version of the McCann interpolation. Under additional assumptions we show a second approximation to the Schrödinger bridge via so-called Mirror Langevin diffusions, which are Langevin diffusions on the Hessian manifold generated by the Brenier map. These two approximations, that appear quite different at first glance, are nonetheless shown to be closely connected. Our proofs utilize a random surface and a novel stochastic operation called the tangent Markov projection.

---

## 论文详细总结（自动生成）

# 论文总结：《薛定谔桥的扩散逼近与熵势的收敛性》

## 1. 核心问题与研究背景

- **研究对象**：二次代价下的 Monge–Kantorovich 最优传输问题
  \[
  W_2^2(\mu,\nu):=\inf_{\pi\in\Pi(\mu,\nu)}\int_{\mathbb{R}^d\times\mathbb{R}^d}\lVert x-y\rVert^2\,d\pi .
  \]
  当 \(\mu\) 有 Lebesgue 密度时，最优传输计划由 Brenier 映射 \(\nabla\varphi\)（凸函数的梯度）诱导。

- **熵正则化视角**：对固定 \(\varepsilon>0\)，\(\varepsilon\)-薛定谔桥是
  \[
  \pi_\varepsilon:=\arg\min_{\pi\in\Pi(\mu,\nu)}\Big(\int\tfrac12\lVert x-y\rVert^2d\pi+\varepsilon H(\pi|\mu\otimes\nu)\Big),
  \]
  其 Radon–Nikodym 导数具有乘积结构 \(d\pi_\varepsilon/dr_\varepsilon(x,z)=a_\varepsilon(x)b_\varepsilon(z)\)，由此定义**熵势** \(f_\varepsilon=\varepsilon\log a_\varepsilon,\ g_\varepsilon=\varepsilon\log b_\varepsilon\)。

- **熵 Brenier 映射**：\(B_\varepsilon(x):=\mathbb{E}_{\pi_\varepsilon}[Y\mid X=x]\)。已知（Pooladian–Niles-Weed、Chiarini–Conforti–Greco–Tamanini）当 \(\varepsilon\downarrow0\) 时 \(B_\varepsilon\to\nabla\varphi\) 于 \(L^2(\mu)\)。**但收敛速率此前未被确定**，仅有 Mordant 的 WKB 非正式论证与 Leger–Vialard 的 Kim–McCann 几何 Laplace 方法。

- **本文核心目标**：确定一阶渐近项。主要定理指出，在边际光滑且强对数凹的条件下，
  \[
  \lim_{\varepsilon\downarrow0}\frac{1}{\varepsilon}\Big(\mathbb{E}_{\pi_\varepsilon}[Y\mid X=x]-\nabla\varphi(x)\Big)=-\tfrac12\nabla f(x),\qquad \text{于 } L^2(e^{-f}),
  \]
  等价地 \(\lim_{\varepsilon\downarrow0}\frac{1}{\varepsilon}\big(\nabla f_\varepsilon(x)-(x-\nabla\varphi(x))\big)=-\tfrac12\nabla f(x)\)。

- **含义**：一阶修正项**只依赖第一边际的 score 函数** \(-\nabla f\)，与第二边际 \(\nu\) 无关。这给出熵 Brenier 映射稳定性的精细刻画：对同一 \(\mu\) 到 \(\nu_1,\nu_2\) 的两个映射，其差与对应 Brenier 映射之差相差 \(o(\varepsilon)\)，优于平凡估计的 \(o(1)\)。

## 2. 方法论：核心思想与关键技术

### 2.1 出发点：熵代价的 Taylor 展开

- 采用 Conforti–Tamanini 的展开（本文在非紧设定下推广）：
  \[
  H(\pi_\varepsilon|r_\varepsilon)=\frac{1}{2\varepsilon}W_2^2(e^{-f},e^{-h})+\frac12\big(\mathrm{Ent}(e^{-f})+\mathrm{Ent}(e^{-h})\big)+\frac{\varepsilon}{8}\int_0^1 I(\rho^0_t)\,dt+o(\varepsilon),
  \]
  其中 \((\rho^0_t)_{t\in[0,1]}\) 为 **McCann 插值**，\(I(\rho)=\mathbb{E}_\rho\lVert\nabla\log\rho\rVert^2\) 为 Fisher 信息。

- **总体策略（四步思想实验）**：
  1. 构造一族耦合 \((q^\varepsilon)\)，使其相对熵 \(H(q^\varepsilon|r_\varepsilon)\) 与上式右端**逐项匹配至 \(o(\varepsilon)\)**；
  2. 利用相对熵的"勾股定理"（Pythagorean theorem）推出 \(H(q^\varepsilon|\pi_\varepsilon)=o(\varepsilon)\)；
  3. 通过泛函不等式把 \(L^2\) 极限从 \(q^\varepsilon\) 传递到 \(\pi_\varepsilon\)；
  4. 用第二种（MLD）构造揭示定性性质（条件协方差等）。

### 2.2 第一类逼近：\(\varepsilon\)-加噪 McCann 插值

- McCann 插值：\(\nabla\varphi_{0\to t}=(1-t)\mathrm{Id}+t\nabla\varphi\)，\(\rho^0_t=(\nabla\varphi_{0\to t})_\#e^{-f}\)。记 \(\nabla\varphi_{s\to t}\) 为 \(\rho^0_s\to\rho^0_t\) 的 Brenier 映射，切向速度为 \(v^0_t=\nabla\varphi_{t\to1}-\nabla\varphi_{t\to0}\)。

- 引入 **\(\varepsilon\)-加噪 McCann 插值**（SDE）：
  \[
  dZ_t=\Big(\nabla\varphi_{t\to1}-\nabla\varphi_{t\to0}+\frac{\varepsilon}{2}\nabla\log\rho^0_t\Big)(Z_t)\,dt+\sqrt{\varepsilon}\,dB_t,\quad Z_0\sim e^{-f},
  \]
  其时间边际仍严格等于 McCann 插值（由 Fokker–Planck 与连续性方程的等价性保证），但其端点联合分布绝对连续。

- 关键恒等式：其路径测度 \(\tilde{Q}^\varepsilon\) 满足
  \[
  H(\tilde{Q}^\varepsilon|R^\varepsilon)=\frac{1}{2\varepsilon}W_2^2+\frac12\big(\mathrm{Ent}(e^{-f})+\mathrm{Ent}(e^{-h})\big)+\frac{\varepsilon}{8}\int_0^1 I(\rho^0_t)\,dt,
  \]
  **无 \(o(\varepsilon)\) 误差**。与 \(H(P^\varepsilon|R^\varepsilon)\) 相减即得 \(H(\tilde{Q}^\varepsilon|P^\varepsilon)=o(\varepsilon)\)。

- **向 \(\pi_\varepsilon\) 传递极限**：由 Chewi–Pooladian 的条件 Hessian 下界知 \(\pi_\varepsilon(\cdot|x)\) 满足 Talagrand 不等式（常数 \(\varepsilon/\kappa\)），配合粘合耦合（gluing coupling）构造三元组 \((X,Y,Z)\)，得到
  \[
  \frac{1}{\varepsilon}\Big\lVert \mathbb{E}_{\pi_\varepsilon}[Y|X=x]-\mathbb{E}_{\tilde\ell_\varepsilon}[Y|X=x]\Big\rVert_{L^2(e^{-f})}\le\sqrt{\frac{2}{\varepsilon^2\kappa}H(\tilde\ell_\varepsilon\|\pi_\varepsilon)}\to0 .
  \]

- **Proposition 4 的技术核心**：对 \(Z_t=\nabla\varphi_{t\to1}(Y_t)\) 做中心化变量替换，证明漂移可写成散度形式
  \[
  \gamma^\varepsilon_t(z)=-\frac{\varepsilon}{2}\big(A_t(z)\nabla h(z)-\nabla_z\cdot A_t(z)\big),\quad A_t(z)=\big(\nabla^2\varphi_{1\to t}(z)\big)^{-2},
  \]
  并用**矩阵恒等式** \(\int_0^1(\nabla^2\varphi_{1\to t}(y))^{-2}dt=(\nabla^2\varphi_{1\to0}(y))^{-1}\) 证明
  \[
  \int_0^1\gamma^\varepsilon_t(z)\,dt=-\frac{\varepsilon}{2}\nabla f(\nabla\varphi^*(z)),
  \]
  即积分漂移恰为对偶 MLD 的生成元作用——这是两种逼近内在联系的第一个"奇迹"。

### 2.3 第二类逼近：镜像 Langevin 扩散（MLD）与切向 Markov 投影

- **MLD**（Hessian 流形 \(\nabla^2\varphi\) 上的 Langevin 扩散）：
  \[
  dX_t=-\tfrac12\nabla h(Y_t)dt+\sqrt{\nabla^2\varphi^*(Y_t)}\,dB_t,\qquad dY_t=-\tfrac12\nabla f(X_t)dt+\sqrt{\nabla^2\varphi(X_t)}\,dB_t,
  \]
  分别以 \(e^{-f}\)、\(e^{-h}\) 为不变测度（\(Y_t=X^*_t\)）。取 \(\ell_\varepsilon=\mathrm{Law}(X_0,X^*_\varepsilon)\) 得
  \[
  \mathbb{E}_{\ell_\varepsilon}[Y|X=x]=\nabla\varphi(x)-\frac{\varepsilon}{2}\nabla f(x)+o(\varepsilon),
  \]
  但此前只知 \(H(\ell_\varepsilon|\pi_\varepsilon)=o(1)\)，无法直接给出 \(o(\varepsilon)\)。

- **切向 Markov 投影**（本文核心新工具）：
  - 以 \(\ell_\varepsilon\) 为端点、用 \(\varepsilon\)-Brownian 桥连接，得**互反过程** \(t\mapsto(1-t)X_0+tX_1+\sqrt{\varepsilon}Z_t\)（非 Markov）；
  - 先做经典 **Markov 投影**：\(\hat\beta^\varepsilon_t(x)=\mathbb{E}_{Q^\varepsilon}[\beta^\varepsilon_t(X_0,X_t)|X_t=x]\)；
  - 再做 **切向投影**：\(\beta^\varepsilon_t:=\arg\min_{\beta_t\in\mathrm{Tan}(\mu_t)}\lVert\hat\beta^\varepsilon_t-\beta_t\rVert_{L^2(\mu_t)}^2\)，其中 \(\mathrm{Tan}(\mu)=\overline{\{\nabla\phi:\phi\in C_c^\infty\}}^{L^2(\mu)}\)。
  - **Proposition 2**：切向投影漂移由纯度量量决定：\(\beta^\varepsilon_t=v_t+\frac{\varepsilon}{2}\nabla\log\mu_t\)。
  - **Proposition 3（相对熵勾股定理）**：对任意梯度型漂移 \(\gamma_t\in\mathrm{Tan}(\mu_t)\) 的 Markov 过程 \(N\)，\(H(Q^\varepsilon|N)=H(Q^\varepsilon|Q^\varepsilon)+H(Q^\varepsilon|N)\)。
  - **Theorem 1**：\(H(Q^\varepsilon|R^\varepsilon)=\frac{1}{2\varepsilon}\int_0^1|(\mu_t)'|^2dt+\frac12(\mathrm{Ent}\mu_0+\mathrm{Ent}\mu_1)+\frac{\varepsilon}{8}\int_0^1 I(\mu_t)dt\)。

- **随机曲面（reciprocal surface）**：定义 \(U(t,s)=(1-t)X_0+tX^*_s+\sqrt{t(1-t)}\,W_s\)，\(\mathrm{Law}(U(t,s))=\rho^s_t\)。两个视角：
  - 固定 \(t\)、令 \(s\approx0\)：\((U(t,s))_s\) 近似为 **\(t\)-对偶 MLD**（时间按 \(t\) 缩放），由于它是平稳过程，故 **Lemma 1**：\(\partial_s\rho^s_t|_{s=0}=0\)；
  - 固定 \(s\approx0\)、变动 \(t\)：其切向 Markov 投影近似为 \(s\)-加噪 McCann 插值。
  - 这一"横截"结构把 Hessian 几何与 McCann 插值的平坦几何联系起来。

- **动能界（Theorem 4）**：\(\frac12\int_0^1|(\rho^\varepsilon_t)'|^2dt=\frac12W_2^2+o(\varepsilon^2)\)，进而 \(\int_0^1\lVert v^\varepsilon_t-v^0_t\rVert^2_{L^2(\rho^\varepsilon_t)}dt=o(\varepsilon^2)\)。证明分三步：
  1. 用 Hamilton–Jacobi 解 \(\psi_t\) 作检验函数，得到动能与 \(W_2^2\) 之差等于速度差范数平方；
  2.
