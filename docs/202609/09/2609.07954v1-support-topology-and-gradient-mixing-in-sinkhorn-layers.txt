Title: Support Topology and Gradient Mixing in Sinkhorn Layers

URL Source: https://arxiv.org/pdf/2609.07954v1

Published Time: Wed, 09 Sep 2026 02:46:38 GMT

Number of Pages: 25

Markdown Content:
Preprint Dylan B. Forde 

# Transport Topology for Fixed-Support Sinkhorn Layers 

Dylan B. Forde Independent Researcher 

forde.dylan@gmail.com 

Abstract 

Fixed-support Sinkhorn scaling exposes an exact quotient Markov operator. For finite active scores, compatible positive marginals, and a fixed support Ω, a finite row-column cycle has homogeneous column-potential linearization δv + = C⊤ 

> t

Rt δv , with Rt = diag( a)−1P rt and Ct = P ct diag( b)−1 formed from the two actual half-step plans. At a balanced fixed point these plans coincide with P , giving 

M = diag( b)−1P ⊤ diag( a)−1P . Both derivative forms are row-stochastic, descend to potentials modulo constants, and their transposes govern homogeneous zero-mass quotient cotangents. This paper makes that fixed-support calculus self-contained, including score/source terms in the first-order half-step equations and the projected-source tail recurrence ηt = M ⊤ 

> t

ηt+1 + ξt. Dobrushin and minorization coefficients then give rigorous homogeneous and inhomogeneous tail-cotangent bounds for this quotient-transport component. The main structural result is a face-lattice dichotomy: a support–marginal pair has a score-uniform one-step quotient-mixing certificate if and only if every feasible face of its transportation polytope has pairwise two-hop column overlap; otherwise finite score directions can make τ (M ) arbitrarily close to one. Windowed products, partition heat-bath supports, product-coordinate sweeps, forced bus mass, and register examples are proved as support-schedule certificates. The validation scope is deliberately certificate-level: small exact VJPs, quotient implicit VJPs, cost benchmarks, generated-score stress tests, and adversarial faces check theorem objects and failure modes, not system speedups or downstream accuracy. 

# 1 Introduction 

Block-Wise Differentiable Sinkhorn Attention (Forde, 2026) studies the local differentiated-surrogate problem for fixed-support Sinkhorn attention: after a stopped base solve, a short refinement tail is differentiated exactly and scheduled blockwise. The question here is different and complementary: given a fixed support or scheduled family of supports, which topologies make the induced quotient reverse dynamics mix, trap, or require windowed products? Transformer architecture papers often separate topology from differentiation. The topology specifies who can attend to whom, which registers or global tokens exist, how experts are reachable, and how residual branches merge; the gradient analysis then treats the resulting computation graph as given. For Sinkhorn-style transport attention, this separation leaves useful structure on the table. Once the support is fixed, a transport layer is a matrix-scaling problem, and a full scaling step has an explicit Markov kernel in reverse mode. The support graph therefore constrains not only the FLOP pattern but also which cotangent modes can mix, which modes remain trapped, and which residual paths can amplify truncation error. This paper proposes the following design principle: Sparse transport topologies should be evaluated by quotient-gradient mixing, not only by density, graph diameter, or token reachability. The coefficient is a derivative certificate, not a generic graph statistic. A fixed support and a score-induced plan produce a testable operator for reverse-mode transport, and the resulting certificate distinguishes dense masks, narrow local windows, disconnected supports, global-token supports, register-augmented supports, and residual DAGs. In the equal-score biregular baseline the certificate is a pure graph quantity; away from that baseline it is controlled by the realized scaled plan, plan-distortion bounds, or mass-floor hypotheses. 

Mathematical setting. The scope is fixed support, compatible positive marginals, finite active scores, and quotient cotangents. The theory proves exact statements about the fixed-support quotient-transport component. It does not prove support-changing training rules, production kernels, hardware speedups, or task-quality gains. When neural parameters, values, Q/K/V maps, residual branches, or losses add source terms, the theorem applies after those source cotangents are projected and their norms are bounded separately. 1arXiv:2609.07954v1 [cs.AI] 7 Sep 2026 Preprint Dylan B. Forde 

Matrix scaling has classical foundations in Sinkhorn and Knopp (1967); Idel (2016) surveys existence, sup-port conditions, and convergence. Entropic optimal transport connects this scaling problem to computational transport (Cuturi, 2013; Peyré and Cuturi, 2019). 

# 2 Fixed-Support Sinkhorn Calculus 

Let Ω ⊆ [m] × [n] be a fixed active support with row neighborhoods Ωi = {j : ( i, j ) ∈ Ω} and column neighborhoods Ωj = {i : ( i, j ) ∈ Ω}. Let a ∈ Rm>0 and b ∈ Rn>0 be compatible marginals with 1⊤a = 1⊤b = T .Scores are dimensionless active-edge scores sij ∈ R for (i, j ) ∈ Ω; inactive entries are omitted, or viewed as 

−∞ . If raw logits are θij at temperature ε > 0, then sij = θij /ε , so raw-score source terms below acquire the factor 1/ε .A log-potential pair (u, v ) ∈ Rm × Rn represents the active-edge plan 

Pij (u, v ; s) = exp( sij + ui + vj ) (( i, j ) ∈ Ω) , Pij = 0 (( i, j ) /∈ Ω) .

The pair is a Sinkhorn solution when P 1 = a and P ⊤1 = b. The potentials have gauge freedom: on each connected component of the bipartite support graph, adding a constant to all row potentials and subtracting the same constant from all column potentials leaves P unchanged. The plan and all kernels below are gauge-invariant. 

Assumption 1 (Fixed support, finite scores, and feasible marginals) . The support Ω is fixed during differentiation, all active scores are finite, and every connected component of the active bipartite support has equal row and column marginal mass. When strict positivity on active edges is needed, we assume that 

Π( a, b ; Ω) contains a relative-interior point positive on every active edge. 

For differentiation of transport layers, Eisenberger et al. (2022) develop implicit gradients with respect to costs and target capacities, while Pauwels and Vaiter (2023) establish convergence of differentiated Sinkhorn iterates under their hypotheses. The calculation below makes the individual half-step source terms explicit and distinguishes a finite cycle from a balanced fixed point. 

## 2.1 Half-Step Equations and Linearization 

Given a column potential v, the exact row-normalization half-step is 

u+ 

> i

(v; s, a ) = log ai − log X

> j∈Ωi

exp( sij + vj ), (1) 

P rij = exp( sij + u+ 

> i

(v; s, a ) + vj ), X

> j

P rij = ai. (2) Given a row potential u, the exact column-normalization half-step is 

v+ 

> j

(u; s, b ) = log bj − log X

> i∈Ωj

exp( sij + ui), (3) 

P cij = exp( sij + ui + v+ 

> j

(u; s, b )) , X

> i

P cij = bj . (4) A full row-column cycle maps v to T (v) = v+(u+(v; s, a ); s, b ). At a fixed point of this map, the plan P has both marginals. For a balanced fixed-point plan P , define 

R = diag( a)−1P ∈ Rm×n, C = P diag( b)−1 ∈ Rm×n.

Rows of R sum to one and columns of C sum to one. For an active-edge score perturbation ˙s, define the row and column score averages 

ri( ˙ s) = X

> j∈Ωi

Rij ˙sij , cj ( ˙ s) = X

> i∈Ωj

Cij ˙sij .

For marginal perturbations write da = ˙ a ⊘ a and db = ˙b ⊘ b, componentwise. 2Preprint Dylan B. Forde 

Proposition 1 (Full first-order half-step linearization) . At a balanced fixed-point plan P with positive marginals, the row half-step and column half-step have differentials 

˙u+ = da − R ˙v − r( ˙ s), (5) 

˙v+ = db − C⊤ ˙u+ − c( ˙ s). (6) 

Consequently one full row-column cycle on column potentials has linearization 

˙v+ = C⊤R ˙v + db − C⊤da + C⊤r( ˙ s) − c( ˙ s)

| {z } 

> ζ( ˙ s, ˙a, ˙b)

. (7) 

For fixed marginals, the source term is ζ( ˙ s) = C⊤r( ˙ s) − c( ˙ s). For raw logits θ at temperature ε, substitute 

˙s = ˙θ/ε .Proof. Differentiate (1). The derivative of the log-sum-exp normalizer in row i is the Rij -weighted average of 

˙sij + ˙ vj , because at the fixed point Rij = Pij /a i. This gives ˙u+ 

> i

= ˙ ai/a i − P 

> j

Rij ˙vj − P 

> j

Rij ˙sij , which is (5). Differentiating (3) gives ˙v+ 

> j

= ˙bj /b j − P 

> i

Cij ˙u+ 

> i

− P 

> i

Cij ˙sij , which is (6). Substituting the first display into the second gives (7). The signs come from the fact that both half-steps subtract a log normalizer; the two minus signs in the homogeneous composition yield the positive map C⊤R.

Finite-cycle extension: two distinct half-step plans. The preceding single-plan formulas are evaluated at a balanced fixed point. For an actual finite cycle, let P rt and P ct be the row-normalized and column-normalized plans from (1)–(3), and define 

Rt = diag( a)−1P rt , Ct = P ct diag( b)−1, Mt = C⊤ 

> t

Rt.

With rt( ˙ s)i = P 

> j

(Rt)ij ˙sij and ct( ˙ s)j = P

> i

(Ct)ij ˙sij , direct differentiation gives 

˙v+ = Mt ˙v + db − C⊤ 

> t

da + C⊤ 

> t

rt( ˙ s) − ct( ˙ s).

Indeed, each log-normalizer differentiates to the conditional probabilities of its own half-step, and composing the two minus signs gives Mt. Rows of Rt and columns of Ct are normalized, so Mt1 = 1. Transposing this identity gives the source ledger in Proposition 2 with Rt, C t in place of R, C . In general P rt̸ = P ct ; neither plan may be substituted for both. The finite kernel need not be reversible or have stationary mass b. The general stochastic-kernel tail bounds below apply to these actual Mt. By contrast, the single-plan support weights, face-lattice characterization, and stationary implicit system concern balanced feasible plans, not arbitrary unbalanced intermediate iterates. 

Definition 1 (Quotient convention) . For a connected support, column potentials are functions modulo additive constants. We write Q = Rn/span {1} and identify its dual with 

Q∗ = {η ∈ Rn : 1⊤η = 0 }.

The total-variation norm on Q∗ is ∥η∥TV = 12 ∥η∥1. Centering, for example Π0x = x − (1⊤x/n )1, chooses a representative only. Since the kernels below are row-stochastic, they satisfy M 1 = 1 and therefore M ⊤Q∗ ⊆ Q∗.Dobrushin certificates are statements on this quotient. If the bipartite support has components Γ with column sets JΓ, the full potential gauge space is GΩ =span {1JΓ : Γ } and the exact component quotient is 

QΩ = Rn/G Ω, Q∗ 

> Ω

= η : 1⊤ 

> JΓ

η = 0 for every Γ .

Every single-support statement below remains valid componentwise after replacing (Q, Q ∗) by (QΩ, Q ∗

> Ω

) and centering separately on each JΓ. For schedules whose component partitions change, the global quotient Q is the common state space. Its Dobrushin coefficient may equal one because it retains intercomponent Markov modes; that value must not be reported as a sharp contraction rate on the smaller component quotient. 

3Preprint Dylan B. Forde 

## 2.2 Derivative Kernel and Overlap 

At a balanced fixed point, the column-to-column homogeneous full-step kernel is 

M = C⊤R ∈ Rn×n.

It is row-stochastic, and b⊤M = b⊤, so b is a stationary distribution. The map M is the fixed-support quotient operator for the homogeneous part of one row-column Sinkhorn cycle; source terms enter through ζ

in (7) and through the projected cotangent sources in Section 3. 

Proposition 2 (Adjoint source ledger for one fixed-support cycle) . Let η+ ∈ Rn be a cotangent on the output column potential v+ of one row-column cycle at a balanced fixed point. For a finite cycle, use the two-half-step extension above. The homogeneous cotangent on the input column potential is 

η− = M ⊤η+.

The cotangents carried by the non-homogeneous source variables in (7) are 

λb = η+, λa = −Cη +, (8) 

gsij = Rij (Cη +)i − Cij η+ 

> j

, (i, j ) ∈ Ω. (9) 

Here λa and λb are cotangents to the logarithmic marginal perturbations da = ˙ a⊘a and db = ˙b⊘b. Equivalently, the raw marginal and raw-logit cotangents are 

ga = −(Cη +) ⊘ a, gb = η+ ⊘ b, gθij = gsij /ε 

when sij = θij /ε .The displayed marginal covectors are unconstrained source covectors. If the marginals are parameterized inside the compatible fixed-component marginal manifold, only their quotient classes are observable. For each connected component Γ = ( IΓ, J Γ) of the support graph, admissible log-marginal perturbations satisfy 

X

> i∈IΓ

ai(da)i = X

> j∈JΓ

bj (db)j .

Hence the log-marginal cotangent pair (λa, λ b) is defined modulo the component normal (aIΓ , −bJΓ ). Equiva-lently, any representative 

(λa, λ b) − αΓ(aIΓ , −bJΓ )

gives the same derivative on admissible perturbations. A concrete Euclidean orthogonal representative is obtained with 

αΓ =

P 

> i∈IΓ

ai(λa)i − P 

> j∈JΓ

bj (λb)j

P 

> i∈IΓ

a2 

> i

+ P 

> j∈JΓ

b2

> j

.

In raw marginal coordinates the analogous quotient is modulo (1IΓ , −1JΓ ). If only one marginal family is varied while the other is fixed, this reduces to the usual componentwise fixed-mass projection of that marginal covector modulo constants on the varied component. If each component mass is fixed separately in both marginal families, then admissible raw variations obey 1⊤ 

> IΓ

˙a = 0 and 1⊤

> JΓ

˙b = 0 independently. The raw cotangents are then defined modulo (1IΓ , 0) and (0 , 1JΓ ) independently; in logarithmic coordinates the corresponding normals are (aIΓ , 0) and (0 , b JΓ ). Thus the projection is determined by the actual marginal parameterization: one joint normal when common component mass may vary, two normals when that mass is fixed. If surrounding differentiable maps feed these source variables from the input quotient coordinates at step t,their VJPs compose with (8) –(9) and are then projected to Q∗. Thus the tail recurrence used below has the form 

ηt = M ⊤ 

> t

ηt+1 + Π Q∗ ψt,

where ψt denotes the aggregate non-transport cotangent delivered to the input column-potential coordinates before quotient projection. The displayed source adjoints are the complete one-cycle ledger; they are not additional applications of M ⊤.

4Preprint Dylan B. Forde 

Proof. Pair an arbitrary perturbation with (7): 

⟨η+, ˙v+⟩ = ⟨η+, M ˙v⟩ + ⟨η+, d b⟩ − ⟨ η+, C ⊤da⟩ + ⟨η+, C ⊤r( ˙ s)⟩ − ⟨ η+, c ( ˙ s)⟩.

The first term is ⟨M ⊤η+, ˙v⟩, giving η− = M ⊤η+. The next two terms give λb = η+ and λa = −Cη +. For scores, 

⟨η+, C ⊤r( ˙ s)⟩ = X

> i

(Cη +)i

X

> j

Rij ˙sij , ⟨η+, c ( ˙ s)⟩ = X

> j

η+

> j

X

> i

Cij ˙sij .

Collecting the coefficient of each active ˙sij gives (9). The raw marginal formulas follow from da = ˙ a ⊘ a

and db = ˙b ⊘ b, and the raw-logit formula from s = θ/ε . The componentwise marginal projection statement is just duality for the linear compatibility constraint a⊤ 

> IΓ

da,I Γ − b⊤ 

> JΓ

db,J Γ = 0 : adding any multiple of its normal to the covector pairs to zero with every admissible perturbation. Finally, quotient projection to Q∗ is necessary for cotangents returned to column-potential quotient coordinates; source-space cotangents use the corresponding source constraints described above. 

Lemma 1 (Bridge from fixed-support half-steps to quotient topology) . Let P be a realized active-support Sinkhorn plan with compatible marginals (a, b ). For homogeneous perturbations of the column log potential, 

˙v+ = C⊤R ˙v = M ˙v. 

Hence the homogeneous reverse cotangent propagation on Q∗ is governed by M ⊤.Proof. Set ˙s = 0 , ˙a = 0 , and ˙b = 0 in Proposition 1. The resulting forward linear map is M = C⊤R.Equivalently, the homogeneous part of Proposition 2 is η− = M ⊤η+. By Definition 1, M ⊤ preserves zero-mass cotangents and is independent of the chosen centered representative. 

Definition 2 (Transport topology certificate) . For a fixed support Ω, score-induced plan P , and quotient operator M = C⊤R, define 

τ (M ) = 12 max 

> p,q

X

> k

|Mpk − Mqk |.

We call τ (M ) the one-step Dobrushin transport-topology certificate. When τ (M ) < 1, the realized fixed-support quotient operator contracts all zero-mass cotangents. When τ (M ) = 1 , this certificate withholds a global one-step contraction guarantee. 

Theorem 1 (Support-to-kernel topology) . For α, β ∈ [n], the full-step derivative kernel satisfies 

Mαβ = X

> i

Piα Piβ 

bαai

.

If all active plan entries are strictly positive, then 

Mαβ > 0 ⇐⇒ ∃i such that (i, α ) ∈ Ω and (i, β ) ∈ Ω.

Thus the support of each row of M is exactly the two-hop column co-neighborhood induced by the bipartite support Ω.Proof. Substitute C = P diag( b)−1 and R = diag( a)−1P into M = C⊤R:

Mαβ = X

> i

Ciα Riβ = X

> i

Piα 

bα

Piβ 

ai

.

Every summand is nonnegative. Under strict positivity on active edges, a summand is positive exactly when row i is incident to both columns α and β.

Corollary 1 (Qualitative overlap criterion) . Under the same strict-positivity assumption, τ (M ) < 1 if and only if every pair of rows of the two-hop kernel support overlap. Equivalently, τ (M ) = 1 if and only if some two columns have disjoint two-hop co-neighborhoods. Proof. Dobrushin’s coefficient is strictly below one exactly when every pair of row distributions has positive common mass. Theorem 1 identifies those row supports with the two-hop co-neighborhoods. 5Preprint Dylan B. Forde 

Main theorem spine. The rest of the main theory asks when this overlap is forced rather than merely observed. The equal-score biregular case gives a graph-walk baseline; plan-distortion and zero-temperature arguments explain how scores can expose bad faces; the face-lattice theorem gives the exact score-uniform one-step criterion; and the windowed face-lattice plus partition heat-bath results show how scheduled sparse layers can mix even when every individual layer has τ = 1 .

## 2.3 Equal-Score Topology Baseline and Plan Distortion 

Theorem 2 (Biregular equal-score topology kernel) . Let Ω ⊆ [m] × [n] be a fixed bipartite support with constant positive row degree dr and constant positive column degree dc. Use uniform compatible marginals 

ai = 1 /m and bα = 1 /n , and set all active effective scores equal. Let E = |Ω| = md r = nd c. Then the Sinkhorn-scaled plan is 

Piα = 1

E 1{(i, α ) ∈ Ω}.

Consequently, 

Mαβ = Nαβ 

dcdr

, Nαβ = # {i : (i, α ) ∈ Ω, (i, β ) ∈ Ω}.

Thus, in this baseline, M is exactly the two-step random walk on the bipartite support graph: from column α,choose an incident row uniformly, then choose an incident column of that row uniformly. In particular, τ (M )

and τ (M K ) are pure topology quantities. Proof. Since |Ω| = md r = nd c, the displayed plan has row sums dr /|Ω| = 1 /m and column sums dc/|Ω| = 1 /n .It is therefore a valid Sinkhorn scaling of the equal-score active matrix; uniqueness of the entropic matrix-scaling plan fixes this primal plan even if the support has multiple compatible connected components. The dual scalings may have separate component gauges, but P and M are unchanged. For active edges, 

Riα = Piα /a i = 1 /d r and Ciα = Piα /b α = 1 /d c. Substituting into M = C⊤R gives 

Mαβ = X

> i

1{(i, α ) ∈ Ω}1{(i, β ) ∈ Ω}

dcdr

= Nαβ 

dcdr

.

The row sum is one because each of the dc rows incident to α has dr incident columns. Theorem 2 is the cleanest fixed-support certificate baseline in this paper. For equal-score biregular supports, a sparse attention topology has an exact Markov-chain representation: its Sinkhorn linearization is the Markov chain on the column-overlap graph. Score-dependent and non-biregular cases then become perturbations or realized-plan certificates rather than graph-only guarantees. If the support has multiple closed column-overlap components, the same formula holds componentwise but the global Dobrushin coefficient remains one, so the theorem does not certify mixing across disconnected topology. 

Theorem 3 (Plan-distortion stability around a topology kernel) . Let Ω be biregular with row degree dr,column degree dc, uniform marginals, and E = |Ω|. Let M0 be the equal-score topology kernel from Theorem 2. Let P be any other feasible plan on the same support with the same uniform marginals, and write 

qiα = EP iα (( i, α ) ∈ Ω) .

If for some κ ≥ 1,

κ−1 ≤ qiα ≤ κ for every active edge (i, α ) ∈ Ω,

then the realized derivative kernel M = C⊤R satisfies the elementwise comparison 

M ≥ κ−2M0.

Consequently, 

τ (M ) ≤ 1 − κ−2 1 − τ (M0).

6Preprint Dylan B. Forde 

More generally, if row-stochastic kernels Mt and reference kernels M 0 

> t

on the same finite state space satisfy 

Mt ≥ δtM 0 

> t

elementwise with δt ∈ [0 , 1] for t = 1 , . . . , K , then 

MK · · · M1 ≥

> K

Y

> t=1

δt

!

M 0 

> K

· · · M 01

and hence 

τ (MK · · · M1) ≤ 1 −

> K

Y

> t=1

δt

!  1 − τ (M 0 

> K

· · · M 01 ).

Proof. With uniform marginals, ai = 1 /m , bα = 1 /n , and E = md r = nd c. For the realized plan, 

Mαβ = X

> i

Piα Piβ 

bαai

= 1

dcdr

X

> i: ( i,α ),(i,β )∈Ω

qiα qiβ .

The edgewise distortion assumption gives qiα qiβ ≥ κ−2 for every common incident row, while (M0)αβ is the same sum with each common-row contribution equal to 1/(dcdr ). This proves M ≥ κ−2M0.If two row-stochastic kernels A, B satisfy A ≥ δB elementwise, then A = δB +(1 −δ)N for a row-stochastic kernel N when δ < 1 (with the evident interpretation when δ = 1 ). Thus 

τ (A) ≤ δτ (B) + (1 − δ) = 1 − δ(1 − τ (B)) .

This gives the one-step Dobrushin bound. The product comparison follows by iterating elementwise nonnega-tive matrix multiplication: if Mt ≥ δtM 0 

> t

, then every term in MK · · · M1 dominates the corresponding term in (Q 

> t

δt)M 0 

> K

· · · M 01 . Applying the same mixture argument to the product gives the final display. Theorem 3 is the first bridge from pure topology to score-dependent plans in this paper. It is not a claim that graph support alone controls the magnitude of τ (M ). It says that once a plan distortion factor is observed or independently certified, the topology certificate degrades continuously rather than disappearing. This is the right form for score-robust CPU validation and for score-range corollaries. 

## 2.4 Score Gauges and Cycle Sensitivity 

Proposition 3 (Additive scores are topology gauges) . Fix a support Ω and compatible marginals a, b , and assume strict feasibility: there exists Q ∈ Π( a, b ; Ω) with Qij > 0 on every fixed-support edge. Suppose a score pattern is row-plus-column separable on each connected component of the fixed support: 

Bij = αi + βj + cΓ (( i, j ) ∈ Γ) ,

where Γ ranges over connected components of the bipartite support. Let PL be the entropic Sinkhorn plan for score S = LB , and let P0 be the plan for the zero score on the same support and marginals. Then 

PL = P0 for every L. 

Consequently the derivative kernel M = C⊤R is unchanged and the plan-distortion factor relative to P0 is 

κL = 1 . In the uniform-marginal biregular case, the topology kernel remains exactly the two-step support random walk from Theorem 2. Proof. For every feasible plan P ∈ Π( a, b ; Ω) ,

⟨B, P ⟩ = X

> i

αi

X

> j

Pij + X

> j

βj

X

> i

Pij + X

> Γ

cΓmΓ = α⊤a + β⊤b + X

> Γ

cΓmΓ,

where mΓ is the fixed marginal mass in component Γ. This is constant over the transport polytope. Therefore maximizing L⟨B, P ⟩ + H(P ) is the same optimization problem as maximizing H(P ) alone. The primal plan is thus P0 for every L, so the derivative kernel and the distortion factor are unchanged. 7Preprint Dylan B. Forde 

On a connected support, the additive condition is equivalent to vanishing alternating cycle sums for B.Thus row/column gauge scores and rank-one Gibbs kernels exp( LB ij ) = exp( Lα i) exp( Lβ j ) cannot create plan distortion. Score sensitivity is carried by non-additive cycle imbalance, not by raw score range alone. 

Corollary 2 (Cycle space is the score-sensitive topology) . Fix compatible positive marginals a, b and assume strict feasibility on the active-edge set Ω. Let the fixed support have c connected components, with active row and column vertex sets I and J. The quotient dimension of score patterns that can affect the entropic plan, after modding out row/column additive gauges on each component, is 

|Ω| − | I| − | J| + c, 

the cycle rank of the bipartite support graph. In particular, if every connected component is a tree and the compatible transport polytope is strictly feasible, then Π( a, b ; Ω) is a singleton. Hence PL, the derivative kernel 

M = C⊤R, and all quotient-cotangent certificates are independent of the score pattern B.Proof. On a connected component, row-plus-column scores form a subspace of dimension |IΓ| + |JΓ| − 1, since adding a constant to all row prices and subtracting it from all column prices gives the same edge scores. Summing over components gives additive-score dimension |I| + |J| − c. Quotienting the |Ω| active-edge score coordinates by this gauge subspace leaves |Ω| − | I| − | J| + c, the graph cycle rank. Proposition 3 shows that these additive directions do not change the entropic optimization problem. Conversely, additive gauges are the only score directions that leave the entropic plan unchanged. Let P0

be the zero-score entropic plan and suppose the score B has the same entropic plan P0. The KKT equations for the two strictly concave entropy-regularized transport problems give dual prices (u0, v 0) and (uB , v B )

such that, on every active edge, 

log( P0)ij = −u0 

> i

− v0 

> j

= Bij − uBi − vBj .

Thus Bij = ( uBi − u0 

> i

) + ( vBj − v0 

> j

) on the support, up to the usual component gauges. Therefore a non-gauge score direction changes the entropic plan for some finite score scale, and the score-sensitive quotient dimension is exactly the cycle rank. If the support is a forest, the cycle rank is zero. Equivalently, the transport constraints have a unique feasible solution on each tree component: remove a leaf and its incident edge recursively; the edge mass is forced by the leaf marginal, and strict feasibility keeps the forced masses positive. Thus every score pattern is a gauge direction on the support, and the plan and its derivative kernel are fixed by the marginals and support alone. 

## 2.5 Zero-Temperature Face Obstructions 

Theorem 4 (Zero-temperature entropy-center obstruction) . Let Π( a, b ; Ω) be the compact transport polytope on a fixed feasible support Ω, and assume it contains a relative-interior point that is positive on every active edge. Let B be a fixed score pattern on Ω. For L > 0, let PL be the entropic Sinkhorn plan for score S = LB 

and marginals a, b , equivalently the maximizer of 

L⟨B, P ⟩ + H(P ), H(P ) = − X

> (i,j )∈Ω

Pij log Pij ,

over P ∈ Π( a, b ; Ω) , with the convention 0 log 0 = 0 . Let 

FB = arg max  

> P∈Π( a,b ;Ω)

⟨B, P ⟩

be the optimal face of the zero-temperature linear transport problem. Let P ent  

> B

be the unique maximizer of H

over FB . Then 

PL → P ent  

> B

as L → ∞ .

Consequently, if an active edge e ∈ Ω has Pe = 0 for every P ∈ F B , then (PL)e → 0. In particular, any edgewise distortion factor relative to a positive reference plan on all of Ω diverges. 

8Preprint Dylan B. Forde 

Proof. Let MB = max P ∈Π( a,b ;Ω) ⟨B, P ⟩ and choose P ⋆ ∈ F B . Since H is strictly concave and continuous on the compact polytope, its restriction to the compact convex face FB has a unique maximizer P ent  

> B

.Since Π( a, b ; Ω) is compact and H is bounded above and below on it, there is a finite constant C such that |H(P ) − H(P ⋆)| ≤ C for every feasible P . Optimality of PL gives 

L⟨B, P L⟩ + H(PL) ≥ LM B + H(P ⋆),

hence ⟨B, P L⟩ ≥ MB −C/L . Every accumulation point ¯P of PL is feasible and therefore satisfies ⟨B, ¯P ⟩ = MB ,so ¯P ∈ F B .Now use P ent  

> B

as the comparison point. Optimality gives 

L⟨B, P L⟩ + H(PL) ≥ LM B + H(P ent  

> B

).

Writing gL = MB − ⟨ B, P L⟩ ≥ 0, this says H(PL) ≥ H(P ent  

> B

) + Lg L ≥ H(P ent  

> B

). Therefore any accumulation point ¯P ∈ F B satisfies H( ¯P ) ≥ H(P ent  

> B

). By uniqueness of the entropy maximizer on FB , ¯P = P ent  

> B

. All subsequential limits are the same, hence the whole sequence converges. If an edge e has zero mass on every point of FB but some subsequence has (PLk )e ≥ ϵ > 0, compactness gives a further subsequence converging to an element of FB with edge mass at least ϵ, a contradiction. Thus 

(PL)e → 0. If the reference plan is positive on e, the ratio between the reference edge mass and (PL)e

diverges. Theorem 4 gives the zero-temperature reading of the topology certificate. Equal-score topology describes the reference Markov operator. Large structured scores select an entropy-centered point of an optimal face of a linear transport problem. Edges outside that face disappear, and the realized derivative kernel can inherit the topology of the selected face rather than the topology of the nominal architecture. This is the finite-dimensional entropic-penalty central-path phenomenon specialized to transport polytopes; see Cominetti and San Martín (1994) and Weed (2018) for general LP analyses. 

Corollary 3 (Exposed bad-face obstruction to graph-only contraction) . Assume the setting of Theorem 4. Suppose there is a score direction B such that the entropy-centered point P ent  

> B

of the exposed zero-temperature face has derivative kernel 

MB =  P ent  

> B

diag( b)−1⊤ diag( a)−1P ent 

> B



with τ (MB ) = 1 . Then for every ε > 0 there exists L such that the entropic plan for score LB has derivative kernel ML satisfying 

τ (ML) > 1 − ε. 

Thus this support/marginal instance admits no score-uniform global column-quotient upper bound τ (M ) ≤ ρ < 1

over arbitrary score directions with those fixed compatible marginals. Proof. Theorem 4 gives PL → P ent  

> B

. The map 

P 7 →  P diag( b)−1⊤ diag( a)−1P 

is continuous because the positive marginals a, b are fixed. Dobrushin’s coefficient is a maximum over finitely many continuous row-pair total-variation distances, hence it is continuous. Therefore τ (ML) → τ (MB ) = 1 ,which gives the claim. For background on the faces and combinatorics of transportation polytopes, see De Loera and Kim (2013). The characterization below couples this polyhedral structure to overlap of the induced quotient kernels. 

Theorem 5 (Face-lattice characterization of score-uniform global mixing) . Let 

P = Π( a, b ; Ω) 

be a nonempty compact transport polytope on a fixed finite support, with positive compatible marginals and a relative-interior point positive on every edge of Ω. This strict-feasibility hypothesis is essential: finite scores 

9Preprint Dylan B. Forde 

then produce plans in relint( P), while boundary faces appear only as zero-temperature limits. For any feasible plan P ∈ P , define 

M (P ) =  P diag( b)−1⊤ diag( a)−1P , Γ( P ) = min 

> α,β

X

> γ

min {M (P )αγ , M (P )βγ }.

Thus τ (M (P )) = 1 − Γ( P ). For a nonempty face F of P, write 

ΩF = {(i, α ) ∈ Ω : some P ∈ F has Piα > 0}.

Say that F has pairwise two-hop column overlap if, for every pair of columns α, β , there is a column γ such that each of α and β shares an active row with γ inside ΩF :

∃i, k (i, α ), (i, γ ), (k, β ), (k, γ ) ∈ ΩF .

Then the following are equivalent. 1. There exists δ > 0 such that every finite-score Sinkhorn plan on the fixed support satisfies Γ( PΩ(S; a, b )) ≥

δ, or equivalently τ (M (PΩ(S; a, b ))) ≤ 1 − δ.2. Every nonempty face of P has pairwise two-hop column overlap. Equivalently, a fixed support and compatible marginals force score-uniform one-step global column-quotient mixing exactly when no feasible face can collapse the derivative kernel into two rows with disjoint support. Componentwise contraction would require restating the condition on the corresponding component quotient. Proof. First, M (P ) and Γ( P ) are continuous functions of P on P, because the marginals a, b are fixed and positive and Γ is a minimum of finitely many continuous row-overlap functions. For a plan P , the support-to-kernel identity gives 

M (P )αγ > 0 ⇐⇒ ∃i with Piα > 0 and Piγ > 0.

Transportation polytopes are standard-form polytopes {x ≥ 0 : Ax = c}, so every face is obtained by setting a subset of edge variables to zero. Hence the minimal face containing P has support supp( P ), and the relative interior of a face F is positive exactly on ΩF . Therefore Γ( P ) > 0 if and only if the minimal face containing 

P has pairwise two-hop column overlap. Assume every nonempty face has pairwise two-hop column overlap. Then Γ( P ) > 0 for every P ∈ P ,including boundary plans. By compactness and continuity, 

δ∗ = min   

> P∈P

Γ( P ) > 0.

Every finite-score Sinkhorn plan lies in P, so Γ( PΩ(S; a, b )) ≥ δ∗ for every finite score matrix. Conversely, suppose a nonempty face F fails pairwise two-hop column overlap. Every face of a finite polytope is exposed, so there is a score direction B whose maximizers over P are exactly F . By Theorem 4, the entropic Sinkhorn plan for score LB converges as L → ∞ to the unique entropy maximizer P ent  

> F

over F .The entropy maximizer over a nonempty face lies in relint( F ): if an edge in ΩF had zero mass, moving from the maximizer a small distance toward any relative-interior point of F would increase entropy to first order because −∂x(x log x) → +∞ as x ↓ 0. Thus the positive support of P ent  

> F

is exactly ΩF .Since ΩF fails pairwise two-hop column overlap, two rows of M (P ent  

> F

) have disjoint support. Hence 

Γ( P ent  

> F

) = 0 and τ (M (P ent  

> F

)) = 1 . Continuity gives Γ( PΩ(LB ; a, b )) → 0, so no positive score-uniform lower bound δ can hold over finite scores. 

Corollary 4 (Score-uniform mixing dichotomy) . Under the hypotheses of Theorem 5, exactly one of the following alternatives holds. 1. There is a constant ρ < 1, depending only on (a, b, Ω) , such that every finite-score Sinkhorn plan on the fixed support satisfies τ (M (PΩ(S; a, b ))) ≤ ρ.

10 Preprint Dylan B. Forde 

2. For every ε > 0, there is a finite score matrix S on the same support such that 

τ (M (PΩ(S; a, b ))) > 1 − ε. 

The first alternative holds exactly when every nonempty feasible face of Π( a, b ; Ω) has pairwise two-hop column overlap. Proof. If every nonempty face has pairwise two-hop column overlap, the theorem gives a positive lower bound 

Γ( P ) ≥ δ for every finite-score plan, so setting ρ = 1 − δ proves the first alternative. If some face fails the overlap condition, the converse part of the theorem constructs an exposed score direction B for which 

Γ( PΩ(LB ; a, b )) → 0 as L → ∞ . Choosing L large enough gives τ (M (PΩ(LB ; a, b ))) > 1 − ε, which proves the second alternative. The alternatives are mutually exclusive because τ (M ) ≤ ρ < 1 for all finite scores is incompatible with finite scores whose coefficients exceed 1 − ε for every ε > 0.Theorem 5 and Corollary 4 are the sharp fixed-support/marginal results of this paper. They explain why equal-score topology can be predictive while arbitrary scores remain dangerous: a nominally well-connected support may still have a bad boundary face, and large scores can expose that face. They also give a concrete design target. To make topology force mixing, one must rule out bad feasible faces, or else work with realized-plan, windowed, or plan-distortion certificates. Exact checking should not be oversold. For a fixed tested column pair α, β , the bad-face search can be organized as a finite disjunction of ordinary transport-feasibility problems: after guessing the rows carrying positive mass into α and β and guessing, for each intermediate column, on which side the two-hop overlap is killed, each resulting subproblem has standard Hall/Gale or max-flow certificates. The difficult part is the exponential support-pattern choice. Thus the face-enumeration validator is a tiny diagnostic, while Corollaries 5 and 6 are checkable sufficient architecture certificates. 

Corollary 5 (Capacity-deficit forced bus edges) . Assume the hypotheses of Theorem 5. For an active edge 

(i, α ) ∈ Ω, define 

diα =

bα − X 

> k: ( k,α )∈Ω, k ̸=i

ak



> +

,

and set diα = 0 for inactive edges. Then every feasible plan satisfies Piα ≥ diα . Let B be a set of bus rows. Suppose that for every pair of distinct columns α, β , there exists i ∈ B with diα > 0 and diβ > 0. Then every feasible face has pairwise two-hop column overlap and the support–marginal pair has a score-uniform one-step global column-quotient certificate. More quantitatively, 

τ (M (P )) ≤ 1 − γ0 for every feasible P, 

where 

γ0 = min  

> α̸=β

max   

> i∈B:diα diβ >0

X  

> γ:diγ >0

min 

 diα diγ 

bαai

, diβ diγ 

bβ ai



.

Proof. For any feasible plan, 

Piα = bα − X 

> k̸=i

Pkα ≥ bα − X 

> k: ( k,α )∈Ω, k ̸=i

ak

on active edges, and the positive part gives the lower bound. If diα , d iβ > 0, then for every γ with diγ > 0,

Mαγ ≥ diα diγ 

bαai

, Mβγ ≥ diβ diγ 

bβ ai

.

Summing coordinatewise minima over such γ gives a common mass lower bound for rows α and β of M .Taking the best bus row for each pair and then the worst pair gives Γ( P ) ≥ γ0 > 0, hence τ (M (P )) ≤ 1 − γ0.Since the lower bounds hold on every feasible plan and every feasible face, Theorem 5 also gives the qualitative face-lattice conclusion. 11 Preprint Dylan B. Forde 

Corollary 5 is the support-schedule version of the face-lattice theorem. It says that support adjacency alone is not enough: support and marginals must force positive edge flow on every feasible face. The following special case is easier to read and useful for sanity checks. 

Corollary 6 (Heavy universal bus row) . Assume the hypotheses of Theorem 5, and let T = 1⊤a = 1⊤b.Suppose there is a row h whose support contains every column, and suppose 

δj := ah + bj − T > 0 for every column j. 

Then every feasible face of Π( a, b ; Ω) has pairwise two-hop column overlap. In fact, every feasible plan satisfies 

Phj ≥ δj for every j. 

Consequently the derivative kernel has the uniform common-component bound 

Mαβ ≥



min 

> p

δp

bp

 δβ

ah

for every α, β, 

and therefore 

τ (M ) ≤ 1 − γh, γh =



min 

> p

δp

bp

 P  

> β

δβ

ah

.

This is a score-uniform one-step global column-quotient certificate. Proof. For any feasible plan P , the non-bus rows have total mass T − ah. Thus column j can receive at most 

T − ah mass from non-bus rows, and hence 

Phj = bj − X

> i̸=h

Pij ≥ bj − (T − ah) = δj > 0.

Therefore the bus row is positive to every column in every feasible plan and, by the coordinate-face structure of transportation polytopes, in every relative face support. Every pair of columns then shares row h, so Theorem 5 applies. The explicit lower bound follows from the support-to-kernel formula: 

Mαβ = X

> i

Piα Piβ 

bαai

≥ Phα Phβ 

bαah

≥ δαδβ

bαah

≥



min 

> p

δp

bp

 δβ

ah

.

Thus every row of M dominates the same subprobability vector ℓβ = (min p δp/b p)δβ /a h of total mass γh.The sparse-support overlap certificate gives τ (M ) ≤ 1 − γh.The condition in Corollary 6 is deliberately strong but checkable. It is not necessary for topology-forced mixing; it is a simple architectural sufficient condition. If ah + bj ≤ T for some column, the bus row can avoid that column in principle, and the corollary withholds rather than guessing. Multiple lighter bus rows can be handled by Corollary 5, but only when the forced edges cover every column pair through at least one common bus row. 

Architecture corollaries in appendix. The core face-lattice theorem is independent of the concrete architecture families used as examples. Equal-score spectral rules, local-band spectra, and other graph-design corollaries are collected in Appendix A. They are useful design consequences, but they are not assumptions in the support-to-kernel or face-lattice proofs. 

# 3 Dobrushin and Tail-Cotangent Certificates 

The following facts are standard Dobrushin/Doeblin tools (Gaubert and Qu, 2013; Levin and Peres, 2017), stated here with the quotient convention of Definition 1. They are the bridge from the fixed-support half-step calculus to rigorous tail-gradient bounds. 12 Preprint Dylan B. Forde 

Definition 3 (Projected source-cotangent interface) . Fix the common column quotient Q = Rn/span {1} for the tail and let 

ι : Q −→ 1⊥, ι([ v]) = Π 0v, Π0 = I − 1

n 11 ⊤

be its unique centered lift. At step t, let Zt collect the other independent local coordinates (parameters, values, residual state, or features), and let 

Φt : Rn × Z t −→ S t

collect every differentiable surrounding map that produces a source variable for the Sinkhorn cycle, evaluated at the centered input potential and fixed local coordinate zt. The source space may contain active-edge scores, logarithmic marginals, values, query/key/value features, residual inputs, or loss-side quantities. Let νt ∈ S ∗ 

> t

be the aggregate source-space cotangent after all local reverse branches have been summed. Its score and marginal coordinates are the covectors in Proposition 2, represented in the marginal cotangent quotients specified there. The exact cotangent returned to the input quotient is the partial VJP of the composite map [v] 7 →

Φt(ι([ v]) , z t):

ξt = D([ v] 7 → Φt(ι([ v]) , z t)) ([ vt]) ∗νt

= ι∗D1Φt(ι([ vt]) , z t)∗νt = Π 0ψt ∈ Q∗,ψt := D1Φt(ι([ vt]) , z t)∗νt.

The final identity uses Euclidean coordinates on the centered section. Thus projection is not an assumed contraction and not another Sinkhorn transpose step: it is the adjoint of the chosen quotient lift. If Φt is already gauge-invariant, then 1⊤ψt = 0 and the projection changes nothing. For a fixed disconnected support, replace ι and Π0 by the component-centered lift and projection from Definition 1. A source branch that does not descend to the declared quotient must first be expressed on a specified section; its section VJP, rather than an arbitrary raw covector, defines ξt. Source coordinates independent of the potential have zero D1Φt

contribution to ξt; their D2Φ∗ 

> t

νt cotangents continue on their own upstream paths. The aggregate νt contains each direct local source branch once and excludes the homogeneous Sinkhorn branch M ⊤ 

> t

ηt+1 , so the recurrence below neither drops nor duplicates a branch. 

Theorem 6 (Zero-mass quotient contraction) . Let A be any row-stochastic kernel on n states. For every cotangent η ∈ Q∗ = {η : 1⊤η = 0 },

∥A⊤η∥TV ≤ τ (A)∥η∥TV .

For row-stochastic kernels A1, . . . , A K ,

∥(AK · · · A1)⊤η∥TV ≤ τ (AK · · · A1)∥η∥TV ≤

> K

Y

> t=1

τ (At)∥η∥TV .

Proof. A zero-mass signed vector η has Jordan decomposition η = η+ − η− with η± ≥ 0 and η+(1) = η−(1) = 

∥η∥TV . After normalizing the two positive parts to probability vectors, A⊤η is the difference of the two probability measures pushed forward by the Markov kernel A, times ∥η∥TV . The Dobrushin coefficient is exactly the largest possible total-variation distance between two rows, equivalently the largest contraction factor on differences of probability measures. This proves the one-step inequality. The product kernel is row-stochastic, so the first inequality applies to it. Submultiplicativity of Dobrushin coefficients gives 

τ (AK · · · A1) ≤ Q 

> t

τ (At).

Theorem 7 (Projected-source tail-cotangent bound) . Let M1, . . . , M K ∈ Rn×n be the fixed-support quotient operators produced by a sequence of Sinkhorn row-column cycles on a common column state space, and let projected quotient sources ξt ∈ Q∗ collect the zero-mass cotangents delivered to the input quotient coordinates at step t. Concretely, the Sinkhorn score and marginal components are the adjoints in Proposition 2; all local source-space covectors are summed into νt, and the exact partial VJP of [v] 7 → Φt(ι([ v]) , z t) in Definition 3 constructs ξt. Suppose the reverse tail recurrence is 

ηt = M ⊤ 

> t

ηt+1 + ξt, t = 1 , . . . , K, (10) 13 Preprint Dylan B. Forde 

with terminal quotient cotangent ηK+1 ∈ Q∗. Then 

η1 = ( MK · · · M1)⊤ηK+1 +

> K

X

> t=1

(Mt−1 · · · M1)⊤ξt, (11) 

where the empty product is the identity. Consequently the homogeneous residual obeys 

∥(MK · · · M1)⊤η∥TV ≤ τ (MK · · · M1)∥η∥TV , (12) 

and the inhomogeneous tail obeys the path/product sum bound 

∥η1∥TV ≤ τ (MK · · · M1)∥ηK+1 ∥TV +

> K

X

> t=1

τ (Mt−1 · · · M1)∥ξt∥TV . (13) 

A looser but sometimes cheaper certificate replaces each product coefficient by Qt−1 

> ℓ=1

τ (Mℓ).Proof. Unroll (10). For K = 2 , η1 = M ⊤ 

> 1

M ⊤ 

> 2

η3 + M ⊤ 

> 1

ξ2 + ξ1; the general formula (11) follows by induction, and M ⊤ 

> 1

· · · M ⊤ 

> K

= ( MK · · · M1)⊤. All terms remain in Q∗ because the Mt are row-stochastic and the sources are quotient VJPs. Apply Theorem 6 to the homogeneous product and to each source product, then use the triangle inequality. Submultiplicativity gives the optional product-of-one-step-coefficients bound. 

Corollary 7 (Implicit quotient VJP certificate) . Let M be a row-stochastic fixed-support quotient operator with τ (M ) < 1, and let ξ ∈ Q∗ be a projected source cotangent. The implicit quotient equation 

η = M ⊤η + ξ

has a unique solution in Q∗,

η =

> ∞

X

> k=0

(M ⊤)kξ, 

and 

∥η∥TV ≤ ∥ξ∥TV 

1 − τ (M ) .

If η(K) = PK−1 

> k=0

(M ⊤)kξ, then the truncation residual is bounded by 

∥η − η(K)∥TV ≤ τ (M K )1 − τ (M ) ∥ξ∥TV ≤ τ (M )K

1 − τ (M ) ∥ξ∥TV .

Proof. On Q∗, Theorem 6 gives ∥(M ⊤)kξ∥TV ≤ τ (M )k∥ξ∥TV . The Neumann series therefore converges absolutely in the finite-dimensional quotient space and solves (I − M ⊤)η = ξ. If another solution existed, their difference z ∈ Q∗ would satisfy z = M ⊤z, hence ∥z∥TV ≤ τ (M )∥z∥TV ; since τ (M ) < 1, z = 0 . The norm bound is the geometric series. For the truncation residual, η − η(K) = ( M K )⊤η, so Theorem 6 gives 

∥η − η(K)∥TV ≤ τ (M K )∥η∥TV , and the previous bound on ∥η∥TV completes the proof. Submultiplicativity gives the final inequality. 

Scope of the tail theorem. Theorem 7 proves the fixed-support quotient-transport component exactly. A full neural-gradient statement also has to bound ∥ξt∥TV and the operator norms of any non-transport maps between quotient spaces. Proposition 2 supplies the one-cycle source VJPs; this theorem supplies the quotient transport of the resulting projected sources. Without the surrounding source-norm and map-norm bounds, 

τ (MK · · · M1) is a rigorous transport-tail certificate, not a complete parameter-to-loss gradient guarantee. 

Proposition 4 (Sparse-support overlap certificate) . Let M be the derivative kernel induced by a fixed support and plan. Suppose there are γ ∈ [0 , 1] and a probability vector q such that 

Mij ≥ γq j for all i, j. 

Then τ (M ) ≤ 1 − γ.

14 Preprint Dylan B. Forde 

Proof. Write each row as Mi = γq + (1 − γ) fMi, where fMi is a probability vector when γ < 1. The common component cancels in Mi − Mi′ , so the total-variation distance between any two rows is at most 1 − γ.This proposition is deliberately stated as a kernel-level condition. A support graph can suggest overlap, but scores and marginal masses determine the actual lower bound after scaling. The validator therefore computes the empirical shared component 

γcert = X

> j

min  

> i

Mij 

from the realized kernel rather than claiming a graph-only theorem. 

Theorem 8 (Mass-floored hub certificate) . Let H ⊆ [n] be a set of hub columns. Suppose there is a number 

pmin > 0 such that for every source column α ∈ [n] and every hub h ∈ H, there exists a witness row i = i(α, h )

with 

Piα ≥ pmin , Pih ≥ pmin .

Let amax = max i ai and bmax = max α bα. Then 

Mαh ≥ p2min 

bmax amax 

for every α ∈ [n], h ∈ H. 

Consequently, writing 

γshared = X

> β

min  

> α

Mαβ ,

we have 

γshared ≥ | H| p2min 

bmax amax 

.

Equivalently, with the defensive cap 

γH = min 



1, |H| p2min 

bmax amax 



,

the derivative kernel has a common component of mass at least γH and τ (M ) ≤ 1 − γH .Proof. Using Theorem 1, for each α and h,

Mαh = X

> i

Piα Pih 

bαai

.

All summands are nonnegative. The witness row i(α, h ) contributes at least 

p2min 

bαai(α,h )

≥ p2min 

bmax amax 

.

Therefore each row of M assigns at least this much mass to every hub column in the fixed common set H.Summing the rowwise minimum over the distinct columns h ∈ H gives the displayed lower bound on γshared ;applying Proposition 4 gives the Dobrushin bound. Under exact hypotheses the uncapped lower bound is automatically at most one because M is row-stochastic; the cap is included only to make numerical reporting robust. This theorem is the safe quantitative form of the topology story. The support graph supplies the hub-witness pattern; the scaled plan supplies the mass floor. It is therefore a realized-plan certificate, not a pure support theorem. Without a mass floor or explicit score/marginal assumptions implying one, dense support alone does not prevent τ (M ) from approaching one. 15 Preprint Dylan B. Forde 

# 4 Negative and Windowed Topology Certificates 

The certificate is most useful when it distinguishes topology failures from topology fixes. The following negative examples are deliberately simple. 

Proposition 5 (Closed disconnected components) . If the row-stochastic kernel M has two closed components with disjoint transition supports, then τ (M ) = 1 . Hence the one-step certificate gives no global quotient contraction. Proof. Choose one row from each closed component. Their row distributions have zero overlap, so the total-variation distance between the rows is one. This is the maximal value in the Dobrushin coefficient. 

Proposition 6 (Permutation-like supports) . If M is a permutation matrix on at least two states, or more generally if two rows of M are point masses on different states, then τ (M ) = 1 . Identity-like transport therefore preserves some quotient cotangent modes exactly. Proof. Two distinct point masses have total-variation distance one. A permutation matrix on at least two states has two rows that are point masses on different states. For the identity matrix on at least two states, every zero-mass cotangent is propagated without decay. One-step failure is not the same as architectural failure. Alternating supports or repeated layers can mix over a window. 

Theorem 9 (Windowed face-lattice characterization) . For t = 1 , . . . , K , let Pt = Π( at, b t; Ω t) be nonempty compact transport polytopes on the same column index set, with positive compatible marginals and strict feasibility on every active edge of Ωt. For Pt ∈ P t, define 

Mt(Pt) =  Pt diag( bt)−1⊤ diag( at)−1Pt

.

For a face tuple F = ( F1, . . . , F K ) with every Ft nonempty, let Rt(Ft) be the two-hop column relation induced by the face support ΩFt :

α Rt(Ft) γ ⇐⇒ ∃i with (i, α ), (i, γ ) ∈ ΩFt .

Say that the tuple has windowed pairwise product overlap if, for every pair of columns α, β , the K-step reachable sets from α and β through the relation product, applied in matrix-product order, 

RK (FK ); RK−1(FK−1); · · · ; R1(F1)

intersect. Then the following are equivalent. 1. There exists δK > 0 such that every finite score sequence on the fixed supports satisfies 

τ (MK (PK ) · · · M1(P1)) ≤ 1 − δK .

2. Every face tuple (F1, . . . , F K ) with every Ft nonempty has windowed pairwise product overlap. Thus a scheduled sparse architecture can force global quotient mixing over a window even when each one-step topology withholds a contraction certificate. Proof. For any feasible tuple (P1, . . . , P K ), the support of MK (PK ) · · · M1(P1) is the Boolean product obtained by first following the support relation of MK (PK ), then the support relation of MK−1(PK−1), and so on through M1(P1). By the support-to-kernel theorem, the one-step support relation of Mt(Pt) is the two-hop relation induced by supp( Pt). Hence the product kernel has Dobrushin coefficient strictly below one exactly when every pair of its rows has overlapping support, which is precisely the matrix-product-order windowed product-overlap condition for the minimal face tuple containing the plans. If every face tuple has windowed product overlap, then 

ΓK (P1, . . . , P K ) = 1 − τ (MK (PK ) · · · M1(P1)) 

16 Preprint Dylan B. Forde 

is positive on the compact product polytope P1 × · · · × P K . Continuity gives a positive minimum δK , and every finite-score sequence satisfies the stated bound. Conversely, suppose a face tuple (F1, . . . , F K ) fails windowed product overlap. Each face Ft is exposed by some score direction Bt. Applying Theorem 4 independently to the score sequence LB t gives plans 

Pt(L) converging to entropy-center points in relint( Ft). The corresponding product kernels converge to a row-stochastic kernel with two disjoint product-row supports, so its Dobrushin coefficient is one. By continuity, 

τ (MK (PK (L)) · · · M1(P1(L))) → 1,

and no positive score-uniform window bound can hold. As in the one-step face-lattice theorem, this is a structural characterization rather than a scalable checker. Exact verification over arbitrary supports requires quantifying over face tuples of a product of transport polytopes. The point is the architectural dichotomy: a scheduled support family either rules out bad zero-temperature face sequences, or some finite score sequence can make the window certificate arbitrarily weak. The product-coordinate theorem below is a tractable family where the good side of the dichotomy can be proved without enumerating faces. Products of stochastic matrices have a classical convergence theory (Wolfowitz, 1963). Here the quantitative certificate is the Dobrushin coefficient of the ordered product on a common quotient space; one-step contraction is not required. 

Theorem 10 (Windowed Dobrushin certificate) . Let M1, . . . , M K be row-stochastic kernels. For every zero-mass cotangent η,

∥(MK · · · M1)⊤η∥TV ≤ τ (MK · · · M1)∥η∥TV .

It is possible that τ (Mt) = 1 for every t while τ (MK · · · M1) < 1.Proof. The first claim is the zero-mass quotient contraction applied to the product kernel, which is row-stochastic. For the second claim, take two partition kernels on four states: one averages within {1, 2} and 

{3, 4}, and the next averages within {1, 3} and {2, 4}. Each kernel has disjoint row pairs and τ = 1 , but their product sends every row to the uniform distribution, so the two-step coefficient is zero. Heat-bath updates resample from a conditional distribution and are standard Markov-chain constructions (Levin and Peres, 2017). The next result identifies such an update exactly as the derivative kernel of a partition-supported transport layer. 

Theorem 11 (Partition heat-bath transport layers) . Let X be a finite column set with a positive probability vector b. Let π : X → Z be a partition map, use rows indexed by z ∈ Z, and define the active support by 

(z, x ) ∈ Ω ⇐⇒ π(x) = z. 

Set the row marginal to the block mass 

az = X 

> x:π(x)= z

bx.

Then the transportation polytope Π( a, b ; Ω) is a singleton. For every finite score matrix on this active support, the Sinkhorn plan is 

Pzx = bx 1{π(x) = z}.

The induced column-to-column derivative kernel is the heat-bath kernel 

Mxy = 1{π(x) = π(y)} by

aπ(x)

.

Consequently any sequence of partition-fiber transport layers has exactly the same quotient-kernel product as the corresponding sequence of heat-bath resampling kernels. Windowed Dobrushin certificates therefore apply directly to the resulting kernel products. 

17 Preprint Dylan B. Forde 

Proof. Each column x is incident to exactly one row, namely π(x). The column constraint therefore forces 

Pπ(x),x = bx, and all other entries in that column are inactive. The row sums match by the definition of az ,so this is the unique feasible plan; because the feasible set is a singleton, finite scores cannot change the plan. Substituting this plan into the support-to-kernel formula gives 

Mxy = X

> z

Pzx Pzy 

bxaz

= 1{π(x) = π(y)} by

aπ(x)

.

The statement about products follows by applying the same identity to every layer. Theorem 11 is deliberately stated as a kernel identification theorem, not as a universal mixing theorem. For non-product b, a sweep of such layers is a systematic block-Gibbs kernel for b; it may contract, but the coefficient depends on additional minorization or reachability properties of the block conditionals and can be arbitrarily close to one under strong dependencies. The stronger identity Mσ(K) · · · Mσ(1) = 1b⊤ in the next theorem uses the product form of b in an essential way. 

Theorem 12 (Product-coordinate window certificate) . Let 

X = X1 × · · · × XK

be a finite product column set. Let b(x) = QKt=1 μt(xt) be a strictly positive product marginal. For layer t, let rows be indexed by fibers z ∈ X−t := Q 

> s̸=t

Xs, connect row z exactly to columns x ∈ X with x−t = z, and set 

at(z) = X

> u∈Xt

b(z, u ) = Y

> s̸=t

μs(zs).

Then the feasible transport plan on layer t is unique, for every finite score matrix on the active support: 

Pt(z, x ) = b(x) 1{x−t = z}.

Consequently its derivative kernel is the coordinate-resampling kernel 

(Mt)xy = 1{x−t = y−t} μt(yt).

For any permutation σ of {1, . . . , K }, applying each coordinate layer once gives 

Mσ(K) · · · Mσ(1) = 1b⊤, τ (Mσ(K) · · · Mσ(1) ) = 0 .

If the product of the coordinates other than t has at least two elements, then τ (Mt) = 1 . Thus coordinate-factorized sparse layers may withhold every one-step contraction certificate while a full coordinate window gives perfect score-uniform quotient mixing. Proof. Fix t. Each column x is incident to exactly one row, namely z = x−t. The column marginal constraint therefore forces Pt(z, x ) = b(x) on that active edge. The row sums match by the definition of at(z):

X 

> x:x−t=z

Pt(z, x ) = X

> u∈Xt

b(z, u ) = at(z).

Thus the transportation polytope for layer t is a singleton and finite scores cannot change the plan. Using the support-to-kernel identity, 

(Mt)xy = X

> z

Pt(z, x )Pt(z, y )

b(x)at(z) .

There is a nonzero summand only when x−t = y−t = z. In that case 

(Mt)xy = b(x)b(y)

b(x)at(x−t) = b(y)

at(x−t) = μt(yt),

18 Preprint Dylan B. Forde 

which proves the coordinate-resampling formula. The kernel Mt leaves all coordinates except t fixed and resamples coordinate t from μt. These coordinate-resampling kernels commute because b is a product measure and each kernel acts on a different coordinate conditional. After every coordinate has been applied once, the output distribution is b independently of the input column, so the product kernel is 1b⊤ and its Dobrushin coefficient is zero. If X−t has at least two elements, choose columns x, y with x−t̸ = y−t. The supports of rows x and y of 

Mt lie in disjoint fibers, so the two rows have total-variation distance one and τ (Mt) = 1 .

Corollary 8 (Hypercube butterfly window) . Let n = 2 r with r ≥ 2, and index columns by bitstrings 

x ∈ { 0, 1}r. For layer t ∈ { 1, . . . , r }, let the fixed support have one row for each unordered pair {x, x ⊕ et},where et flips bit t, and connect that row to exactly the two columns in the pair. Use equal active scores, row marginals ai = 2 /n on the n/ 2 matching rows, and column marginals bx = 1 /n . Let Mt be the resulting column-to-column derivative kernel. Then 

Mt = 12 (I + Ft),

where Ft is the permutation matrix for bit flip x 7 → x ⊕ et. Consequently τ (Mt) = 1 for every t, but 

Mr Mr−1 · · · M1 = 1

n 11 ⊤ and hence τ (Mr Mr−1 · · · M1) = 0 .

Proof. Apply Theorem 12 with Xt = {0, 1} and μt uniform. The fiber row indexed by x−t is exactly the unordered pair {x, x ⊕ et}, and the coordinate-resampling kernel is Mt = ( I + Ft)/2. Since r ≥ 2, each X−t

has at least two elements, so τ (Mt) = 1 . Applying all bit coordinates once gives the uniform kernel n−111 ⊤,hence the window coefficient is zero. 

# 5 Support-Schedule Design Consequences 

The theorem spine turns support design into a question about feasible faces and row-overlap of induced Markov kernels. Three examples are important for interpretation. First, local windows can be qualitatively connected while still withholding one-step contraction, so powered or windowed certificates are needed. Second, register or dustbin rows can create shared mass, but only when the realized plan or the marginal constraints force genuine common components. Third, layered sparse schedules can mix globally even when each layer alone has τ = 1 , as shown by Theorem 12 and the hypercube butterfly corollary. The detailed spectral, register, and shared-bus corollaries are collected in Appendix A; the DAG path-sum material is only a short appendix remark. The main claims here are the fixed-support quotient operator, the face-lattice dichotomy, and the windowed/product-coordinate support-schedule certificates. 

# 6 Computational Complexity of Certificates 

Let di = |Ωi| be the active degree of row i, let E = |Ω|, and let sα denote the number of nonzeros in row α of the realized kernel M .

Building the quotient operator. Using 

Mαβ = X

> i

Piα Piβ /(bαai),

one can build M by adding the rank-one contribution of each support row i to the column pairs in Ωi × Ωi.The arithmetic work and the candidate-pair storage are 

O X

> i

d2

> i

!

, nnz( M ) ≤ X

> i

d2 

> i

.

The bound is tight up to duplicated pairs. A dense row, or enough sparse rows covering all column pairs, gives the worst case nnz( M ) = n2 and O(n2) storage even when P is sparse. 19 Preprint Dylan B. Forde 

Exact Dobrushin coefficient. Exact τ (M ) compares all pairs of rows of M :

τ (M ) = 12 max  

> α,β

∥Mα· − Mβ·∥1.

For a dense kernel this costs O(n3) time and O(n2) memory. With sparse rows, a merge of row supports costs O(sα + sβ ), so the all-pairs cost is O(P 

> α<β

(sα + sβ )) ≤ O(n nnz( M )) , which again becomes O(n3)

after densification. 

Window products. A window certificate uses τ (MK · · · M1). Even if each Mt is sparse, products can densify after a few layers; exact products are therefore intended for small or offline validation unless a family theorem, sparse approximation, or symbolic product structure is available. The spectral bounds for reversible equal-score baselines, the Doeblin/minorization bounds, forced-bus certificates, and product-coordinate heat-bath identities avoid materializing dense products when their hypotheses hold. 

Face-lattice checking. The exact one-step criterion quantifies over every nonempty face of Π( a, b ; Ω) , and the windowed criterion quantifies over tuples of faces. For arbitrary supports this is exponential in the number of active edges. The practical uses are therefore: prove tractable family theorems, compute realized-plan certificates for observed scores, certify distortion or mass floors, and search for counterexample faces. These certificates are offline design and validation tools unless a sparse approximation or family theorem is provided. 

# 7 Discussion 

The theorem spine separates four levels of reasoning. At the half-step level, fixed support and finite scores give an exact quotient Markov operator M = C⊤R plus explicit source terms. At the tail level, Dobrushin coefficients bound homogeneous residuals and projected-source path sums. At the pure topology level, equal-score biregular supports become explicit graph walks, and the face-lattice dichotomy says exactly when a fixed support and marginals force score-uniform one-step quotient mixing. At the scheduled support level, windowed face tuples and product-coordinate supports explain how individually non-mixing sparse layers can mix over a finite window. The face-lattice and product-coordinate theorems depend only on primal entropy-center face convergence, compactness, and support overlap. The quantitative regimes in the manuscript are exactly the equal-score, forced-capacity, realized-plan, and plan-distortion regimes. 

# 8 Conclusion 

Fixed-support Sinkhorn scaling exposes a precise quotient-transport operator. The homogeneous column-potential update is δv + = C⊤Rδv , and the reverse quotient cotangent update is governed by M ⊤ together with projected source terms. Dobrushin, minorization, face-lattice, and windowed certificates turn support schedules into rigorous statements about this fixed calculus. The result consists of exact fixed-support proofs, explicit limits, computational costs, and certificate-level validation without downstream or systems overclaiming. 

# A Spectral Architecture Corollaries 

The spectral estimates below use standard reversible-chain bounds (Levin and Peres, 2017), applied to the column-overlap kernel. 

Corollary 9 (Spectral topology certificate) . In the setting of Theorem 2, M is symmetric and doubly stochastic. Let 

λ⋆ = ∥M Π1⊥ ∥2 = max {| λ| : λ is an eigenvalue of M on 1⊥}.

Then, for every zero-mass cotangent η and every integer K ≥ 1,

∥(M K )⊤η∥2 ≤ λK⋆ ∥η∥2, ∥(M K )⊤η∥TV ≤ pn/ 2λK⋆ ∥η∥TV .

20 Preprint Dylan B. Forde 

Moreover, 

τ (M K ) ≤ min {1, pn/ 2λK⋆ }.

Proof. The formula M = N/ (dcdr) has N = N ⊤ and row sums dcdr, hence M is symmetric and doubly stochastic. The Euclidean bound follows by diagonalizing M on the invariant subspace 1⊥. For total variation, use ∥x∥TV ≤ √n∥x∥2/2 and ∥η∥2 ≤ √2∥η∥TV for zero-mass η. For Dobrushin’s coefficient, apply the same Euclidean contraction to the zero-mass row difference eα − eβ , whose Euclidean norm is √2, then maximize over row pairs and cap by the trivial bound τ ≤ 1.This corollary is the bridge from graph design to tail-depth design. In the equal-score biregular baseline, choosing a support is choosing a reversible Markov chain for quotient cotangents. Expander-like column-overlap walks make λ⋆ small, while butterfly-like supports may have weak one-step τ (M ) but strong powered certificates for M K .

Corollary 10 (Expander tail-depth rule) . In the setting of Theorem 2, suppose the zero-mass spectral rate satisfies λ⋆ ≤ ρ < 1. Then 

τ (M K ) ≤ min {1, pn/ 2ρK }.

If ρ = 0 , any K ≥ 1 suffices. If 0 < ρ < 1, then for any ε ∈ (0 , 1) it is sufficient to choose 

K ≥

&

log( pn/ 2/ε )

− log ρ

'

to obtain τ (M K ) ≤ ε.Proof. Substitute λ⋆ ≤ ρ in Corollary 9 and solve pn/ 2ρK ≤ ε for K.

Corollary 11 (Circular local-band spectrum) . Consider the equal-score biregular baseline on n circular positions where row i is connected to columns i − r, . . . , i + r modulo n. Assume 0 ≤ r < n/ 2, so the local degree is d = 2 r + 1 . Then the derivative kernel M is circulant and has Fourier eigenvalues 

λk = 1

d2

> r

X

> s=−r

exp(2 πiks/n )

> 2

=



1, k = 0 ,

 sin( πkd/n )

d sin( πk/n )

2

, k = 1 , . . . , n − 1.

Consequently the zero-mass spectral rate is 

λband = max 

> 1≤k≤n−1

 sin( πkd/n )

d sin( πk/n )

2

,

and Corollary 9 gives 

τ (M K ) ≤ min {1, pn/ 2 λK

> band

}.

For fixed r and n → ∞ ,

λband ≥ λ1 = 1 − π2(d2 − 1) 3n2 + O(n−4),

so this spectral certificate is diffusive for fixed-radius local bands. More sharply, for the first nonconstant Fourier cotangent η(1) ,

∥(M K )⊤η(1) ∥2

∥η(1) ∥2

= λK 

> 1

.

Thus, if r is fixed and K = o(n2), then λK 

> 1

→ 1. No fixed-radius circular local band can uniformly contract all zero-mass quotient modes in subquadratic depth under this equal-score spectral model. 

21 Preprint Dylan B. Forde 

Proof. Let h ∈ Rn be the circular band indicator, hs = 1 for s ∈ {− r, . . . , r } modulo n and hs = 0 otherwise. By Theorem 2, Mαβ is d−2 times the number of rows incident to both columns α and β. Hence M is the circulant cyclic autocorrelation 

Mαβ = 1

d2

X

> s∈Z/n Z

hshs+β−α.

The discrete Fourier characters diagonalize circulant convolution operators, and the autocorrelation has Fourier transform |bhk|2, where 

bhk =

> r

X

> s=−r

exp(2 πiks/n ).

This gives the displayed eigenvalues. For k̸ = 0 , the finite geometric sum is the Dirichlet kernel 

bhk = sin( πkd/n )sin( πk/n )

up to a phase, whose absolute value gives the second expression. The zero-mass subspace removes the k = 0 

constant eigenvector, and the spectral Dobrushin bound is Corollary 9. Finally, put x = π/n . For fixed d,

sin( dx )

d sin x = 1 − (d2 − 1) x2

6 + O(x4),

and squaring gives the expansion for λ1. Since λband ≥ λ1, the stated lower asymptotic follows. The first nonconstant Fourier character is an eigenvector with eigenvalue λ1, so the displayed L2 ratio after K

applications is exact. If K = o(n2) and r is fixed, the expansion gives K(1 − λ1) → 0, hence λK 

> 1

→ 1.

Proposition 7 (Circular local-band one-step obstruction) . Consider the equal-score biregular baseline on n

circular positions where row i is connected to columns i − r, . . . , i + r modulo n, with r ≥ 0. If n ≥ 8r + 2 ,then τ (M ) = 1 .Proof. The support of row α of the derivative kernel M consists exactly of columns within circular distance at most 2r from α: to move from column α to column β in one full derivative step, the two columns must share a support row, and this happens precisely when their circular distance is at most 2r. If n ≥ 8r + 2 ,there are two columns whose circular distance is at least 4r + 1 . Their radius-2r derivative neighborhoods are disjoint. The corresponding rows of M therefore have disjoint supports, so their total-variation distance is one and τ (M ) = 1 .

# B Registers, Dustbins, and Shared Buses 

Global tokens, register tokens, dustbin rows or columns, and expert buses can all create a common component in the derivative kernel. The architectural reading is simple: if every local region sends some reverse-mode mass through the same bus, zero-mass cotangents can no longer remain perfectly isolated. 

Proposition 8 (Register/dustbin mixing certificate) . Consider a fixed support in which a register or dustbin substructure induces a realized derivative kernel M . If the realized rows of M share common mass 

γcert = X

> j

min  

> i

Mij > 0,

then 

∥M ⊤η∥TV ≤ (1 − γcert )∥η∥TV 

for every zero-mass cotangent η.Proof. This is the sparse-support overlap certificate with qj = min i Mij /γ cert . The register or dustbin interpretation is architectural; the theorem only uses the realized common component. 22 Preprint Dylan B. Forde 

Theorem 13 (Equal-score register-bus certificate) . Let n ≥ 1, r ≥ 1, and let the token-token support on the 

n token rows and columns be d-regular with d ≥ 1: every token row and every token column has exactly d

active token-token edges. Add r ≥ 1 register rows and r register columns. Token rows connect to their active token columns and to every register column; register rows connect to every token and register column. Let 

N = n + r, use equal active scores, and set uniform marginals a = b = N −11.Define t > 0 by 

rt 2 + ( n − r)t − d = 0 , t = 2d

p(n − r)2 + 4 rd + ( n − r) .

Then the Sinkhorn plan has four values: 

Pij =



A, i, j token and (i, j ) active ,B, one endpoint token and one endpoint register ,D, i, j both registers ,

where 

A = 1

N (d + rt ) , B = tA, D = t2A. 

For the derivative kernel M = C⊤R, every source column α and every register target column h satisfy 

Mαh ≥ mreg := t(d + rt 2)(d + rt )2 .

Consequently the r register columns form a common component of mass at least 

γreg = r m reg = rt (d + rt 2)(d + rt )2 ,

and 

τ (M ) ≤ 1 − γreg .

Proof. The proposed plan has Sinkhorn scaling form for equal scores: token row and column scales are constant within the token class, and register row and column scales are constant within the register class. Its token row sum is 

dA + rB = A(d + rt ) = 1

N .

The token column sum is identical because the token-token support is d-regular and B is used for register-token edges. The register row and register column sums are 

nB + rD = A(nt + rt 2).

The defining quadratic for t is equivalent to nt + rt 2 = d + rt , so these sums also equal 1/N . Thus the candidate is a strictly positive feasible scaling of the equal-score support. By uniqueness of the entropic Sinkhorn plan on the fixed connected support, it is the Sinkhorn plan. Since a = b = N −11,

Mαβ = N 2 X

> i

Piα Piβ .

If α is a token column and h is a register column, the contributing rows are the d token rows incident to α

and the r register rows: 

Mαh = N 2(dAB + rBD ) = t(d + rt 2)(d + rt )2 .

If α is itself a register column, then 

Mαh = N 2(nB 2 + rD 2) = t2(n + rt 2)(d + rt )2 .

23 Preprint Dylan B. Forde 

Using d = rt 2 + ( n − r)t, the difference between the register-source value and the token-source value is 

rt 2(t − 1) 2

(d + rt )2 ≥ 0.

Thus every row of M assigns at least mreg to each register target column. The sparse-support overlap certificate applied to the common mass on the r register columns gives τ (M ) ≤ 1 − γreg .

Equal-score limitation. Theorem 13 is a pure topology theorem because equal scores and uniform marginals force a four-value scaled plan. With structured scores or nonuniform marginals, the same support still has a register-bus interpretation, but quantitative contraction must be certified from the realized plan or from a separate distortion/mass-floor hypothesis. The degenerate case d = 0 is a pure register-mediated support and is not covered by the four-value formula above. 

Design implication. A narrow local window may have τ (M ) = 1 because distant row distributions do not overlap. Adding a small register budget can reduce τ (M ) if the scaled plan routes enough mass through the shared component. This is a certificate knob, not a proof of task accuracy. 

# C Certificate-Level Design Rules 

The previous theorems describe what can be proved for the fixed-support transport derivative under the stated assumptions. They establish certificate logic rather than accuracy claims. These results suggest a practical matched-edge workflow. First test an equal-score biregular baseline to understand the pure graph walk. Then compare the candidate support’s realized τ (M K ) against simple graph descriptors and against a local-band control with the same edge budget. Finally, stress the candidate with nonuniform marginals and score perturbations; if the certificate survives, the topology becomes a candidate for a downstream task gate. If it does not survive, the failure is informative: the architecture may still be useful for restricted data, but the fixed-support transport derivative has no global quotient-mixing certificate under those conditions. 

# D Short DAG Path-Sum Remark 

A residual or branching computation graph is outside the core chain theorem, but the same quotient bookkeeping applies once every edge map has an explicitly bounded operator norm on the chosen quotient representative. If a sink-to-source cotangent can be expanded as a finite path sum 

ηsource = X

> p

wp

Y

> e∈p

M ⊤

> e

!

ηsink 

with zero-mass cotangents throughout, then Theorem 6 and the triangle inequality give 

∥ηsource ∥TV ≤ X

> p

|wp| Y

> e∈p

τ (Me)

!

∥ηsink ∥TV .

Any inhomogeneous source term must be projected and bounded separately, exactly as in Theorem 7. This appendix remark is a bookkeeping consequence, not an additional architecture claim. 

# References 

Roberto Cominetti and Jaime San Martín. Asymptotic analysis of the exponential penalty trajectory in linear programming. Mathematical Programming , 67:169–187, 1994. doi: 10.1007/BF01582220. 24 Preprint Dylan B. Forde 

Marco Cuturi. Sinkhorn distances: Lightspeed computation of optimal transport. In Advances in Neural Information Processing Systems , volume 26, 2013. URL https://papers.nips.cc/paper_files/paper/ 2013/hash/af21d0c97db2e27e13572cbf59eb343d-Abstract.html .Jesús A. De Loera and Edward D. Kim. Combinatorics and geometry of transportation polytopes: An update. arXiv preprint arXiv:1307.0124, 2013. URL https://arxiv.org/abs/1307.0124 .Marvin Eisenberger, Aysim Toker, Laura Leal-Taixé, Florian Bernard, and Daniel Cremers. A unified framework for implicit Sinkhorn differentiation. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition , pages 509–518, 2022. Dylan B. Forde. Block-Wise Differentiable Sinkhorn Attention: Tail-refinement gradients with a Single-Active-Dustbin Bridge. arXiv preprint arXiv:2605.08123v2, 2026. URL https://arxiv.org/html/2605.08123v2 .Title as printed in the v2 manuscript; arXiv listing uses Gap-Aware Dustbin Bridge. Stéphane Gaubert and Zheng Qu. Dobrushin ergodicity coefficient for Markov operators on cones, and beyond. arXiv preprint arXiv:1302.5226 , 2013. Martin Idel. A review of matrix scaling and Sinkhorn’s normal form for matrices and positive maps. arXiv preprint arXiv:1609.06349 , 2016. David A. Levin and Yuval Peres. Markov Chains and Mixing Times . American Mathematical Society, second edition, 2017. URL https://pages.uoregon.edu/dlevin/MARKOV/ . With contributions by Elizabeth L. Wilmer. Edouard Pauwels and Samuel Vaiter. The derivatives of Sinkhorn–Knopp converge. SIAM Journal on Optimization , 33(3):1494–1517, 2023. doi: 10.1137/22M1512703. URL https://epubs.siam.org/doi/10. 1137/22M1512703 .Gabriel Peyré and Marco Cuturi. Computational optimal transport: With applications to data science. 

Foundations and Trends in Machine Learning , 11(5–6):355–607, 2019. doi: 10.1561/2200000073. Richard Sinkhorn and Paul Knopp. Concerning nonnegative matrices and doubly stochastic matrices. Pacific Journal of Mathematics , 21(2):343–348, 1967. Jonathan Weed. An explicit analysis of the entropic penalty in linear programming. In Proceedings of the 31st Conference on Learning Theory , volume 75 of Proceedings of Machine Learning Research , pages 1841–1855, 2018. Jacob Wolfowitz. Products of indecomposable, aperiodic, stochastic matrices. Proceedings of the American Mathematical Society , 14(5):733–737, 1963. doi: 10.1090/S0002-9939-1963-0154756-3. 25