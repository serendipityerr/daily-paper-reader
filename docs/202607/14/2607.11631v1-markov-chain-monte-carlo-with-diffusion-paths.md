---
title: Markov Chain Monte Carlo with Diffusion Paths
title_zh: 基于扩散路径的马尔可夫链蒙特卡洛
authors: "Han Chen, Sifan Liu, Jun Yang"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11631v1"
tags: ["query:diff-bridge"]
score: 8.0
evidence: 扩散路径在目标分布和参考分布之间插值
tldr: 针对多模态分布采样难题，传统退火方法会扭曲非对称模态的相对权重。本文提出沿扩散路径插值，该路径保留模态权重并具有良好混合性质，通过谱隙分析理论保障。为消除变分估计和离散化引入的偏差，设计了MAD-Path采样器，在增广空间校正。实验表明，相比退火MCMC和未调整扩散采样器，该方法显著提升全局探索和模态权重估计精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.11631v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1445, \"height\": 407, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11631v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 762, \"height\": 573, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11631v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1556, \"height\": 553, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11631v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1547, \"height\": 728, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11631v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 387, \"height\": 297, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11631v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1546, \"height\": 429, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11631v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1548, \"height\": 1112, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11631v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1556, \"height\": 667, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11631v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1519, \"height\": 990, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11631v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1535, \"height\": 426, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11631v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1555, \"height\": 295, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.11631v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1513, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11631v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 692, \"height\": 258, \"label\": \"Table\"}]"
motivation: 传统退火方法在插值时扭曲非对称模态的相对权重，导致多模态采样混合缓慢。
method: 提出沿扩散路径插值，并设计MAD-Path采样器在增广路径空间进行Metropolis校正，消除变分得分估计和离散化误差。
result: 在多种贝叶斯后验分布上，MAD-Path改善了全局探索和模态权重估计，优于退火MCMC和未调整扩散采样器。
conclusion: MAD-Path为多模态后验采样提供了无需精确得分且鲁棒的MCMC方法。
---

## 摘要
从多模态分布中采样对于经典的局部马尔可夫链蒙特卡洛（MCMC）方法来说是一个长期存在的挑战。一种流行的补救措施是引入一系列在目标分布和更简单的参考分布之间插值的中间分布。经典的退火方法将密度提升到某个幂次，但会扭曲非对称模态的相对权重，并可能导致混合不良。我们转而提出沿着扩散路径进行插值，即一个将目标分布向高斯分布演化的加噪扩散过程的边际分布。该路径保留了模态的相对权重，并具有良好的混合性质，我们通过对相应理想转移核的谱间隙分析来精确化这一性质。沿路径采样需要其中间的得分函数，这可以通过变分方法从非归一化目标分布中估计，但只能得到近似采样器。为了消除由此产生的偏差，我们引入了Metropolis调整的扩散路径（MAD-Path）采样器，该采样器在增广路径空间中校正扩散路径提议，无论学习到的得分函数精度如何或离散化误差多大，都保持目标分布不变。我们进一步量化了这两种误差如何影响接受概率，为实际调参提供了指导。在一系列贝叶斯后验分布上的实验表明，与基于退火的MCMC方法和未调整的扩散采样器相比，MAD-Path改进了全局探索和模态权重估计。

## Abstract
Sampling from multimodal distributions is a longstanding challenge for classical local Markov chain Monte Carlo (MCMC) methods. A popular remedy is to introduce a sequence of intermediate distributions that interpolate between the target and a simpler reference. The classical choice, tempering, raises the density to a power, but distorts the relative weights of asymmetric modes and can lead to poor mixing. We instead propose interpolating along the diffusion path, the marginals of a noising diffusion process that carries the target toward a Gaussian. This path preserves the relative weights of the modes and enjoys favorable mixing properties, which we make precise through a spectral-gap analysis of the corresponding ideal transition kernel. Sampling along the path requires its intermediate scores, which can be estimated from the unnormalized target through variational approaches, yielding only an approximate sampler. To remove the resulting bias, we introduce the Metropolis-adjusted diffusion path (MAD-Path) sampler, which corrects the diffusion-path proposal in an augmented path space and leaves the target invariant regardless of the accuracy of the learned score or the discretization error. We further quantify how these two errors affect the acceptance probability, providing guidance for practical tuning. Experiments on a range of Bayesian posteriors show that MAD-Path improves global exploration and mode-weight estimation relative to tempering-based MCMC methods and unadjusted diffusion samplers.

---

## 论文详细总结（自动生成）

好的，遵循您的要求，以下是对该论文的结构化、详细的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：从**多模态分布**中进行采样是马尔可夫链蒙特卡洛方法的一个核心难题。传统局部MCMC方法（如随机游走Metropolis、HMC）在处理分离良好的多模态分布时，会因难以穿越低概率区域而在不同模态间混合不良。
- **现有方法的局限**：解决多模态采样问题的常见策略是引入**退火**（Tempering）方法，它通过一系列中间分布（\(p^{\beta_k}\)）在目标和简单分布间插值。然而，退火方法存在严重缺陷：当模态不对称时，将密度提升到幂次会**扭曲模态的相对权重**。例如，等权重但不同方差的混合高斯中，窄模态的权重在退火过程中会呈指数级缩小，导致混合极度缓慢（torpid mixing）。这一现象被称为“质量远距离传送”（teleportation of mass）。
- **本文的贡献**：论文提出一种新的MCMC方法，使用**扩散路径**（diffusion path）作为插值桥梁，从根本上避免了退火方法的权重扭曲问题，并通过Metropolis-Hastings校正确保了采样器的无偏性。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：
    1.  **扩散路径插值**：使用一个前向加噪的Ornstein-Uhlenbeck过程 \(dX_t = -\frac{1}{2}X_t dt + dB_t, X_0 \sim p\)，将目标分布 \(p\) 逐渐演化为近似标准高斯分布 \(N(0, I_d)\)。其边际分布 \(p_t\) 构成的路径 \((p_t)_{t\in[0,T]}\) 被称为扩散路径。
    2.  **保留权重优势**：与退火不同，扩散路径**精确保留了混合分布的权重**。如果目标是混合分布，那么任何时刻的边际分布 \(p_t\) 仍然是具有相同权重的混合分布，不受模态尺度差异影响。这使得在不同模态间移动成为可能。
    3.  **理想转移核**：前向过程的**时间逆过程**提供了从高斯分布回到目标分布的映射。由此构造的理想MCMC转移核：从当前状态 \(x_0\) 出发，沿前向扩散路径演进至时刻 \(T\)，然后沿精确反向路径回到时刻0，得到的 \(y_0\) 即为提议状态。该核可逆且以 \(p\) 为不变分布。
    4.  **谱间隙分析**：论文从理论上分析理想核的混合速度。通过将理想核视为数据增广吉布斯采样器，其谱间隙与 \(X_0\) 和 \(X_T\) 的最大相关系数 \(\rho_{\text{max}}\) 相关。对于满足Poincaré不等式的目标， \[ \text{Gap}(P) \ge 1 - \frac{\alpha C_P}{1-\alpha + \alpha C_P} \] 其中 \(\alpha = e^{-T}\)，\(C_P\) 是Poincaré常数。当目标为混合分布时，普间隙可分解为“模态间”（label）和“模态内”（within-component）贡献，证明了其对模态尺度差异不敏感。

- **关键技术细节：Metropolis调整扩散路径（MAD-Path）采样器**
    - **问题**：理想的扩散路径需要知道精确的中间得分函数 \(\nabla \log p_t\) 并连续模拟，这在实际中不可行。
    - **解决方案（MAD-Path算法）**：
        1.  **近似模拟**：使用学习到的近似得分 \(\hat{s}_t\) 和欧拉-丸山（Euler-Maruyama）离散化来近似前向和反向扩散过程。
        2.  **Metropolis-Hastings校正**：由于近似，提议分布存在偏差。论文提出在**增广路径空间**中进行校正。将前向路径 \((x_0, ..., x_N)\) 和反向路径 \((y_N, ..., y_0)\) 视为一个整体状态 \(z\)。提议由路径反转映射 \(R\) 产生，它交换前向和反向路径。由于 \(R\) 是保体积的，Metropolis接受概率为 \(1 \wedge \frac{\Pi(Rz)}{\Pi(z)}\)，其中 \(\Pi\) 是路径的联合分布。这一步确保了**无论得分函数和离散化有多大的误差，最终的马尔可夫链精确地以目标分布 \(p\) 为不变分布**。

- **接受概率分析**：
    - **离散化误差**：当使用精确得分时，对数MR比 \(\Delta_h\) 的期望为 \(- \frac{h}{2} D(T)\)，方差为 \(hD(T)\)，其中 \(D(T)\) 是累积Hessian偏差。这导致拒绝概率为 \(O(h^{1/2})\)。在高维i.i.d.目标下，最优接受率约为0.234。
    - **得分估计误差**：当使用近似得分时，\(\mathbb{E}[\Delta_h] = -\mathcal{E}_{\text{score}}(\delta, T)\)，其中 \(\mathcal{E}_{\text{score}}\) 是对称化路径KL散度，也等价于积分均方得分误差。这意味着得分误差成为影响接受概率的主导因素。

### 3. 实验设计：数据集、基准和对比方法

- **数据集/场景**：论文在六个逐渐复杂的场景上评估了MAD-Path：
    1.  **合成数据1（高斯混合）**：20维，不等方差的等权双高斯混合，用于展示退火方法的失败案例。
    2.  **合成数据2（偏斜正态混合）**：20维，13个分离良好且带有偏斜的模态。
    3.  **贝叶斯高斯混合模型**：使用 **Old Faithful 间歇泉**数据集（\(n=272, m=2\)），参数空间11维，后验因标签切换呈多模态。
    4.  **贝叶斯混合模型（Beta-Binomial & Binomial）**：使用 **胎鼠死亡**数据集，用于研究扩散时间T和步数N的影响。
    5.  **传感器网络定位**：推断11个传感器（其中3个已知）中8个未知位置的后验分布，后验呈多模态且呈“香蕉形”脊。
    6.  **看似不相关回归（SUR）模型**：M=5个方程，N=10个观测的小样本情况，轮廓似然呈现三个不同模态。

- **基准方法**：
    - **自适应并行退火（APT）**、**APT-DEO**、**退火过渡（TT）**
    - **退火重要性采样（AIS）**、**序贯蒙特卡洛（SMC）**
    - **未调整扩散（UD）采样器**：使用相同学习得分但无Metropolis校正。

### 4. 资源与算力

- 论文明确说明“所有实验均在一台配备 **Apple M3 处理器和 16 GB 统一内存** 的机器上进行。” 文中提到各算法在相似计算预算（目标得分评估次数）下比较，并且所有方法的**总运行时间（包括训练和采样）也基本相当**。
- 论文未提及GPU型号或更具体的训练时长细节。

### 5. 实验数量与充分性

- **实验数量**：论文进行了**大量、系统的实验**，覆盖了从合成数据到真实世界数据的多个场景，并包含详细的消融研究。
    - 在6.1和6.2节，对比了7种方法，并在6.2节进行了**100次独立重复实验**以估计模态权重的精度。
    - 在6.3节，对贝叶斯高斯混合模型进行了**100次独立重复实验**以计算成功率。
    - 在6.4节，系统地研究了时间 \(T\) 和步数 \(N\) 的影响，进行了**100次独立重复实验**并报告了中位数和四分位距。
    - 在6.5和6.6节，进行了多个链条的定性比较。
- **实验充分性与公平性**：
    - **充分性**：实验设计非常全面，不仅验证了MAD-Path的优越性，还深入探讨了关键超参数（\(T, N\)）的影响，并分析了得分估计误差的作用，实验覆盖度很高。
    - **客观性与公平性**：
        - 论文努力确保计算预算（目标得分评估次数）可比。
        - 对比了多种主流的退火和非退火基线方法。
        - 在多个场景下进行了独立重复实验以评估统计健壮性。
        - 结论基于定量指标（如总变距离、成功率、Max/Min比率）和定性可视化（如迹图、直方图）。
    - **潜在偏差风险**：虽然实验设计公平，但MAD-Path和UD的得分函数是在离线训练阶段学习的，而其他基线方法是在线MCMC。尽管论文提到总运行时间相近，但两者对计算资源的需求分配不同，这可能对某些方法更有利或不利。

### 6. 论文的主要结论与发现

- **主要发现**：提出的MAD-Path采样器在处理多模态后验分布时，**显著优于**所有对比的退火方法和未调整扩散采样器。
- **具体结论**：
    1.  **克服退火缺陷**：在不等方差高斯混合等“失败场景”中，退火方法（APT，APTDEO，TT，AIS，SMC）均产生严重偏差或混合不良，而MAD-Path准确恢复了目标分布。
    2.  **提升模态权重估计**：在偏斜正态混合和高斯混合模型中，MAD-Path的模态权重估计总变距离更小、Max/Min比率更接近1，且在100次实验中成功率最高（100%）。
    3.  **全局探索能力**：迹图和散点图显示，MAD-Path在所有模态间频繁跳跃，而其他方法经常陷入单一模态或缓慢移动。尤其是在传感器网络和SUR模型中，MAD-Path几乎无瞬态相位即开始采样，优于需要数百次迭代进行初始化的APTDEO。
    4.  **校正偏差**：与未调整扩散采样器对比表明，Metropolis-Hastings校正对于移除由近似得分引起的偏差至关重要，即使得分的近似质量不高，最终的马尔可夫链也是无偏的。
    5.  **关键因素**：足够的扩散时间 \(T\) 和细的离散化步长 \(N\) 是确保高接受率和模态间跃迁率的关键。

### 7. 优点

- **理论扎实**：对理想扩散路径核的谱间隙进行了详尽分析，从理论上证明了其对非对称多模态分布的优势，为算法设计提供了坚实的理论基础。
- **方法严谨**：MAD-Path的精妙之处在于，它利用增广路径空间上的Metropolis-Hastings校正，巧妙地消除了近似得分和离散化带来的偏差，使得算法既是**精确的**MCMC，又能够利用强大的近似工具（如神经网络学习得分）。这种“组合”思路非常优雅。
- **分析透彻**：论文系统性地分析了离散化误差和得分估计误差对接受概率的影响，并给出了高维下的最优调参指南（如最优接受率0.234），具有很高的实用价值。
- **实验全面**：实验设计覆盖了多个具有挑战性的多模态场景，对比方法全面，且通过重复实验验证了结果的统计显著性。特别是6.4节的消融研究，清晰地展示了超参数对性能的影响。

### 8. 不足与局限

- **得分学习依赖**：虽然MAD-Path的纠错机制保证了无偏性，但**效率高度依赖得分函数的近似质量**。如果得分学习不够好（如存在模态坍缩），接受率会很低，导致采样效率低下。论文中的得分学习通过一个两阶段方法（先发现模态，再构建混合高斯参考分布）来缓解模态坍缩，但这增加了实现的复杂性。一个自动且通用的高质量得分学习器仍是开放问题。
- **计算成本**：方法需要1) 离线训练得分函数 (可能耗时) 和 2) 在线MCMC中每一步都需要模拟完整的前向-反向路径（\(N\)步）。相比于传统单步MCMC，其**单次提议的计算成本更高**。虽然论文指出总时间与APT-DEO相当，但算力分配在不同方法间并不完全对等。
- **理论假设**：谱间隙分析依赖于Poincaré不等式等较强的前提假设。对于现实中不满足这些条件的目标，理论结论需要谨慎推广。
- **参数敏感性**：算法的性能对扩散时间 \(T\) 和步数 \(N\) 敏感。虽然6.4节提供了指导，但选择最优的超参数可能需要额外的调试工作。
- **应用限制**：MAD-Path主要适用于概率密度可显式评估但归一化常数未知的场景。如果评估目标密度或其梯度代价极高（如高保真物理模拟），那么训练得分函数和运行MCMC的总成本可能无法接受。

（完）
