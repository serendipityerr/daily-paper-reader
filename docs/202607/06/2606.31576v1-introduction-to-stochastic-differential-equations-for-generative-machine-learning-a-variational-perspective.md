---
title: "Introduction to Stochastic Differential Equations for Generative Machine Learning: A Variational Perspective"
title_zh: 面向生成式机器学习的随机微分方程导论：一种变分视角
authors: "Ole Winther, Paul Jeha, Sander Dieleman, Andriy Mnih, Manfred Opper, Andrea Dittadi"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31576v1"
tags: ["query:diff-bridge"]
score: 8.0
evidence: 随机微分方程用于生成建模
tldr: 生成式机器学习中随机微分方程的应用取得显著进展。本文从变分视角介绍常微分和随机微分方程的概率框架及Fokker-Planck方程，推导ELBO。将扩散模型、得分匹配和流匹配统一为变分方法的特定参数化，并通过一维密度建模示例比较。该工作为理解和实现生成模型提供了统一数学基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31576v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1636, \"height\": 842, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31576v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1687, \"height\": 1566, \"label\": \"Figure\"}]"
motivation: 提供生成式模型中基于微分方程的统一变分视角，并以ELBO为通用出发点。
method: 从变分框架导出ELBO，将扩散模型、得分匹配和流匹配视为不同参数化，并用Fokker-Planck方程描述分布演化。
result: 通过一维密度建模示例比较不同参数化，展示了变分框架的统一性。
conclusion: 该变分视角统一了多种生成模型，为理解和改进生成式模型提供理论基础。
---

## 摘要
常微分和随机微分方程的使用已经推动了生成式机器学习的重大进展，例如在图像、视频和生物分子生成中的应用。本文以自包含且非正式的方式介绍了这些微分方程、将其用于生成式建模的概率框架，以及控制微分方程随机变量边缘分布随时间演变的福克-普朗克方程。推导了对数似然的变分下界（证据下界，ELBO），并将其作为讨论扩散模型、分数匹配和流匹配的一般起点。所有这些方法都可以被视为最一般变分方法的特定参数化。使用一维密度建模问题作为简单示例来比较不同的参数化。

## Abstract
The use of ordinary and stochastic differential equations has led to substantial progress in generative machine learning with applications to, for example, image, video and biomolecule generation. This paper provides a self-contained and informal introduction to the differential equations, the probabilistic framework for using them in generative modeling and the Fokker--Planck equation that governs the temporal evolution of the marginal distribution of the stochastic variables of the differential equations. The variational lower bound on the log-likelihood (the evidence lower bound, ELBO) is derived and used as a general starting point for a discussion of diffusion models, score matching, and flow matching. All of these approaches may be viewed as specific parameterizations of the most general variational approach. A one-dimensional density modeling problem is used as a simple example to compare different parameterizations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

近年来，基于常微分方程（ODE）和随机微分方程（SDE）的生成模型（如扩散模型、分数匹配、流匹配、神经ODE等）在图像、视频和分子生成等任务上取得了突破性进展。然而，这些方法的文献往往假设读者具备随机过程的高级知识，且不同方法在表述上存在差异，缺乏统一的理论框架。本文旨在提供一个自包含、非正式但严谨的导论，从变分视角统一这些方法，推导出证据下界（ELBO），并展示扩散模型、分数匹配和流匹配均是该ELBO的特定参数化。其核心贡献是：①系统推导了Fokker–Planck方程、逆时SDE等基础工具；②从变分框架出发，建立了统一的训练目标；③通过一个一维密度建模实验，直观比较了不同参数化的表现。

### 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**  
将生成过程建模为从简单先验分布 \(p_0(x_0)\) 出发，经过一个ODE或SDE驱动的潜在过程，在终点 \(t=1\) 得到 \(x_1\)，然后通过似然 \(p(y|x_1)\) 生成数据 \(y\)。通过引入变分后验 \(q_1(x_1|y)\)（可扩展为整个路径 \(q(X|y)\)），推导出ELBO。不同方法对应于对生成漂移和变分漂移的不同参数化。

**关键技术细节**  
1. **Fokker–Planck方程**：描述了SDE边缘分布的时间演化，并用于建立ODE与SDE之间保持相同边缘分布的关系。通过概率流ODE，可以将SDE转换为具有相同边缘分布的ODE。  
2. **变分下界推导**：  
   - 对于ODE，可以通过改变变量公式精确计算 \(\log p_1(x_1)\)，从而得到可计算的ELBO（需模拟ODE）。  
   - 对于SDE，直接计算不可行，需将边缘KL散度分解为路径KL散度。通过引入一个与生成模型共享扩散系数的变分SDE，将ELBO分解为三项：重建项、先验KL项和扩散损失项 \(D(f,g,y)\)（即时间平均的均方误差）。  
3. **统一参数化**：  
   - 扩散模型：变分漂移为线性高斯形式，生成漂移参数化为信号预测网络。  
   - 分数匹配：生成漂移参数化为 \(\tilde{f}(x,t) = \tilde{g}(x,t) + \sigma^2(x,t) s(x,t)\)，其中 \(s\) 是学习的分数函数。  
   - 流匹配：生成漂移参数化为 \(f(x,t) = 2 v(x,t) - \tilde{g}(x,t)\)，其中 \(v\) 是学习的流函数。  

**算法流程**（文字说明）  
- **ODE训练**：采样 \(x_1 \sim q_1(x_1|y)\)，然后逆向求解ODE（从 \(t=1\) 到 \(0\)），同时累加对数密度变化，得到ELBO。  
- **SDE训练**：采样 \(t \sim U[0,1]\) 和 \(x_t \sim q_t(x_t|y)\)，计算扩散损失项，加上重建项和先验KL项得到ELBO，无需模拟整个路径。  
- **生成**：从先验采样 \(x_0\)，正向求解ODE/SDE得到 \(x_1\)，再从似然中采样 \(y\)。

### 3. 实验设计

- **实验场景**：一维密度建模，真实分布为5个拉普拉斯分布的混合（位置在 \([-8, -4, 0, 4, 8]\)，尺度为1）。训练数据包含 \(n=1000\) 个样本，验证集 \(n_{val}=8000\) 个样本。  
- **基准（Benchmark）**：真实分布的对数似然精确值（计算为 \(-2.9862\)）。  
- **对比方法**：  
  - ODE模型（连续流）：使用2-64-64-1全连接网络作为漂移，Dirac似然。  
  - 流匹配（Flow Matching）：使用相同的网络架构，扩散系数 \(\sigma(x,t)=1\)，训练10000 epoch。  
  - SDE模型（VDM）：变分高斯分布，基于信号预测网络。  
  - SDE模型（一般参数化）：变分漂移自由学习，同样基于神经网络。  
- **实验设置**：均使用Adam优化器，batch size=125，学习率 \(10^{-2}\) 或 \(10^{-3}\)。未使用真实图像数据集，仅为一维密度。

### 4. 资源与算力

论文**未明确说明**所使用的GPU型号、数量或训练时间。仅给出了epoch数量（ODE 200 epoch，流匹配 10000 epoch，SDE 2000 epoch）和batch size，未提供总算力消耗。

### 5. 实验数量与充分性

- **实验数量**：仅一个简单的一维密度建模问题，包含4种方法配置（ODE、流匹配、VDM-SDE、一般SDE）。每个方法仅运行一次（无多次重复消融）。  
- **充分性**：作为教程性质的文章，该实验足以定性展示不同参数化的行为和ELBO值的差异，但不足以评估方法在实际复杂数据上的性能。实验覆盖度较低，缺乏对超参数敏感性、不同噪声调度、多维数据的评估。  
- **公平性**：所有方法使用类似大小的网络，优化器参数一致，但训练epoch数差异较大（流匹配需要更多epochs）。未进行等计算量比较，因此不能直接断言方法的优劣。此外，流匹配未优化ELBO（仅优化扩散损失），其ELBO不可直接比较。

### 6. 论文的主要结论与发现

1. 扩散模型、分数匹配和流匹配均可被理解为变分ELBO的特定参数化，从而统一了这些方法的理论基础。  
2. ODE模型可以精确计算对数似然，但需要模拟ODE；SDE模型通过路径KL上界得到模拟自由的ELBO，但下界更松。  
3. 分数匹配的最优分数函数等于无条件边缘分布的分数 \(\nabla \log q_t(x)\)；流匹配的最优流函数等于数据条件漂移的边缘化 \(E_{q_t(y|x)}[g(x,y,t)]\)。  
4. 在一维实验中，ODE和一般SDE（自由学习变分漂移）取得了相似的验证对数似然（约-3.05），优于VDM-SDE（约-3.25）。流匹配虽然未直接优化ELBO，但能学习到合理的密度。  
5. 对于SDE模型，如果变分分布选择得当（如一般参数化），可以获得接近ODE的ELBO值，表明路径KL间隙可能较小。

### 7. 优点

- **理论统一性**：首次将扩散模型、分数匹配、流匹配系统性地纳入同一个变分框架，推导清晰且严谨。  
- **自包含性**：无需Itô微积分或Girsanov定理，通过Taylor展开和Euler–Maruyama离散化进行直观推导，适合入门者。  
- **灵活性**：方法论中处理了高斯变分分布及其混合情形，并提供了从流中导出漂移的通用公式，为设计新的变分过程提供指导。  
- **对比实验**：尽管简单，但展示了不同参数化的ELBO差异和训练表现，有助于理解各方法的权衡。

### 8. 不足与局限

- **实验规模极小**：仅使用一维合成数据，无法评估在高维图像/视频等实际任务中的表现。  
- **缺乏消融与统计分析**：每种方法仅运行一次，未报告方差或多次重复结果，不能判断性能的稳定性。  
- **流匹配评估不完整**：流匹配的ELBO未计算，仅从密度图定性判断，无法与其他方法定量比较。  
- **未探讨计算效率**：流匹配虽训练更便宜，但生成的采样步骤与其他方法相同，论文未对比采样效率或训练时间。  
- **假设限制**：变分分布仅考虑高斯或高斯混合（一般参数化也基于线性变换），未涉及更复杂的分布；扩散系数通常取标量，未讨论各向异性情况。  
- **缺失实际应用场景**：论文未使用标准生成基准（如CIFAR-10、ImageNet）进行实验，因此方法的实用性能无法从本文评估。  
- **未讨论超参数选择**：如扩散系数 \(\sigma(x,t)\) 的选择、时间调度的影响等，文中未给出指导或敏感性分析。  

（完）
