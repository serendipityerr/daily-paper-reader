---
title: Effective dynamics of the Sinkhorn algorithm in the regime of low entropy regularization
title_zh: 低熵正则化条件下Sinkhorn算法的有效动力学
authors: Guillaume Wang
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00665v1"
tags: ["query:diff-bridge"]
score: 8.0
evidence: Sinkhorn算法用于最优传输
tldr: "针对熵正则化最优传输的Sinkhorn算法，理论认为需O(τ^{-2})步才能收敛。本文发现当正则化参数τ很小时，将迭代步数重缩放为t=τk后，Sinkhorn迭代逼近一条连续时间曲线，表现出分段线性与鞍点动态，称为冷Sinkhorn动态。该动态在有限步内达到无正则化解，对偶次优性为Õ(τ)，仅需O(τ^{-1})步，显著优于经典分析。这一结果解释了低熵下Sinkhorn算法的加速本质。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00665v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1539, \"height\": 666, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00665v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1540, \"height\": 538, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00665v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1539, \"height\": 360, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00665v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1463, \"height\": 791, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00665v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1461, \"height\": 793, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00665v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1285, \"height\": 637, \"label\": \"Table\"}]"
motivation: "低熵正则化下Sinkhorn收敛速度的理论界O(τ^{-2})与实际观察不符，需更精确刻画。"
method: 引入时间重缩放t=τk，证明Sinkhorn迭代收敛到连续时间曲线并解析其动态方程。
result: "冷Sinkhorn动态在有限时间收敛于无正则化解，实现Õ(τ)次优性仅需O(τ^{-1})步。"
conclusion: "揭示Sinkhorn算法在低熵下具有类似单纯形法的有效动态，收敛阶提升至O(τ^{-1})。"
---

## 摘要
Sinkhorn算法是数值求解有限集上熵正则化最优传输问题的事实标准方法。本文研究了当Sinkhorn算法应用于小正则化参数τ时出现的一种现象：对偶变量（缩放因子的对数）的演化近似分段线性，而原始变量（近似传输方案）则表现出鞍点至鞍点类型的行为。我们证明当τ→0时，若时间尺度重标为t=τk，Sinkhorn迭代确实收敛到与这些观察一致的连续时间曲线，并显式刻画了极限的“冷Sinkhorn”动力学。特别地，我们展示了该动力学作为非正则化问题的对偶优化动力学，具有类似于单纯形算法的性质。值得注意的是，该动力学在有限时间内收敛到非正则化解，这意味着Sinkhorn算法本身有一个新颖的保证：它在k=O(τ^{-1})次迭代中达到Õ(τ)的对偶次优性，而非现有分析所暗示的k=O(τ^{-2})。

## Abstract
The Sinkhorn algorithm is the de facto standard method for numerically solving entropy-regularized optimal transport problems over finite sets. In this work, we investigate a phenomenon arising when Sinkhorn is applied with a small regularization parameter $τ$: the evolution of the dual variables (the logarithm of the scaling factors) is approximately piecewise-linear, while the primal variables (the approximate transport plans) exhibit a saddle-to-saddle type behavior. We prove that as $τ\to 0$, the Sinkhorn iterates indeed converge to a continuous-time curve consistent with these observations, when time is rescaled as $t = τk$, and we characterize the limiting "cold Sinkhorn" dynamics explicitly. In particular, we show that it acts as a dual optimization dynamics for the unregularized problem with properties analogous to the simplex algorithm. Notably, this dynamics converges in finite time to an unregularized solution, implying a novel guarantee for the Sinkhorn algorithm itself: it achieves $\tilde{O}(τ)$ dual suboptimality in $k = O(τ^{-1})$ iterations, instead of $k = O(τ^{-2})$ as existing analyses would suggest.

---

## 论文详细总结（自动生成）

### 论文核心总结

#### 1. 核心问题与整体含义（研究动机与背景）
*   **研究动机**：Sinkhorn算法是求解熵正则化最优传输（EOT）问题的事实标准方法。经典收敛分析表明，其迭代次数与正则化参数τ的平方成反比（即 \( k = O(\tau^{-2}) \)），才能达到高精度解。然而，数值实验观察到Sinkhorn算法在τ很小时收敛速度远快于此预期。
*   **核心问题**：解释并量化Sinkhorn算法在低熵（小τ）正则化条件下的加速收敛现象，揭示其潜在的动力学机制。
*   **整体含义**：理论表明，当τ→0时，Sinkhorn算法本质上是实现了一种**离散的、连续时间的“冷Sinkhorn动力学”**，该动力学是一种针对无正则化最优传输问题的优化算法，具有类似单纯形法的分段线性行为。该发现不仅解释了实验现象，还将收敛阶从 \( O(\tau^{-2}) \) 提升至 \( O(\tau^{-1}) \)，显著改进了理论界。

#### 2. 方法论
*   **核心思想**：引入时间重缩放 \( t = \tau k \)，将离散的Sinkhorn迭代（以k编号）视为连续时间曲线 \( (f(t), g(t)) \) 的离散化。通过证明当τ→0时，Sinkhorn迭代一致收敛到由“冷Sinkhorn动力学”定义的极限曲线。
*   **关键技术细节**：
    1.  **冷Sinkhorn动力学定义**：一种连续时间分段线性动态系统，其运动由当前有效边集 \( E_\ell \) 决定。动力学轨迹为 \( (f(t), g(t)) \in \partial \mathcal{F} \)（Pareto前沿），满足：
        \[
        \frac{d}{dt} f(t) = v^\ell, \quad \frac{d}{dt} g(t) = w^\ell
        \]
        其中 \( v^\ell, w^\ell \) 是由 \( E_\ell \) 确定的增量向量（来源于极限 \( \tau \to 0 \) 时的Sinkhorn更新规则）。
    2.  **算法流程（文字描述）**：
        *   初始化：\( t_0=0 \)，初始点 \( (f(0), g(0)) \) 位于Pareto前沿 \( \partial \mathcal{F} \)。
        *   迭代过程：在每个阶段 \( \ell \)，系统以恒定速度 \( (v^\ell, w^\ell) \) 在 \( \partial \mathcal{F} \) 上沿直线运动。
        *   下一阶段的触发：当任意非活动边 \( (i,j) \notin E_\ell \) 对应的“约束差距” \( f_i(t) + g_j(t) - C_{ij} \) 达到0时，当前阶段结束，触发阶段转换。
        *   阶段转换：通过重新确定当前活动边集 \( E_{\ell+1} \)，并计算新的增量方向 \( (v^{\ell+1}, w^{\ell+1}) \)，进入下一阶段。
    3.  **关键引理**：
        *   动态系统的有限性：由于活动边集 \( E_\ell \) 有限且单调，动态必然在有限时间 \( t_L \) 和有限阶段 \( L \) 后终止。
        *   最终收敛性：在最终阶段，系统到达一个不动点 \( (v^L, w^L) = (0, 0) \)，该点恰好是原无正则化对偶最优传输问题的一个解。
    4.  **Sinkhorn迭代的收敛性**：论文证明，在时间重缩放 \( t = \tau k \) 下，Sinkhorn迭代 \( (f^{\lfloor 2t/\tau \rfloor}, g^{\lfloor 2t/\tau \rfloor}) \) 在相空间内一致收敛到冷Sinkhorn动力学轨迹 \( (f(t), g(t)) \)，收敛速度为 \( O(\tau (\log 1/\tau)^{2L+1}) \)。

#### 3. 实验设计
*   **数据集/场景**：实验未使用标准基准数据集，而是使用服从高斯分布的随机代价矩阵 \( C \)，并设定均匀或随机的边际分布 \( \mu, \nu \)。实验中改变了维度（m=5, n=6; m=n=50; m=n=400）和正则化参数τ（例如0.005, 0.001）。
*   **Benchmark**：论文未设置外部的SOTA方法作为对比基准。其核心是**理论分析与数值实验的相互印证**，侧重于展示作者提出的“冷Sinkhorn动力学”现象和理论界。
*   **对比方法**：未与其他加速算法（如Greenkhorn、Overrelaxed Sinkhorn）进行对比。实验主要用于验证理论预测，如分段线性增量和鞍点动态。

#### 4. 资源与算力
*   **资源说明**：文中未明确说明所使用的GPU型号、数量或训练/运行时长。从论文描述看，实验在小规模问题（\( m,n \leq 400 \)）上运行，算力需求不大，应可在单台CPU或普通GPU上轻松复现。

#### 5. 实验数量与充分性
*   **实验数量**：提供了约4组独立实验（图1对应m=5, n=6；图2对应m=2, n=3；附录A有m=n=50和m=n=400两图）。此外，论文还包含多个理论辅助引理（Lemma B.1, B.2, B.4等），这些引理的数值验证通常也属于实验范畴。
*   **充分性评估**：
    *   **充分性**：实验充分展示了所发现的核心现象（分段线性、鞍点动态），并对不同维度（小、中、大）进行了测试，验证了理论预测的普遍性。
    *   **客观性**：实验设计清晰，对比了自身的理论推导与数值结果，结论具有说服力。
    *   **局限性**：实验规模偏小（最大400维），未涉及大规模真实世界数据集（如高维图像、单细胞数据），也未与其他Sinkhorn变体进行效率对比。因此，实验的全面性和在实际应用中的验证程度有限。

#### 6. 核心结论与发现
1.  **极限动力学**：当 \( \tau \to 0 \)，时间重缩放为 \( t = \tau k \) 后，Sinkhorn迭代收敛到一条由“冷Sinkhorn动力学”定义的连续时间曲线。
2.  **分段线性与鞍点动态**：该极限动力学呈分段线性，对应原始变量（传输方案）的鞍点行为。
3.  **收敛阶提升**：该动力学在有限时间 \( t_L \) 内收敛到无正则化对偶最优问题的一个解。这直接导出Sinkhorn算法的新保证：仅需 \( k = O(\tau^{-1}) \) 次迭代即可达到 \( \widetilde{O}(\tau) \) 的对偶次优性，取代了经典的 \( O(\tau^{-2}) \)。
4.  **算法本质**：冷Sinkhorn动力学是一种专门针对对偶最优传输问题的“优化动力学”，其行为类似于传统线性规划中的单纯形法，但路径在Pareto前沿上。

#### 7. 优点
*   **理论创新性**：第一次揭示了Sinkhorn在低熵极限下的内在“加速”本质，严格证明了它对应于一个更快的、基于边集的优化过程，而非一个简单的梯度下降过程。
*   **清晰的现象刻画**：通过时间重缩放技术，将复杂的离散迭代行为转化为简洁的分段线性连续动态，提供了非常直观的理解。
*   **显著的理论界提升**：将最坏情况下的迭代复杂度从 \( O(\tau^{-2}) \) 提升至 \( O(\tau^{-1}) \)，对实际算法设计具有指导意义。
*   **泛化能力**：论文指出，除了标准Sinkhorn，其他变体（如Greenkhorn）也可能存在类似现象，显示了该发现的普遍性潜力。

#### 8. 不足与局限
*   **理论不完美**：收敛率 \( O(\tau (\log 1/\tau)^{2L+1}) \) 的常数项未显式给出，且依赖问题规模（m, n）和边际分布。在非常低的精度要求下，实际加速效果可能被高阶对数项掩盖。
*   **实验覆盖局限**：实验仅在简化（随机代价、统一或简单随机边际）的小规模问题上验证，未在标准基准测试（如Wasserstein距离计算、单细胞轨迹推断等）上展示其实际优越性。
*   **无对比试验**：论文未与其它旨在加速Sinkhorn的算法（如Overrelaxed Sinkhorn）进行等精度对比，也未在真实应用场景下测试。
*   **未完全解决复杂性**：尽管提升了收敛阶，但并没有从理论上证明其总能达到超越线性规划的复杂度最优。论文也承认，当问题维度很大时，分段线性阶段的切换次数（即相数）可能非常大。

（完）
