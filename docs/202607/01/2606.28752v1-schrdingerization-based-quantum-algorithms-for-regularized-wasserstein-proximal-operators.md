---
title: Schrödingerization based quantum algorithms for regularized Wasserstein proximal operators
title_zh: 基于薛定谔化的正则化Wasserstein近端算子的量子算法
authors: "Shi Jin, Nana Liu, Yue Yu"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28752v1"
tags: ["query:diff-bridge"]
score: 8.0
evidence: 利用薛定谔化方法求解正则化Wasserstein近端算子，与薛定谔桥问题直接相关
tldr: 最优输运和平均场博弈中，正则化Wasserstein近端算子求解涉及Fokker-Planck与粘性Hamilton-Jacobi耦合PDE系统。本文通过Cole-Hopf变换将其转化为前向热方程，并利用Schrödingerization方法高效求解。量子算法在d维空间N_x网格点上达到O(d N_x T log²(1/ε))查询复杂度，相对经典方法呈指数加速。数值实验验证了算法有效性，为相关领域提供了新的量子求解途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28752v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 965, \"height\": 253, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28752v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1313, \"height\": 523, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28752v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1345, \"height\": 507, \"label\": \"Figure\"}]"
motivation: 正则化Wasserstein近端算子是求解最优输运和平均场博弈的基础工具，经典方法受维度灾难制约。
method: 通过Cole-Hopf变换将耦合PDE转为热方程，用Schrödingerization量子求解，并用Hadamard运算处理初始与终态耦合。
result: 算法达到O(d N_x T log²(1/ε))查询复杂度，较经典O(N_x^d)实现指数加速，数值实验验证有效。
conclusion: 首个正则化Wasserstein近端算子的高效量子算法，理论分析与数值验证均表明其显著加速潜力。
---

## 摘要
我们为正则化Wasserstein近端算子开发了一种量子算法，这是最优输运和平均场博弈中的基本工具。正则化在Benamou-Brenier公式的连续性方程中引入了一个小的扩散项，从而产生一个前向-后向偏微分方程组，包含一个Fokker-Planck方程和一个具有二次哈密顿量的粘性Hamilton-Jacobi方程。通过Cole-Hopf变换，两个方程都转化为前向热方程，其耦合需要哈达玛除法来准备第二个热方程的初始数据，以及哈达玛乘积来恢复终端密度。我们通过薛定谔化方法求解这些热方程，并使用简单的矩阵-向量乘法表示实现哈达玛除法和乘积操作。完整的量子算法以$\mathcal{O}(d N_x T \log^2(1/\varepsilon))$的查询复杂度制备终端密度态的$\varepsilon$近似，其中常数依赖于势能和初始密度，$d$是空间维度，$N_x$是每个空间维度的网格点数，$T$是演化时间。复杂度仅{\it线性}依赖于$d N_x$，相比于每时间步成本为$N_x^d$的经典方法实现了{\it指数级}加速。数值实验验证了所提算法的有效性。

## Abstract
We develop a quantum algorithm for the regularized Wasserstein proximal operator, which is a fundamental tool in optimal transport and mean-field games. The regularization introduces a small diffusive term into the continuity equation of the Benamou-Brenier formulation, which results in a forward-backward PDE system consisting of a Fokker-Planck equation and a viscous Hamilton-Jacobi equation with a quadratic Hamiltonian. Through the Cole-Hopf transformation, both equations are converted to forward heat equations, whose coupling requires a Hadamard division to prepare the initial data for the second heat equation and a Hadamard product to recover the terminal density. We solve these heat equations via the Schrödingerization method and implement the Hadamard division and product operations using simple matrix-vector multiplication representations. The complete quantum algorithm prepares an $\varepsilon$-approximation of the terminal density state with $\mathcal{O}(d N_x T \log^2(1/\varepsilon))$ query complexity, up to constants depending on the potential and initial density, where $d$ is the spatial dimension, $N_x$ is the number of grid points per spatial dimension and $T$ is the evolution time. The complexity depends only {\it linearly} on $d N_x$, yielding an {\it exponential} speedup over classical methods, whose cost scales as $N_x^d$ per time step. Numerical experiments validate the effectiveness of the proposed algorithm.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：正则化 Wasserstein 近端算子 (WPO) 是最优输运、平均场博弈、扩散模型等领域的核心工具。其核心是通过求解一个耦合的前向-后向偏微分方程组 (PDEs) 来演化概率分布。然而，在经典计算框架下，随着空间维度 \(d\) 的增加，该问题的求解复杂度呈指数级增长（维度灾难），在高维场景（如生成模型、平均场博弈）中变得难以处理。
- **研究动机**：开发高效的量子算法，利用量子计算机的并行性来克服经典计算的维度灾难，从而实现正则化 WPO 的快速求解。这不仅具有理论意义，也为量子生成模型、量子控制等领域提供新的基础算法。
- **整体含义**：本文首次提出针对正则化 WPO 的高效量子算法，证明了其相对于经典方法的指数级加速潜力，为量子计算在科学计算和机器学习中的应用开辟了新路径。

#### 2. 方法论：核心思想、关键技术细节

- **核心思想**：通过数学变换将复杂的耦合 PDE 系统简化为适合量子模拟的形式，然后利用量子优势来高效求解。

- **关键技术细节与算法流程**：
    1.  **问题转化**：正则化 WPO 对应一个由 Fokker-Planck 方程 (2.2) 和粘性 Hamilton-Jacobi 方程 (2.3) 组成的前向-后向 PDE 系统。
    2.  **Cole-Hopf 变换**：这是关键的一步。该变换将原始的耦合非线性系统**精确地**转化为两个独立且线性的**前向热方程**：
        *   方程一：\(\partial_t \eta = \beta \Delta \eta\)，初始条件 \(\eta(0, x) = e^{-V(x)/(2\beta)}\)。
        *   方程二：\(\partial_t \psi = \beta \Delta \psi\)，初始条件 \(\psi(0, x) = \rho_0(x) / \eta(T, x)\)。
        *   最终解通过 \(\rho(T, x) = \psi(T, x) \cdot e^{-V(x)/(2\beta)}\) 恢复。
    3.  **核心挑战与解决**：转化后，核心挑战变为求解两个热方程，并在第二步和第三步执行高效的**点除**（Hadamard division）和**点乘**（Hadamard product）以耦合和恢复结果。
    4.  **Schrödingerization 方法**：用于高效求解热方程。该方法将扩散方程“量子化”为薛定谔方程的形式，从而适合在量子计算机上模拟。其复杂度与 \(\beta T d N_x\) 成正比，是线性的。
    5.  **Hadamard 除法和乘积的量子实现**：论文提出利用**对角块编码**（Diagonal Block-Encoding）和**量子矩阵求逆**（QLSA）来实现。对于向量 \(\boldsymbol{a}\) 和 \(\boldsymbol{b}\)：
        *   **点积**：\(\boldsymbol{a} \odot \boldsymbol{b}\) 可通过构造对角矩阵 \(D_{\boldsymbol{a}}\) 并作用到 \(|b\rangle\) 上实现。
        *   **点除**：\(\boldsymbol{a} \oslash \boldsymbol{b}\) 等效于求解 \(D_{\boldsymbol{b}} \boldsymbol{x} = \boldsymbol{a}\)，可通过 QLSA 高效求解，其复杂度与矩阵 \(D_{\boldsymbol{b}}\) 的条件数 \(\kappa_{\eta(T)}\) 和对数精度相关。
    6.  **完整算法流程**：
        *   **Step 1**：使用 Schrödingerization 求解热方程 (2.5)，得到状态 \(|\eta(T)\rangle\)。
        *   **Step 2**：利用 Hadamard 除法电路，从 \(|\rho_0\rangle\) 和 \(|\eta(T)\rangle\) 计算并制备初始状态 \(|\psi(0)\rangle = |\rho_0 \oslash \eta(T)\rangle\)。
        *   **Step 3**：再次使用 Schrödingerization 求解热方程 (2.6)，得到状态 \(|\psi(T)\rangle\)。
        *   **Step 4**：利用 Hadamard 乘法电路，从 \(|\psi(T)\rangle\) 和 \(|\eta_0\rangle\) 计算并输出最终状态 \(|\rho(T)\rangle = |\psi(T) \odot \eta_0\rangle\)。

#### 3. 实验设计

- **使用场景与数据**：采用论文 [LLO23] 中的标准示例（Example A）。
    - 势能函数：\(V(x) = e^{-|x+0.25|^2 / 0.5}\)。
    - 初始密度：\(\rho_0(x) = \frac{1}{\sigma_0 \sqrt{2\pi}} e^{-|x-0.25|^2 / (2\sigma_0^2)}\)，\(\sigma_0=0.1\)。
    - 未提及使用特定的真实世界数据集。
- **Benchmark与对比方法**：
    - **对比方法**：核心是与 [LLO23] 论文中提出的**核积分公式**（kernel integral formula）的经典方法结果进行比较。经典 Fourier 谱方法的复杂度也被列出作为对比基线。
    - **Benchmark**：展示不同参数（\(T, \beta\)）下的计算结果，并验证其与核方法的一致性。

#### 4. 资源与算力

- **未明确说明**：论文是理论分析和数值模拟研究，并未提及使用多少 GPU 型号、数量或训练时长。模拟是在经典计算机上运行量子算法的数值模拟。对于量子算法的潜在大规模实现，其资源需求（如量子比特数、门深度）在理论上被估计，但未进行实际硬件实验。

#### 5. 实验数量与充分性

- **实验分组**：
    1.  **一维（1D）实验**：展示了在 \(T=0.1, 0.2, 0.5\) 固定 \(\beta=0.25\)，以及 \(\beta=0.125, 0.25, 0.5\) 固定 \(T=0.2\) 下的结果曲线。
    2.  **二维（2D）实验**：展示了 \(T=0.2, \beta=0.25\) 下的结果曲面。
- **充分性评价**：
    - **优点**：实验验证了算法在低维度下的正确性，并显示了参数变化对输出的影响，与经典核方法的结果一致，证明了方法的有效性。
    - **不足**：
        *   **维度不够高**：实验仅在 1D 和 2D 进行，未能直接展示在高维（如 d=10, 100）下的表现，而这是论文声称有指数加速的最关键场景。
        *   **缺乏对比**：没有与更经典的 WPO 求解器（如直接求解 JKO 方案或使用更高效经典算法的求解器）进行性能对比。
        *   **无消融实验**：没有对算法的各个组成部分（如 Schrödingerization 的准确性、Hadamard 除法的精度、不同网格大小等）进行独立的消融研究。
        *   **无统计稳定性分析**：未报告多次独立运行的结果以评估算法的随机性。

#### 6. 主要结论与发现

- **核心结论**：成功开发了首个用于正则化 Wasserstein 近端算子的高效量子算法。
- **关键发现**：
    1.  通过 Cole-Hopf 变换和 Schrödingerization 方法，可以将复杂的耦合 PDE 问题转化为可量子模拟的线性热方程求解问题。
    2.  设计的 Huadamard 除法和乘法量子电路能够可靠地处理状态之间的耦合。
    3.  通过理论分析（利用缩放不变性），证明了算法最终的查询复杂度为 \(O(d N_x T \log^2(1/\varepsilon))\)，**仅与空间维度 \(d\) 和每维网格点数 \(N_x\) 线性相关**，相对于经典方法的 \(O(N_x^d)\) 实现了**指数级加速**。
    4.  低维数值实验验证了所得解的正确性。

#### 7. 优点

- **方法创新性强**：巧妙地结合了 Cole-Hopf 变换、Schrödingerization 方法和量子矩阵运算（Hadamard product/division），为解决此类最优输运问题提供了一个全新的、系统的量子计算框架。
- **理论分析严谨深入**：不仅给出了算法设计，还对关键的归一化系数、条件数等进行了深入分析（Lemma 3.2），并通过引入“最小化器不变性缩放”（minimizer-invariance scaling）成功地消除了对正则化参数 \(\beta\) 的指数依赖，保证了最终复杂度界限的紧致性。
- **解决实际挑战**：精准地识别并解决了量子实现中的两个核心挑战——点除和点乘操作，并给出了具体的、基于块的编码和量子线性系统的解决方案。
- **加速效果明确**：清晰阐明了量子算法相对于经典方法在维度上的指数级加速，具有重要的理论价值和潜在的应用前景。

#### 8. 不足与局限

- **数值实验局限**：
    *   **维度验证不足**：如前述，实验结果仅覆盖低维，未提供高维（d>2）下的数值证据或仿真，难以完全支撑“指数加速”的声称。
    *   **缺乏实用性对比**：未与任何可实际运行的经典或量子基线（如当前最先进的经典 WPO 求解器）进行端到端的性能对比（如计算时间、精度）。
    *   **泛化性未验证**：仅测试了单个示例（Example A），其势能和初始密度形状简单。对于具有复杂几何或非光滑势能的更一般问题，算法的鲁棒性和有效性未知。
- **方法适用性局限**：
    *   **线性假设**：算法目前局限于**线性**能量泛函 \(F(q) = \int V q dx\)。对于非线性泛函（如熵、相互作用能），Cole-Hopf 变换不再适用，算法需要重大修改。
    *   **边界条件依赖**：算法假设了周期性边界条件，虽然提到可扩展，但未进行实验验证。在更通用的边界条件下，算法的实现复杂度和效率可能会受影响。
    *   **量子资源开销**：虽然查询复杂度是线性的，但实际量子电路中的量子比特数、门深度和错误率等资源开销对于大规模问题是否可行，论文仅进行了理论估计，未给出具体的可执行门级线路。
- **偏见与风险**：论文的落点是“指数级加速”，但这一结论建立在理论复杂度分析上。实际实验中低维成功并不能完全排除高维下因常数因子过大或量子算法实现难度过高而导致的优势丧失风险。

（完）
