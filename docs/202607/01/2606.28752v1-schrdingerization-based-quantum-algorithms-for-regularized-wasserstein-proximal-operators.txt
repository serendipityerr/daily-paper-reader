Title: Schrödingerization based quantum algorithms for regularized Wasserstein proximal operators

URL Source: https://arxiv.org/pdf/2606.28752v1

Published Time: Tue, 30 Jun 2026 00:26:54 GMT

Number of Pages: 23

Markdown Content:
# Schr¨ odingerization based quantum algorithms for regularized Wasserstein proximal operators 

Shi Jin ∗1, Nana Liu †1, 2 , and Yue Yu ‡3,4,5 1School of Mathematical Sciences, Institute of Natural Sciences, MOE-LSC, Shanghai Jiao Tong University, Shanghai, 200240, China 

> 2

Global College, Shanghai Jiao Tong University, Shanghai 200240, China 

> 3

School of Mathematics and Computational Science, Xiangtan University, Xiangtan, Hunan 411105, China 

> 4

Hunan Research Center of the Basic Discipline Fundamental Algorithmic Theory and Novel Computational Methods, Xiangtan, Hunan 411105, China 

> 5

National Center for Applied Mathematics in Hunan, Xiangtan, Hunan 411105, China June 30, 2026 

Abstract 

We develop a quantum algorithm for the regularized Wasserstein proximal operator, which is a fundamental tool in optimal transport and mean-field games. The regularization intro-duces a small diffusive term into the continuity equation of the Benamou-Brenier formulation, which results in a forward-backward PDE system consisting of a Fokker-Planck equation and a viscous Hamilton-Jacobi equation with a quadratic Hamiltonian. Through the Cole-Hopf transformation, both equations are converted to forward heat equations, whose coupling re-quires a Hadamard division to prepare the initial data for the second heat equation and a Hadamard product to recover the terminal density. We solve these heat equations via the Schr¨ odingerization method and implement the Hadamard division and product operations using simple matrix-vector multiplication representations. The complete quantum algorithm prepares an ε-approximation of the terminal density state with O(dN xT log 2(1 /ε )) query complexity, up to constants depending on the potential and initial density, where d is the spatial dimension, 

Nx is the number of grid points per spatial dimension and T is the evolution time. The com-plexity depends only linearly on dN x, yielding an exponential speedup over classical methods, whose cost scales as N dx per time step. Numerical experiments validate the effectiveness of the proposed algorithm. 

> ∗

shijin-m@sjtu.edu.cn 

> †

nana.liu@quantumlah.org 

> ‡

terenceyuyue@xtu.edu.cn 

1

> arXiv:2606.28752v1 [math.NA] 27 Jun 2026

# 1 Introduction 

The Wasserstein proximal operator (WPO) naturally models how a distribution evolves under the influence of an energy landscape while strictly respecting the geometry of optimal transport. It serves as a powerful engine for sampling, optimization, special applications in mean-field games, and generative modeling [Vil09, GKRBZ24, HOL25b, HOL25a]. In statistics and machine learning, a major goal is to draw samples from a complex target probability distribution p(x) ∝ e−V (x), where V (x) is an unnormalized potential energy. The celebrated JKO scheme [JKO98] formulates the classical diffusion and Fokker-Planck equation as a gradient flow of the Boltzmann entropy in the Wasserstein space, where each step is realized by solving a WPO problem. Similar problems also arise in potential (or variational) mean-field games [LL07, HMC06], where the agent running cost derives from a global population functional 

F(ρ) (interaction energy, congestion, entropy etc.). Standard mean-field game equilibrium can be obtained by solving coupled Hamilton-Jacobi-Bellman system, in which the WPO is also the time discretization or the JKO scheme of the corresponding Wasserstein gradient flow. In generative AI, modern models such as Stable Diffusion and Sora transport a simple noise distribution through a learned velocity field to generate realistic data. Recent work [ZLL +24] establishes a fundamental connection between score-based generative models and WPOs, showing that the forward-backward structure of the Wasserstein proximal system is mathematically identical to the reverse-time stochastic differential equations used in diffusion models [ZLL +24, LLOM21]. In stochastic optimal control, the classical Schr¨ odinger bridge problem (SBP) [CH22] seeks to steer the joint state probability density function between two prescribed endpoint distributions while minimizing the expected control effort subject to controlled diffusion. Its variational formu-lation shares the same dynamic optimal transport structure as the Benamou-Brenier formulation, but with the continuity equation replaced by a Fokker-Planck equation, where the diffusion coef-ficient serves as the diffusive regularization. The regularized WPO [LLO23] further extends this framework by incorporating an additional terminal cost functional, thereby generalizing the SBP to a broader class of density control problems. In [BB00], Benamou and Brenier provided a fluid dynamical formulation for the Monge-Kantorovich mass transfer problem, whose solution can be obtained by solving a coupled continuity equation and a Hamilton-Jacobi equation with a terminal condition for the density, which needs to be solved iteratively. For the WPO, however, only their decoupled system needs to be solved. The regularized WPO, introduced in [LLO23], adds a diffusion regularization to the continuity equation, yielding a forward-backward PDE system consisting of a Fokker-Planck equation and a viscous Hamilton-Jacobi equation with a quadratic Hamiltonian. This enables the application of the classical Cole-Hopf transformation that equivalently transfers the system into two linear heat equations, one forward and one backward in time. However, when the spatial dimension is high, classical computation becomes daunting, which motivates our development of quantum algorithms. Our quantum algorithms are based on the viscously regularized WPOs introduced in [LLO23], which are combined with the Schr¨ odingerization method [JLY23,JLY24,JL24,JLM +26]. The latter is a simple and general methodology to unitarize general linear ordinary and partial differential 2equations into unitary evolution processes, thus making them suitable for quantum simulation. The overall quantum algorithm proceeds as follows. 

• First, through the Cole-Hopf transformation, the forward-backward PDE system is reduced to two forward heat equations of the form ∂tu = β∆u, where u represents either η in (2.5) or 

ψ in (2.6). We solve these heat equations using the Schr¨ odingerization method, whose query complexity for preparing an ε-approximation of the normalized state |u(T )⟩ is 

O

 ∥u(0) ∥∥u(T )∥ dN xβT log ∥u(0) ∥

ε∥u(T )∥



,

where Nx is the number of grid points per spatial dimension, d is the spatial dimension, T is the stopping time, β is the artificial viscosity coefficient, and ε is the precision. 

• Second, to couple the two heat equations, we implement a quantum Hadamard division to prepare the initial data for the second heat equation from the output of the first. 

• Third, we recover the terminal density via a quantum Hadamard product. The overall query complexity contains, in addition to the dN xβT factor from the heat equation solver, several normalization factors and a condition number κη(T ) = max j |η(T, x j )|/ min j |η(T, x j )|

from the Hadamard division and product steps. We carefully analyze these quantities and show that, under the assumption 0 ≤ V (x) ≤ V ∗, they are bounded by constants depending only on the range of the potential: 

∥η0∥∥η(T )∥ ≲ eV ∗/(2 β), κη(T ) ≲ eV ∗/(2 β), gprod · ∥ψ0∥∥ψ(T )∥ ≲ e3V ∗/(2 β)∥ρ0∥L2 ,

where gprod is the Hadamard product normalization factor. These estimates naively introduce expo-nential factors in β−1. However, by exploiting the minimizer-invariance scaling V 7 → V /G, T 7 → T G 

with G = 1 /β , which leaves the WPO unchanged, we remove this β-dependence entirely. Under this scaling, the complete quantum algorithm prepares an ε-approximation of the normalized terminal density state |ρ(T )⟩ with Ω(1) success probability, using 

O



e3V ∗/2∥ρ0∥L2 dN xT log 2 1

ε



queries to the block-encoding of the discrete Laplacian, and 

O(e 3V ∗/2∥ρ0∥L2 log 1

ε



queries to the state preparation oracles Uρ0 and Uη0 . Crucially, the complexity depends only linearly 

on d and Nx.For comparison, a classical Fourier spectral method for the same discretization requires 

O(N dx log Nx) operations per time step for each heat equation, with additional O(N dx ) operations for the Hadamard division and product. This classical cost exhibits an exponential dependence on the spatial dimension d through N dx . In contrast, the quantum complexity is linear in d and Nx,offering an exponential advantage for high-dimensional regularized WPOs. 3The remainder of this paper is organized as follows. In Section 2, we formulate the problem and provide an overview of our algorithm, highlighting the reduction to forward heat equations via Cole-Hopf transformations. Section 3 presents the quantum algorithm for the regularized WPO via Schr¨ odingerization, which consists of three parts: quantum Hadamard product and division protocols, the Schr¨ odingerization-based heat equation solver, and the assembly of these components into the complete algorithm along with its complexity analysis. Numerical experiment is provided in Section 4, and concluding remarks and outlooks are given in Section 5. 

# 2 Problem description and algorithm overview 

Proximal operators are fundamental tools in optimization. Their counterpart in the Wasserstein-2 space, known as the WPO, has proven to be highly effective in various applica-tions, including scientific computing and machine learning. In this section, we review the approach proposed in [LLO23] for approximating this operator. 

2.1 The regularized WPO 

To make this paper accessible to readers from quantum computation and other fields, we begin by introducing the WPO in intuitive terms, before presenting its mathematical formulation and the regularization that enables our quantum algorithm. 

Euclidean proximal operators. In classical optimization, a proximal operator is a simple but powerful tool, which takes a point and move it a small step in the direction that decreases the energy while staying close to the original point. Specifically, given a current point xk and an energy function F , the proximal step finds a new point that balances two competing objectives: staying close to xk, and decreasing F . This is achieved by solving 

xk+1 = arg min 

> x

n 12τ ∥x − xk∥2 + F (x)

o

,

where the first term penalizes moving too far from the current point, and the second term encourages lowering the energy. The parameter τ controls the step size. This idea is the foundation of many optimization algorithms, including gradient descent and the proximal gradient method. When working with probability distributions, it is often useful to describe how a distribution evolves over time under the influence of some energy or objective functional. The WPO extends the idea of proximal operators from points to probability distributions, and from Euclidean distance to the Wasserstein distance from optimal transport. 

Wasserstein distance. We now replace the Euclidean distance ∥ · ∥ by the Wasserstein dis-tance W(·, ·) from optimal transport, which measures the minimal cost of moving mass from one distribution to another. Formally, for two probability distributions ρ and q on Rd, the squared Wasserstein-2 distance is defined as 

W(ρ, q )2 = inf 

> π∈Π( ρ,q )

Z

> Rd×Rd

∥x − y∥2 dπ(x, y ),

4where Π( ρ, q ) denotes the set of all joint distributions on Rd × Rd whose marginals are ρ and q.Intuitively, π(x, y ) specifies how much mass from location x in the initial distribution is transported to location y in the target distribution, and the integral sums the squared distance of each such movement. The Wasserstein distance has found widespread applications across mathematics, statistics, and engineering [Vil09]. It is used to study optimal approximation of measures by finite point sets, to compare color distributions in image processing, and to analyze mixing and convergence of Markov chains. In statistical mechanics, it plays a central role in the theory of propagation of chaos and the mean-field behavior of large particle systems. One of its key advantages is that it behaves well in high-dimensional settings, making it particularly suitable for problems in infinite-dimensional spaces, such as the long-time behavior of stochastic partial differential equations and the hydrodynamic limits of interacting particle systems. In Riemannian geometry, the Wasserstein-2 distance is closely tied to the study of Ricci curvature through its connection to diffusion equations. 

Benamou-Brenier formulation of the Wasserstein distance. The Wasserstein-2 distance admits a dynamic reformulation as an optimal control problem, known as the Benamou-Brenier formulation [BB00]: 

W(ρ0, q )2

2T = inf ρ,v ρ(0)= ρ0, ρ (T )= q

Z T

> 0

Z

> Rd

12 ∥v(t, x )∥2ρ(t, x )d xdt, 

where the infimum is taken over density functions ρ : [0 , T ] × Rd → R+ and vector fields v :[0 , T ] × Rd → Rd satisfying the continuity equation 

∂tρ(t, x ) + ∇ ·  ρ(t, x )v(t, x ) = 0 ,

with 

ρ(0 , x ) = ρ0(x), ρ(T, x ) = q(x).

A crucial distinction for the WPO from the general optimal transport problem is that the terminal density q here is not prescribed in advance; rather, it is itself a distribution to be opti-mized. This seemingly subtle difference has profound consequences: as we shall see, it decouples the Hamilton-Jacobi equation from the continuity equation in the optimality system, ultimately allowing both equations to be linearized via the Cole-Hopf transformation when viscosously regu-larized. 

Wasserstein proximal operators. Suppose we want to update not a point, but a probability distribution ρk — a cloud of mass representing, for example, the location of a population, the state of a physical system, or the distribution of data in machine learning. We would like to decrease some energy functional F(ρ) — such as entropy, potential energy, or congestion cost — while ensuring that the new distribution does not stray too far from the original one. This leads to the Wasserstein proximal operator: 

ρk+1 = arg min 

> ρ

n 12τ W2(ρ, ρ k) + F(ρ)

o

.

5This operator performs a gradient-descent step in the space of probability distributions, where distance is measured by optimal transport. In other words, the next distribution is the best compromise between staying close to the current distribution (measured by optimal transport cost) and reducing the energy. This construction, introduced in the seminal JKO scheme [JKO98], provides a natural time discretization of Wasserstein gradient flows and plays a central role in optimal transport, diffusion equations, and mean field game models. 

The regularized Wasserstein proximal operator. In this work, we focus on a specific case where the energy functional is linear: 

F(q) = 

Z

> Rd

V (x)q(x)d x, 

where V ∈ C1(Rd) is a given potential function. For this choice, the Wasserstein proximal operator is defined as 

ρT := WProx T, F (ρ0) := arg min 

> q∈P 2(Rd)

F(q) + W(ρ0, q )2

2T , (2.1) where W(ρ0, q ) is the Wasserstein-2 distance between ρ0 and q, and P2(Rd) is the probability density set with finite second moment. The parameter T controls the trade-off between transport cost and energy reduction and can be interpreted as the time horizon over which the transport occurs. Following [LLO23], we consider a regularized version of this operator, where a diffusion term is added to the dynamics. The regularization serves two important purposes: it makes the problem well-posed and numerically stable, and — crucially for our purposes — it allows the optimality conditions to be decoupled and linearized. The regularized WPO is defined by the mean-field control problem 

ρT = WProx T, F,β (ρ0) := arg min 

> ρ,ν,q

Z T

> 0

Z

> Rd

12 ∥v(t, x )∥2ρ(t, x )d xdt +

Z

> Rd

V (x)q(x)d x, 

subject to the Fokker-Planck equation 

∂tρ(t, x ) + ∇ · (ρ(t, x )v(t, x )) = β∆ρ(t, x ), ρ(0 , x ) = ρ0(x), ρ(T, x ) = q(x),

where β > 0 is the small regularization parameter–the viscosity coefficient. This regularization is critical, since it allows the use of the Cole-Hopf transformation, as will be seen later. 

Optimality conditions and the forward-backward system. To derive the optimality con-ditions, we introduce a Lagrange multiplier Φ( t, x ) to enforce the Fokker-Planck constraint. The Lagrangian is 

L(ρ, v, Φ) = 

Z T

> 0

Z

> Rd

 12 ∥v∥2ρ + Φ 



∂tρ + ∇ · (ρv ) − β∆ρ

 

dx dt +

Z

> Rd

V (x)ρ(T, x ) d x. 

Taking variations with respect to ρ and v yields the optimality conditions (see Proposition 2 of [LLO23]). In particular, variation with respect to v gives v = ∇Φ: the optimal velocity is the 6gradient of the Lagrange multiplier. Substituting this into the associated Euler-Lagrange equations gives the forward-backward PDE system 

∂tρ + ∇ · (ρ∇Φ) = β∆ρ, (2.2) 

∂tΦ + 12 |∇ Φ|2 = −β∆Φ , (2.3) with 

ρ(0 , x ) = ρ0(x), Φ( T, x ) = −V (x).

According to Proposition 2 of [LLO23], 

ρT := WProx T, F,β (ρ0) = ρ(T, x ).

The structure of this system is special: the Hamilton-Jacobi equation (2.3) is a backward equation with a known terminal condition Φ( T, x ) = −V (x). Because it does not depend on ρ, it can be solved independently (by reversing time). Once Φ is known, the Fokker-Planck equation (2.2) becomes a linear equation for ρ that can be solved forward in time from the initial condition 

ρ0. This decoupling is the key that makes the problem amenable for exact linearization by the Cole-Hopf transformation, which we present in the next section. 

2.2 The heat equation form via Cole-Hopf transformation 

The second equation (2.3) is a backward Hamilton-Jacobi equation with a terminal condition. By setting Sβ (t, x ) = −Φ( T − t, x ), one obtains an initial value problem for Sβ :

∂tSβ + 12 |∇ Sβ |2 = β∆Sβ , Sβ (0 , x ) = V (x), (2.4) which is precisely the viscous Hamilton-Jacobi equation with a quadratic Hamiltonian. It is simple to check that the Cole-Hopf transformation 

η(t, x ) = e −Sβ (t,x )/(2 β)

converts (2.4) into the heat equation 

∂tη = β∆η, η(0 , x ) = e −V (x)/(2 β) =: η0(x). (2.5) The first equation (2.2) is a Fokker-Planck equation. By using the Cole-Hopf type transfor-mation ψ(t, x ) = ρ(t, x )e −Φ( t,x )/(2 β), one readily verifies that ψ also satisfies the heat equation 

∂tψ = β∆ψ, (2.6) with initial condition 

ψ(0 , x ) = ρ0(x)e −Φ(0 ,x )/(2 β) = ρ0(x)e Sβ (T,x )/(2 β) = ρ0(x)

η(T, x ) .

Therefore, one can first solve (2.5) to obtain η(T, x ), then solve (2.6) to obtain ψ(T, x ), and finally recover the terminal density via 

ρ(T, x ) = ψ(T, x )e −V (x)/(2 β) = ψ(T, x )η0(x).

7Remark 2.1. Let {xj }Nj=1 be a set of discrete points. Define the vectors 

ρ0 :=  ρ0(xj )Nj=1 , η(T ) :=  η(T, x j )Nj=1 , ψ(T ) :=  ψ(T, x j )Nj=1 , η0 :=  η0(xj )Nj=1 .

For the quantum implementation, the difficulty lies in the computation of the componentwise (Hadamard) division ρ0 ⊘ η(T ) and the componentwise (Hadamard) product ψ(T ) ⊙ η0, i.e., the vectors whose j-th components are ρ0(xj )/η (T, x j ) and ψ(T, x j ) η0(xj ), respectively. 

Remark 2.2. We assume that the density function ρ has compact support or nearly vanishes near the boundary of a sufficiently large finite spatial domain, so that the periodic boundary condition can be imposed for computational convenience without introducing significant boundary effects. The algorithm can be readily extended to other boundary conditions as well. 

# 3 Quantum algorithm for regularized Wasserstein proximal oper-ators via Schr¨ odingerization 

The heat equations (2.5) and (2.6) can be solved via the Schr¨ odingerization approach. The overall procedure requires quantum Hadamard division and product operations, which are used to prepare the initial data for coupling the two heat equations and to reconstruct the terminal density state. 

3.1 Quantum Hadamard product and division 

Block encoding is a general input model for matrix operations on a quantum com-puter [GSLW19, CGJ19, Lin22], with the definition given below. 

Definition 3.1. Suppose that A is an n-qubit matrix and let Π = ⟨0m| ⊗ I with I being an n-qubit identity matrix. If there exist positive numbers α and ε, and a unitary matrix UA of (m + n)-qubits, such that 

∥A − αΠUAΠ†∥ = ∥A − α(⟨0m| ⊗ I)UA(|0m⟩ ⊗ I)∥ ≤ ε, 

then UA is called an (α, m, ε ) block-encoding of A.

Our construction relies on the diagonal block encoding of amplitudes described below. 

Lemma 3.1 (Theorem 2 of [RR23]) . Let Uψ be an n-qubit unitary that prepares the quantum state |ψ⟩n = Uψ|0⟩n = PN −1 

> j=0

ψj |j⟩n with complex amplitudes ψj . Then one can construct a 

(1 , n + 3 , 0) -block-encoding UDψ for the diagonal operator Dψ = diag (ψ0, ψ 1, · · · , ψ N −1). The con-struction requires circuit depth O(n) in terms of single and two qubit gates and makes O(1) calls to a controlled-Uψ gate. 

From the explicit construction of the diagonal block encoding in Theorem 2 of [RR23], we observe that the entire circuit can be implemented using only single- and two-qubit gates. In particular, the controlled-copy circuit C is realized with n Toffoli gates, each decomposable into a constant number of elementary gates. All other components (e.g., Wp, Gp, ˆZ, ˆH, ˆS) contribute at 8most O(n) elementary gates. Consequently, the total number of elementary gates is O(n), and the circuit depth is also O(n) under parallel execution. Let 

a = [ a0, a 1, · · · , a N −1]⊤, b = [ b0, b 1, · · · , b N −1]⊤ ∈ CN ,

and denote their corresponding quantum states as 

|a⟩ = 1

∥a∥ 

> N−1

X

> j=0

aj |j⟩, |b⟩ = 1

∥b∥ 

> N−1

X

> j=0

bj |j⟩.

We assume that the state preparation unitaries Ua and Ub satisfying Ua|0n⟩ = |a⟩ and Ub|0n⟩ = |b⟩

are given. Our construction for the Hadamard product and division is based on the following observation: 

• Hadamard product. Observe that the componentwise product can be written as 

a ⊙ b = Dab, Da := diag( a0, a 1, · · · , a N −1).

Thus, up to normalization, the quantum state encoding a ⊙ b is given by Da|b⟩.

• Hadamard division. Similarly, the componentwise quotient (assuming bj̸ = 0 for all j) is 

a ⊘ b = D−1 

> b

a, (3.1) i.e., D−1 

> b

|a⟩ up to normalization. Hence the task reduces to inverting the diagonal operator 

Db.

Theorem 3.1 (Quantum Hadamard product and division) . Let Ua and Ub be n-qubit state prepa-ration unitaries for the vectors a, b ∈ CN with N = 2 n, as defined above. Then: (i) Hadamard product. There exists a quantum circuit that produces the normalized state encoding a ⊙ b. The circuit uses O(gprod ) calls to controlled-Ua and Ub and has circuit depth 

O(ng prod ) in terms of single- and two-qubit gates, where 

gprod = ∥a∥∥ b∥∥a ⊙ b∥ .

(ii) Hadamard division. For any ϵ > 0, there exists a quantum circuit that produces a state 

ϵ-close (in ℓ2-norm) to the normalized state encoding a ⊘ b. The circuit requires O(gdiv ) calls to controlled-Ub and Ua, and its circuit depth is O(ng div ), where 

gdiv = κb log 1

ϵ , κb = max j |bj |

min j |bj | .

Proof. Part (i). By Lemma 3.1, from the normalized state |a⟩ = Ua|0n⟩ we can construct a (1 , n + 3 , 0)-block-encoding UDa of the diagonal matrix D|a⟩ = diag( a0/∥a∥, · · · , a N −1/∥a∥). By definition, UDa is also a ( ∥a∥, n + 3 , 0)-block-encoding of Da = diag( a0, · · · , a N −1). Prepare the initial state |0⟩⊗(n+3) ⊗ | b⟩ by Ub. Applying UDa yields 

UDa

 |0⟩⊗(n+3) ⊗ | b⟩ = 1

∥a∥ |0⟩⊗(n+3) ⊗ Da|b⟩ + |Φ⊥⟩,

9where |Φ⊥⟩ is orthogonal to the subspace where the first n + 3 qubits are in |0⟩⊗(n+3) . Substituting 

|b⟩ = b/∥b∥, we obtain 

UDa

 |0⟩⊗(n+3) ⊗ | b⟩ = 1

∥a∥∥ b∥ |0⟩⊗(n+3) ⊗ Dab + |Φ⊥⟩.

Measuring the ancillary register and post-selecting on the all-zero outcome collapses the system to 

|Dab⟩ = |a ⊙ b⟩. The success probability is Pprod =

 ∥Dab∥∥a∥∥ b∥

2

=

 ∥a ⊙ b∥∥a∥∥ b∥

2

.

The circuit depth and query complexity follow directly from Lemma 3.1 and the amplitude ampli-fication. 

Part (ii). Following the same procedure as in part (i), we can construct a ( ∥b∥, n +3 , 0)-block-encoding UDb of Db = diag( b0, · · · , b N −1). According to Theorem 12 of [CAS +22], there exists a quantum linear systems algorithm (QLSA) that produces the normalized state |D−1 

> b

a⟩ = |a ⊘ b⟩

within an error ϵ, using O(κb log 1 

> ϵ

) calls to the oracles UDb and Ua, where κb = max j |bj |   

> min j|bj|

is the condition number of Db. By Lemma 3.1, the query complexity to controlled-Ub is O(κb log(1 /ϵ )), and the circuit depth is multiplied by the same factor because each QSVT step calls the block-encoding once. This completes the proof. 

Remark 3.1. While quantum circuits for the Hadamard product using CNOT gates and post-selection exist [RNF +23], with complexity comparable to ours (cf. Proposition 1 of [HNZM26]), the Hadamard division counterpart appears to be absent in the literature. 

3.2 Schr¨ odingerization based quantum algorithm for the heat equation 

According to the previous discussion, the problem reduces to solving the heat equation 

∂tu(t, x ) = β∆u(t, x ), u(0 , x ) = u0(x) (3.2) on the spatial domain Ω = [ a, b ]d, where a, b, β > 0 are constants. The periodic boundary conditions are imposed in what follows. Our goal is to solve the regularized Wasserstein proximal problem, where β corresponds to the regularization parameter and u is either η or ψ (see (2.5) and (2.6)). The spatial discretization is performed using the Fourier spectral method, which leads to a system of linear ODEs that can be solved via the Schr¨ odingerization approach. We consider a d-dimensional domain [ a, b ]d with a uniform spatial step ∆ x = ( b−a)/N x, where 

Nx = 2 Mx is an even integer. The grid points are defined as 

xj = ( xj1 , · · · , x jd ), xji = a + ji∆x, ji = 0 , 1, · · · , N x − 1, i = 1 , · · · , d. 

For a spatial coordinate x = ( x1, · · · , x d) ∈ [a, b ]d, we adopt the Fourier basis functions 

ϕl(x) = 

> d

Y

> i=1

ϕli (xi), ϕli (xi) = e iνli (xi−a), νli = 2π(li − Mx)

b − a , li = 0 , 1, · · · , N x − 1,

10 where l = ( l1, · · · , l d) and 0 ≤ l ≤ Nx − 1 (meaning each component satisfies 0 ≤ li ≤ Nx − 1). The spectral approximation of the solution is expressed as 

u(t, x ) = X

> l

cl(t)ϕl(x), x = xj , 0 ≤ j ≤ Nx − 1, (3.3) where the coefficients cl(t) are determined by the collocation values at the grid points. Periodic boundary conditions are automatically satisfied by this expansion. The grid values are organized into a column vector as u(t) = P 

> j

u(t, x j )|j⟩, where |j⟩ = |j1⟩ ⊗ · · · ⊗ | jd⟩. The vector of spectral coefficients is similarly given by c(t) = P 

> l

cl(t)|l⟩. The transformation between the physical and spectral representations is u = Φ ⊗dc, where Φ = ( ϕjl )Nx×Nx , with ϕjl = ϕl(xj ), is the matrix representation of the discrete Fourier transform. The semi-discretization of (3.2) reads: du(t)dt = β(P 21 + · · · + P 2 

> d

)u(t), u(0) = u0,

where Pl = I⊗(l−1) ⊗ Pν ⊗ I⊗(d−l), and Pν = Φ Dν Φ−1, with Dν = diag( ν0, ν 1, · · · , ν Nx−1), is the matrix representation of the momentum operator ˆ p = −i ∂∂x . Let c(t) = F −1 

> x

u(t), where Fx = Φ ⊗d.The ODEs can be rewritten as dc(t)dt = Ac(t), c(0) = F −1 

> x

u0, (3.4) where 

A = β



(Dν 

> 1

)2 + · · · + ( Dνd )2

, Dνl = I⊗(l−1) ⊗ Dν ⊗ I⊗(d−l).

For convenience, we set Nx = 2 nx . The matrix A is diagonal and can be constructed from the block-encoding of Dν by using the LCU procedure. According to [GSLW19, CGJ19, Lin22], we can construct an ( αA, m A, 0)-block-encoding of A, with αA = dβν max , mA = O(dn x) and 

νmax = max l |νl|.The ODE system (3.4) can be solved via the Schr¨ odingerization approach [JLY23, JLY24, JLM +26], which we briefly review below. Applying the warped phase transformation w(t, p ) = e−pc(t) for p ≥ 0, with the equation extended naturally to p < 0, yields 

∂∂t w(t) = −A∂ pw. (3.5) The initial data for (3.5) is given by w(0 , p ) = ψ(p)c(0), where ψ(p) ∈ Hr(R) with r ≥ 1 is chosen such that ψ(p) = e −p for p ≥ 0 and decays sufficiently fast as p → −∞ . A simple choice is 

ψ(p) = e −| p| ∈ H1(R). A construction of ψ with higher regularity is provided in [JLM25]. More recently, in [JLM +26], we presented a construction that achieves near-optimal query complexity using cut-off function and error function techniques. For numerical implementation, we truncate the auxiliary variable p to a finite interval [ −R, R ] with R > 0 satisfying the conditions given therein. We then choose a uniform mesh size ∆ p = 2 R/N p, where Np = 2 np is an even number, and denote the grid points by −R = p0 < p 1 < · · · < p Np = R. The circuit for implementing the quantum simulation of the Schr¨ odingerization method in [JLM +26] is shown in Fig. 1, where 

ψ = P 

> k∈[Np]

ψ(pk)|k⟩. The evolution operator U(T ) can be expressed as a select oracle 

U(T ) = 

> Np−1

X

> k=0

|k⟩⟨ k| ⊗ e−iμk AT =: 

> Np−1

X

> k=0

|k⟩⟨ k| ⊗ U k(T ),

11 where each unitary Uk(T ) corresponds to the simulation of the Hamiltonian Hμk := μkA, with 

μk = πR (k − Np 

> 2

) being the Fourier modes. 

|ψ⟩ IQFT 

U(T )QFT |k∗⟩|c(0) ⟩ |c(T )⟩

Fig. 1: Quantum circuit for the Schr¨ odingerization approach in [JLM +26]. Following the similar discussion in [JLM +26], we can conclude the following lemma. 

Theorem 3.2. Let ε > 0 be the precision. Then there exists a quantum algorithm that prepares an ε-approximation of the state |u(T )⟩ with Ω(1) success probability and a flag indicating success, using 

O

 ∥u(0) ∥∥u(T )∥ dN xβT log ∥u(0) ∥

ε∥u(T )∥



queries to the block-encoding oracle of A.Proof. According to Theorem 2.2 of [JLM +26], there exists a quantum algorithm that prepares an ε-approximation of the state |c(T )⟩ with Ω(1) success probability and a flag indicating success, using 

O

 ∥c(0) ∥∥c(T )∥ αAT log ∥c(0) ∥

ε∥c(T )∥



queries to the block-encoding oracle of A. The proof is concluded by noting that αA = dβν max =

O(dβN x) and ∥c(t)∥ = ∥u(t)∥.

3.3 Quantum algorithm for the regularized Wasserstein proximal operator 

3.3.1 Assembly of the quantum subroutines 

We now assemble the quantum algorithm for the regularized WPO using the heat equation solver (Theorem 3.2) and the Hadamard product/division protocols (Theorem 3.1). The overall procedure consists of three main steps: 1. Solve the heat equation (2.5) to obtain η(T, x ). 2. Solve the heat equation (2.6) to obtain ψ(T, x ). 3. Combine the results to recover the terminal density ρ(T, x ) = ψ(T, x )η0(x). As noted in Remark 2.1, a difficulty arises in the quantum implementation of Step 2, where the ini-tial condition for ψ requires the componentwise division ρ0(x)/η (T, x ). This operation is performed using the quantum Hadamard division protocol developed in Theorem 3.1. Before proceeding, we specify the required quantum oracles: 

• State preparation oracles. – Uρ0 : prepares the normalized initial density state |ρ0⟩ = P 

> j

ρ0(xj )|j⟩/∥ρ0∥.12 – Uη0 : prepares the state encoding e −V (x)/(2 β), i.e., |η0⟩ = P 

> j

e−V (xj )/(2 β)|j⟩/∥η0∥.

• Block-encoding oracle. – UA: an ( αA, m A, 0)-block-encoding of the diagonal matrix A = β Pdl=1 (Dνl )2, which rep-resents the discrete Laplacian in the Fourier basis. The construction of UA follows from the block-encoding of Dν via standard linear combination of unitaries (LCU) techniques. We assume these oracles are given and can be queried. The query complexity counts calls to these oracles. 

Step 1: Solving for η(T, x ). Using Uη0 , we prepare |η0⟩. Applying the heat equation solver (Theorem 3.2) with the block-encoding UA, we obtain a state |η(T )⟩ that is ε-close to the normalized exact state |ηexact (T )⟩, with success probability Ω(1). The associated unitary that prepares |η(T )⟩

is denoted as Uη(T ). The cost is 

O

 ∥η0∥∥η(T )∥ dN xβT log ∥η0∥

ε∥η(T )∥



queries to UA and O(∥η0∥/∥η(T )∥) queries to Uη0 .

Step 2: Solving for ψ(T, x ). The initial condition for ψ is ψ(0 , x ) = ρ0(x)/η (T, x ). Using the Hadamard division circuit (Theorem 3.1(ii)), we prepare a state |ψ0⟩ that is ε-close to the normalized exact initial state |ψexact (0) ⟩. This circuit, denoted by Uψ0 , requires: 

• Uρ0 to prepare |ρ0⟩,

• Uη(T ) to prepare |η(T )⟩,

• O(κη(T ) log(1 /ε )) calls to controlled-Uη(T ) and Uρ0 , where 

κη(T ) = max j |η(T, x j )|

min j |η(T, x j )| (3.6) is the condition number of the diagonal operator Dη(T ).Once |ψ0⟩ is prepared, we apply the heat equation solver again to obtain |ψ(T )⟩ with precision 

ε. The cost is 

O

 ∥ψ0∥∥ψ(T )∥ dN xβT log ∥ψ0∥

ε∥ψ(T )∥



queries to UA and O(∥ψ0∥/∥ψ(T )∥) queries to Uψ0 .

Step 3: Recovering ρ(T, x ). The terminal density is ρ(T, x ) = ψ(T, x )η0(x). Using the Hadamard product circuit (Theorem 3.1(i)), we compute |ρ(T )⟩ from |ψ(T )⟩ and the known state 

|η0⟩. This requires O(gprod ) calls to controlled-Uψ(T ) and Uη0 , where 

gprod = ∥ψ(T )∥∥ η0∥∥ψ(T ) ⊙ η0∥ .

13 3.3.2 The query complexity 

Combining the three steps, the overall algorithm prepares an ε-approximation of the normal-ized terminal density state |ρ(T )⟩ with Ω(1) success probability. The total query complexity is dominated by the heat equation solvers and the Hadamard division step. 

Theorem 3.3 (Quantum algorithm for the regularized WPO) . Assume the existence of the state preparation oracles Uρ0 and Uη0 , and the block-encoding UA of the discrete Laplacian. Then there exists a quantum algorithm that prepares an ε-approximation of the normalized state |ρ(T )⟩ cor-responding to the terminal density ρ(T, x ) = WProx T, F,β (ρ0)( x), with Ω(1) success probability, using 

•

O



gprod 

∥ψ0∥∥ψ(T )∥ dN xβT 



log ∥ψ0∥

ε∥ψ(T )∥ + ∥η0∥∥η(T )∥ κη(T ) log 1

ε log ∥η0∥

ε∥η(T )∥

 

queries to UA,

•

O



gprod 

∥ψ0∥∥ψ(T )∥ κη(T ) log 1

ε



queries to Uρ0 ,

•

O



gprod 

∥ψ0∥∥ψ(T )∥∥η0∥∥η(T )∥ κη(T ) log 1

ε



queries to Uη0 ,where 

gprod = ∥ψ(T )∥∥ η0∥∥ψ(T ) ⊙ η0∥ , κη(T ) = max j |η(T, x j )|

min j |η(T, x j )| .

The query complexity depends on certain normalization factors and on the condition number 

κη(T ). It is therefore important to assess whether these “heat equation quantities” remain moderate; otherwise, the resulting bounds may fail to be efficient with respect to the parameters d and Nx.For simplicity, we take the spatial domain to be the periodic box Td = [0 , L ]d in what follows. 

Lemma 3.2 (Estimates for the heat equation quantities) . Let η(t, x ) and ψ(t, x ) be the solutions to the heat equations (2.5) and (2.6) , respectively, on the periodic domain Td = [0 , L ]d with periodic boundary conditions. Assume that the potential V satisfies 0 ≤ V (x) ≤ V ∗ for all x ∈ Td, and that 

ρ0 is a normalized probability density, i.e., R 

> Td

ρ0(x)d x = 1 . Then the following estimates hold: 

∥η0∥∥η(T )∥ ≲ eV ∗/(2 β), ∥ψ0∥∥ψ(T )∥ ≲ eV ∗/(2 β)Ld/ 2∥ρ0∥L2 ,gprod ≲ (L∆x)d/ 2eV ∗/β , κη(T ) ≤ eV ∗/(2 β).

Proof. Estimate of ∥η0∥∥η(T )∥ . Noting that 

∥η0∥∥η(T )∥ ≈ ∥η(0) ∥L2

∥η(T )∥L2

,

14 we only need to derive an upper bound for the right-hand side. Consider the heat equation (2.5) for η on the periodic domain Td = [0 , L ]d with periodic boundary conditions. First, we note that the spatial average of η is conserved. Indeed, ddt

Z

> Td

η(t, x )d x = β

Z

> Td

∆η(t, x )d x = 0 ,

where the last equality follows from the periodic boundary conditions. Hence, ¯η(t) := 1

Ld

Z

> Td

η(t, x )d x = ¯ η0,

where ¯η0 := 1

Ld

Z

> Td

η0(x)d x. 

We decompose η into its mean and zero-mean parts: 

η(t, x ) = ¯ η0 + u(t, x ),

Z

> Td

u(t, x )d x = 0 .

Since ¯ η0 is constant in space and time, u satisfies the same heat equation: 

∂tu = β∆u, u(0 , x ) = η0(x) − ¯η0.

The L2-norm of η can be decomposed as 

∥η(t)∥2 

> L2

= Ld|¯η0|2 + ∥u(t)∥2 

> L2

, (3.7) because the mean and zero-mean parts are orthogonal. To estimate ∥u(t)∥L2 , we multiply the heat equation for u by u and integrate over Td:12ddt ∥u(t)∥2 

> L2

= β

Z

> Td

u∆udx = −β∥∇ u(t)∥2 

> L2

, (3.8) where the integration by parts has no boundary terms due to the periodic conditions. Thus, 

∥u(t)∥L2 is non-increasing: 

∥u(t)∥L2 ≤ ∥ u(0) ∥L2 .

Consequently, from (3.7) we obtain the monotonicity of the full norm: 

∥η(t)∥L2 ≤ ∥ η(0) ∥L2 . (3.9) Moreover, on the periodic domain Td, the Poincar´ e inequality for zero-mean functions gives 

∥∇ u∥2 

> L2

≥ λ1∥u∥2 

> L2

, λ1 =

 2πL

2

,

where λ1 is the smallest nonzero eigenvalue of −∆ on the periodic domain. Substituting this into (3.8) yields ddt ∥u(t)∥2 

> L2

≤ − 2βλ 1∥u(t)∥2 

> L2

, (3.10) which implies the exponential decay 

∥u(t)∥L2 ≤ e−βλ 1t∥u(0) ∥L2 . (3.11) 15 Combining (3.7) and (3.11), we obtain a lower bound for ∥η(t)∥L2 :

∥η(t)∥2 

> L2

≥ Ld|¯η0|2 + e −2βλ 1t∥u(0) ∥2 

> L2

.

Since ∥u(0) ∥2 

> L2

= ∥η0∥2 

> L2

− Ld|¯η0|2, we have 

∥η(t)∥2 

> L2

≥ Ld|¯η0|2 + e −2βλ 1t

∥η0∥2 

> L2

− Ld|¯η0|2

. (3.12) Taking square roots and rearranging, we arrive at the desired upper bound: 

∥η(0) ∥L2

∥η(t)∥L2

≤ ∥η0∥L2

q

Ld|¯η0|2 + e −2βλ 1t(∥η0∥2 

> L2

− Ld|¯η0|2)

.

In particular, since the second term in the denominator is nonnegative, we have the simpler uniform bound ∥η(0) ∥L2

∥η(T )∥L2

≤ ∥η0∥L2

√Ld |¯η0| . (3.13) Under the assumption that the potential V satisfies 0 ≤ V (x) ≤ V ∗ for all x ∈ Td, the uniform bound in (3.13) can be made more explicit. Indeed, since e −V ∗/(2 β) ≤ e−V (x)/(2 β) ≤ 1, we have ¯η0 = 1

Ld

Z

> Td

e−V (x)/(2 β)dx ≥ e−V ∗/(2 β).

Also, η0(x) ≤ 1 implies ∥η0∥L2 ≤ √Ld. Therefore, 

∥η0∥L2

∥η(T )∥L2

≤ ∥η0∥L2

√Ld |¯η0| ≤ eV ∗/(2 β).

Estimate of ∥ψ0∥∥ψ(T )∥ . The ratio ∥ψ0∥/∥ψ(T )∥ can be estimated using the same argument as for η, which gives 

∥ψ0∥∥ψ(T )∥ ≈ ∥ψ0∥L2

∥ψ(T )∥L2

≤ ∥ψ0∥L2

√Ld | ¯ψ0| ,

where ¯ψ0 = L−d R ψ(0 , x )d x. Using ψ(0 , x ) = ρ0(x)/η (T, x ) and the fact that η(T, x ) ≤ 1 (by the maximum principle, since η0 ≤ 1), we have ¯ψ0 ≥ ¯ρ0 = L−d R ρ0dx. Also, ∥ψ0∥L2 ≤ eV ∗/(2 β)∥ρ0∥L2 .Therefore, 

∥ψ0∥L2

∥ψ(T )∥L2

≤ eV ∗/(2 β) ∥ρ0∥√Ld ¯ρ0

.

Since ρ0 is a normalized probability density ( R ρ0dx = 1), this reduces to 

∥ψ0∥L2

∥ψ(T )∥L2

≤ eV ∗/(2 β)√Ld∥ρ0∥L2 .

Estimate of gprod . The factor gprod appearing in the Hadamard product complexity can be estimated as follows. In the continuous setting, 

gprod ≈ ∆xd/ 2 ∥ψ(T )∥L2 ∥η0∥L2

∥ψ(T ) · η0∥L2

.

Since ψ(T, x ) = ρ(T, x )η0(x) and 0 < m ≤ η0(x) ≤ 1 with m = e −V ∗/(2 β), we have 

∥ψ(T )∥L2 = ∥ρ(T )η0∥L2 ≤ ∥ ρ(T )∥L2 ,

16 and 

∥ψ(T ) · η0∥L2 = ∥ρ(T )η20 ∥L2 ≥ m2∥ρ(T )∥L2 .

Therefore, 

gprod ≲ ∆xd/ 2 ∥η0∥L2

m2 .

Since ∥η0∥2 

> L2

= R e−V (x)/β dx ≤ Ld, we obtain 

gprod ≲ ∆xd/ 2√Ld eV ∗/β = ( L∆x)d/ 2eV ∗/β .

Estimate of κη(T ). This quantity governs the query complexity of the quantum Hadamard division protocol. For the initial data η0(x) = e −V (x)/(2 β) with 0 ≤ V (x) ≤ V ∗, the maximum principle gives min  

> x

η(T, x ) ≥ min  

> x

η0(x) = e −V ∗/(2 β), max  

> x

η(T, x ) ≤ max  

> x

η0(x) = 1 .

Therefore, 

κη(T ) ≤ max x η0(x)min x η0(x) = e V ∗/(2 β).

This completes the proof. 

Theorem 3.4 ( Query complexity under minimizer-invariance scaling) . Suppose that the potential 

V satisfies 0 ≤ V (x) ≤ V ∗ for all x ∈ Td. By choosing the grid spacing such that L2∆x ≤ 1, and noting that the WPO is invariant under the scaling 

V 7 → VG , T 7 → T G, 

for any G > 0, we may take G = 1 /β without changing the minimizer ρT . Under this scaling, there exists a quantum algorithm that prepares an ε-approximation of the normalized state |ρ(T )⟩

corresponding to the terminal density ρ(T, x ) = WProx T, F,β (ρ0)( x), with Ω(1) success probability, using 

O



C∗dN xT log 2 1

ε



queries to UA, and 

O



C∗ log 1

ε



queries to Uρ0 and Uη0 , where C∗ = e 3V ∗/2∥ρ0∥L2 is a constant. Proof. The proof proceeds by analyzing the scaling behavior of the factors appearing in the complex-ity estimate of Theorem 3.3. We show that, under a suitable rescaling of the problem parameters, all factors remain bounded by constants independent of the discretization and the regularization parameter β.17 Cancellation of the domain-size dependence. In the complexity estimate for the Hadamard product step, the factors gprod and ∥ψ0∥/∥ψ(T )∥ appear multiplicatively. Combining the estimates from Lemma 3.2, 

gprod ≲ (L∆x)d/ 2eV ∗/β , ∥ψ0∥∥ψ(T )∥ ≲ eV ∗/(2 β)Ld/ 2∥ρ0∥L2 ,

we obtain 

gprod · ∥ψ0∥∥ψ(T )∥ ≲ (L2∆x)d/ 2e3V ∗/(2 β)∥ρ0∥L2 .

By choosing the grid spacing such that L2∆x ≤ 1, the factor ( L2∆x)d/ 2 is bounded by a constant. Hence the combined factor eliminates the exponential growth with respect to the domain size, and only data-dependent constants remain. 

Removal of β-dependence via minimizer-invariance scaling. The estimates in Lemma 3.2 depend on β through the factor e V ∗/(2 β). However, the WPO is invariant under the simultaneous scaling 

V 7 → VG , T 7 → T G 

for any G > 0, since this corresponds to dividing the objective functional (2.1) by G and thus does not change the minimizer ρT . Under this transformation, the estimate for gprod in Lemma 3.2 becomes 

gprod ≲ (L∆x)d/ 2eV ∗/(βG ).

Choosing G = 1 /β yields 

gprod ≲ (L∆x)d/ 2eV ∗

.

Similarly, the other factors become 

∥η0∥∥η(T )∥ ≲ eV ∗/2, κη(T ) ≤ eV ∗/2.

Thus, all exponential factors are now independent of β. The evolution time increases from T to 

T /β , but this is exactly compensated since the heat equation solver complexity in Theorem 3.3 contains a factor βT , resulting in no net additional cost. Combining the above estimates with Theorem 3.3 yields the desired complexity bound. 

Classical computational cost. For comparison, we briefly discuss the classical complexity of solving the regularized WPO using the discretization described in Section 3.2. The main compu-tational task consists of solving two heat equations of the form (3.2) on a d-dimensional grid with 

N = N dx points. Using a Fourier spectral method, each heat equation can be solved in O(N log N ) operations per time step via fast Fourier transforms. In addition, the Hadamard division ρ0 ⊘ η(T ) and Hadamard product ψ(T ) ⊙ η0 each require O(N ) operations. Therefore, the classical cost per time step scales as 

O N log N  = O dN dx log Nx

,

up to problem-dependent constants. 18 This classical cost exhibits an exponential dependence on the spatial dimension d through the factor N dx , which becomes prohibitive for high-dimensional problems. In contrast, the quantum algorithm in Theorem 3.4 achieves a query complexity that is polynomial in d and Nx (specifi-cally, linear in dN x), yielding an exponential speedup over classical methods. This exponential improvement is the primary advantage of our quantum approach for high-dimensional regularized WPOs. 

Remark 3.2. The classical cost stated above follows the Fourier spectral discretization in Sec-tion 3.2. An alternative classical approach, based on the kernel integral formula (Proposition 5 of [LLO23]), would require evaluating the kernel K(x, y ) for all pairs of grid points, resulting in a cost of O(N 2) = O(N 2dx ) per iteration, which is even more expensive for large d.

# 4 Numerical experiment 

We consider Example A from [LLO23]. The parameter setup is as follows: Ω = [ −b, b ]d, V (x) = e −| x+0 .25 |2/0.5, ρ0(x) = 1

σ0

√2π e−| x−0.25 |2/(2 σ20 ), σ0 = 0 .1.

For the unitary Uk(T ) = e −iμk AT , one can employ the quantum circuits detailed in [SKH +24, HJLZ24, JLY25], which are implemented using UnitaryLab, an open-source quantum computing framework at http://unitarylab.com .For the 1D case, we set b = 5 and use Nx = 2 8 spatial grid points. The numerical solutions are displayed in Fig. 2. We examine the effects of the coefficients β and T on the resulting density distribution. In the left panel, we vary T ∈ { 0.1, 0.2, 0.5} with β = 0 .25 fixed; in the right panel, we vary β ∈ { 0.125 , 0.25 , 0.5} with T = 0 .2 fixed. The results are consistent with those reported in Fig. 1 of [LLO23]. -5 0 5  

> x
> 00.5 11.5 2
> T(x)
> T = 0.1
> T = 0.2
> T = 0.5 -5 05
> x
> 00.5 11.5 2
> T(x)

Fig. 2: Numerical results for the regularized Wasserstein proximal problem in 1D. We repeat the test for the 2D case with b = 1 .5, Nx = 2 5, β = 0 .25, and T = 0 .2. The results are shown in Fig. 3. For comparison, we also provide the result obtained via the kernel integral formula (cf. Proposition 5 of [LLO23]). The two sets of results agree well. 19 0            

> 1.5
> 0.5
> T
> 1
> 11.5
> heat equation method
> 0.5 1
> x2
> 00.5
> x1
> 0-0.5 -0.5
> -1 -1
> -1.5 -1.5 0
> 1.5
> 0.5
> T
> 1
> 11.5
> kernel method
> 0.5 1
> x2
> 00.5
> x1
> 0-0.5 -0.5
> -1 -1
> -1.5 -1.5

Fig. 3: Numerical results for the regularized Wasserstein proximal problem in 2D. 

# 5 Conclusion and outlook 

In this work, we have developed efficient quantum algorithms for the regularized Wasserstein proximal operators. Building on the work of [LLO23], the forward-backward PDE system is first converted to forward heat equations via the Cole-Hopf transform, which is then combined with Schr¨ odingerization so becomes suitable for quantum simulation. We constructed quantum circuits for Hadamard division and product operations based on a simple matrix-vector multiplication representation, enabling the preparation of initial data for the second heat equation and the recovery of the terminal density state. The complete quantum algorithm prepares an approximation of the terminal density state with exponential speedups. Due to the broad applications of the Wasserstain proximal operators, the quantum algorithm developed in this work opens up several promising directions for future research. Here we list a few possibilities: 

• In gradient flows and nonlinear PDEs , it could accelerate the JKO scheme for evoluting PDEs by time-discretizing the Wasserstein gradient flow, with applications in, for examples, porous media flows and thin film equations [JKO98, LLW20]. 

• In generative AI , the connection between WPOs and diffusion models [LLOM21, ZLL +24] suggests that our quantum algorithms may offer a pathway toward efficient quantum gener-ative modeling. 

• In mean-field games [LL07, HMC06, Gu´ e12, GLL11], the WPO serves as a fundamental building block for computing equilibrium dynamics of large interacting populations; our ap-proach could potentially provide quantum acceleration in high-dimensional settings. 

• In stochastic optimal control , the close connection between the regularized WPO and the Schr¨ odinger bridge problem [L´ eo12, CGP16, CH22] points to possible extensions of our framework to density control problems. The extension of the present algorithm to nonlinear energy functionals – namely, replacing the linear functional F(q) in (2.1) with a nonlinear counterpart – also remains for future work. 20 Acknowledgments 

SJ and NL acknowledge the support of the NSFC grant No. 12341104, the Shanghai Pilot Program for Basic Research, the Science and Technology Commission of Shanghai Municipality (STCSM) grant no. 24LZ1401200 (21JC1402900), the Major Basic Research Project of the Shang-hai Jiao Tong University 2030 Initiative, and the Fundamental Research Funds for the Central Universities. NL is also supported by grant NSFC No. 12471411. YY was supported by NSFC grant (No. 12301561), the Key Project of Scientific Research Project of Hunan Provincial De-partment of Education (No. 24A0100), the Science and Technology Innovation Program of Hunan Province (No. 2025RC3150) and the general program of Hunan Provincial Natural Science Founda-tion (No. 2026JJ50003). This research was supported in part by the 111 Project (No. D23017), and Program for Science and Technology Innovative Research Team in Higher Educational Institutions of Hunan Province of China. 

# References 

[BB00] J. Benamou and Y. Brenier. A computational fluid mechanics solution to the Monge-Kantorovich mass transfer problem. Numer. Math. , 84(3):375–393, 2000. [CAS +22] P. C. S. Costa, D. An, Y. R. Sanders, Y. Su, R. Babbush, and D. W. Berry. Optimal scaling quantum linear-systems solver via discrete adiabatic theorem. PRX quantum ,3(4):040303, 2022. [CGJ19] S. Chakraborty, A. Gily´ en, and S. Jeffery. The power of block-encoded matrix powers: improved regression techniques via faster Hamiltonian simulation. In 46th Interna-tional Colloquium on Automata, Languages, and Programming (ICALP 2019) , volume 132 of Leibniz International Proceedings in Informatics (LIPIcs) , pages 33:1–33:14, 2019. [CGP16] Yongxin Chen, Tryphon T. Georgiou, and Michele Pavon. On the relation between optimal transport and schr¨ odinger bridges: a stochastic control viewpoint. Journal of Optimization Theory and Applications , 169(2):671–691, 2016. [CH22] K. F. Caluya and A. Halder. Wasserstein proximal algorithms for the Schr¨ odinger bridge problem: Density control with nonlinear drift. IEEE Trans. Autom. Control ,67(3):1163–1178, 2022. [GKRBZ24] H. Gu, M. A. Katsoulakis, L. Rey-Bellet, and B. Zhang. Combining Wasserstein-1 and Wasserstein-2 proximals: robust manifold learning via well-posed generative flows. 

arXiv:2407.11901 , 2024. [GLL11] Olivier Gu´ eant, Jean-Michel Lasry, and Pierre-Louis Lions. Mean field games and applications. In Paris–Princeton Lectures on Mathematical Finance 2010 , volume 2003 of Lecture Notes in Mathematics , pages 205–266. Springer, 2011. 21 [GSLW19] A. Gily´ en, Y. Su, G. Low, and N. Wiebe. Quantum singular value transformation and beyond: exponential improvements for quantum matrix arithmetics. In Proceedings of the 51st Annual ACM SIGACT Symposium on Theory of Computing , pages 193–204. STOC, 2019. [Gu´ e12] Olivier Gu´ eant. Mean field games equations with quadratic hamiltonian: a specific approach. Mathematical Models and Methods in Applied Sciences , 22(09):1250022, 2012. [HJLZ24] J. Hu, S. Jin, N. Liu, and L. Zhang. Quantum circuits for partial differential equations via Schr¨ odingerisation. Multiscale Model. Simulation , 22:1030–1067, 2024. [HMC06] M. Huang, R. P. Malham´ e, and P. E. Caines. Large population stochastic dynamic games: closed-loop McKean-Vlasov systems and the Nash certainty equivalence prin-ciple. Commun. Inf. Syst. , 6(3):221–252, 2006. [HNZM26] X. Huang, H. Nishi, T. Zushi, and Y. Matsushita. An efficient quantum Hadamard product algorithm for functions. arXiv:2606.03612 , 2026. [HOL25a] F. Han, S. Osher, and W. Li. Splitting regularized Wasserstein proximal algorithms for nonsmooth sampling problems. arXiv:2502.16773 , 2025. [HOL25b] F. Han, S. Osher, and W. Li. Tensor train based sampling algorithms for approximat-ing regularized Wasserstein proximal operators. SIAM/ASA J. Uncertain. Quantif. ,13(2):775–804, 2025. [JKO98] R. Jordan, D. Kinderlehrer, and F. Otto. The variational formulation of the Fokker-Planck equation. SIAM J. Math. Anal. , 29(1):1–17, 1998. [JL24] S. Jin and N. Liu. Analog quantum simulation of partial differential equations. Quan-tum Sci. Technol. , 9(3):035047, 2024. [JLM25] S. Jin, N. Liu, and C. Ma. Schr¨ odingerisation based computationally stable algo-rithms for ill-posed problems in partial differential equations. SIAM J. Sci. Comp. ,47(4):B976–B1000, 2025. [JLM +26] S. Jin, N. Liu, C. Ma, Y. Peng, and Y. Yu. On the Schr¨ odingerization method for linear non-unitary dynamics with optimal dependence on matrix queries. Commun. Math. Sci. , 24(2):565–592, 2026. [JLY23] S. Jin, N. Liu, and Y. Yu. Quantum simulation of partial differential equations: Applications and detailed analysis. Phys. Rev. A , 108:032603, 2023. [JLY24] S. Jin, N. Liu, and Y. Yu. Quantum simulation of partial differential equations via Schr¨ odingerization. Phys. Rev. Lett. , 133(23):230602, 2024. [JLY25] S. Jin, N. Liu, and Y. Yu. Quantum circuits for the heat equation with physical boundary conditions via Schr¨ odingerization. J. Comp. Phys. , 538:114138, 2025. 22 [L´ eo12] Christian L´ eonard. From the schr¨ odinger problem to the monge–kantorovich problem. 

Journal of Functional Analysis , 262(4):1879–1920, 2012. [Lin22] L. Lin. Lecture notes on quantum algorithms for scientific computation. 

arXiv:2201.08309 , 2022. [LL07] J. M. Lasry and P. L. Lions. Mean field games. Jpn. J. Math. , 2(1):229–260, 2007. [LLO23] W. Li, S. Liu, and S. Osher. A kernel formula for regularized Wasserstein proximal operators. Res. Math. Sci. , 10:43, 2023. [LLOM21] A. T. Lin, W. Li, S. Osher, and G. Mont´ ufar. Wasserstein proximal of GANs. In Inter-national Conference on Geometric Science of Information , pages 524–533. Springer, 2021. [LLW20] Wuchen Li, Jianfeng Lu, and Lexing Wang. Fisher information regularization schemes for wasserstein gradient flows. Journal of Computational Physics , 416:109449, 2020. [RNF +23] M. Ramezani, M. Nikaeen, F. Farman, S. M. Ashrafi, and A. Bahrampour. Quantum multiplication algorithm based on the convolution theorem. Phys. Rev. A , 108:052405, 2023. [RR23] A. G. Rattew and P. Rebentrost. Non-linear transformations of quantum amplitudes: Exponential improvement, generalization, and applications. arXiv:2309.09839 , 2023. [SKH +24] Y. Sato, R. Kondo, I. Hamamura, T. Onodera, and N. Yamamoto. Hamiltonian simulation for hyperbolic partial differential equations by scalable quantum circuits. 

Phys. Rev. Res. , 6:033246, 2024. [Vil09] C´ edric Villani. Optimal Transport: Old and New . Springer, 2009. [ZLL +24] B. J. Zhang, S. Liu, W. Li, M. A. Katsoulakis, and S. J. Osher. Wasserstein proximal operators describe score-based generative models and resolve memorization. 

arXiv:2402.12345 , 2024. 23