---
title: Triangular-Reference Schrödinger Bridges for Time Series Generation
title_zh: 三角参考薛定谔桥用于时间序列生成
authors: Gabriele Bocchi
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27478v2"
tags: ["query:diff-bridge"]
score: 10.0
evidence: 引入三角参考 Schrödinger 桥用于时间序列生成，通过区间冻结扩散参考扩展 SBTS 框架。
tldr: 传统SBTS框架采用固定布朗运动参考，难以适应时间序列的非平稳局部协方差结构。TR-SBTS通过分段冻结且可能退化的三角形扩散参考，在增广状态空间上进行单一熵投影，并利用分层潜在波动水平联合约束，最优解为参考的h变换。在冻结区间上，漂移由活跃协方差方向上的对数梯度给出，即使协方差秩亏时公式依然成立。理论证明了冻结近似的稳定性和正则化核估计的收敛性；实现上，结合块PCR、马氏核和WLS回归的条件映射与状态-协方差桥接，实验证实有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27478v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1191, \"height\": 632, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27478v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1227, \"height\": 879, \"label\": \"Figure\"}]"
motivation: 现有SBTS采用固定布朗参考，难以匹配真实时间序列的局部非平稳动态，需要更灵活的参考结构以提升生成性能。
method: 提出TR-SBTS，用分段冻结的三角形扩散参考替代布朗参考，通过增广状态熵投影和h变换得到闭式漂移，并设计整合PCR、马氏核和WLS回归的条件映射。
result: 获得了在秩亏协方差下有效的对数梯度漂移公式，证明了冻结近似稳定性和正则化核估计收敛性，实验显示优越性。
conclusion: TR-SBTS将SBTS的布朗参考推广为三角形参考，增强了模型对非平稳局部结构的适应性，理论保证和数值实验均验证了其有效性。
---

## 摘要
我们引入三角参考薛定谔桥用于时间序列（TR-SBTS），这是SBTS框架的保守扩展，其中布朗参考被分段冻结的、可能退化的扩散参考所取代，该参考在潜在波动率层级中呈三角结构。该构造是在增广状态空间上的单次熵投影，变分约束跨时间和潜在层级联合施加，并通过相对熵的分解逐层展开。保留SBTS的变分核心：熵最小化器是参考的h-变换，并且在每个冻结区间上，最优动力学在活动协方差方向的仿射叶面上承认对数梯度漂移公式，即使在冻结协方差秩亏时也有效。我们建立了冻结近似的稳定性以及相应正则化核估计量的收敛性。该构造通过由过去信息的三种互补约化组成的有限维条件映射实现——块主成分回归（PCR）摘要，由运行时冻结协方差累积量在过去增量上诱导的参考感知马哈拉诺比斯核，以及在相同参考度量下过去窗口的加权最小二乘漂移回归量——以及一个耦合的状态-协方差桥步骤，其中每个潜在层级为其上层生成动态参考，由协方差描述符总结；通过数值实验评估了该构造。

## Abstract
We introduce Triangular-Reference Schrödinger Bridges for Time Series (TR-SBTS), a conservative extension of the SBTS framework in which the Brownian reference is replaced by an intervalwise frozen, possibly degenerate diffusion reference, triangular across a hierarchy of latent volatility levels. The construction is a single entropy projection on the augmented state space, with the variational constraint imposed jointly across time and the latent levels and unfolded hierarchically by the disintegration of relative entropy. The variational core of SBTS is preserved: the entropy minimiser is the h-transform of the reference, and on each frozen interval the optimal dynamics admit a logarithmic-gradient drift formula on the affine leaves of the active covariance directions, valid even when the frozen covariance is rank-deficient. We establish stability of the frozen approximation and convergence of the corresponding regularised kernel estimators. The construction is realised through a finite-dimensional conditioning map assembled from three complementary reductions of the past -- a block PCR summary, a reference-aware Mahalanobis kernel on past increments induced by the runtime frozen covariance cumulants, and a past-window WLS drift regressor under the same reference metric -- together with a coupled state-covariance bridge step in which each latent level produces a dynamic reference for the level above, summarised by a covariance descriptor; the construction is evaluated on numerical experiments.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **背景**：时间序列生成（特别是金融场景）需要同时捕捉边际分布、时间依赖性、截面相关性、波动率聚类、尾部行为和突变的体制转换等。传统参数模型校准困难，而现有的 Schrödinger Bridge for Time Series (SBTS) 框架以布朗运动作为参考过程，其固定的二次变差无法模拟时变的局部协方差结构、随机波动率以及秩亏（rank-deficient）的协方差矩阵。
- **研究动机**：为了弥补 SBTS 的不足，需要在不破坏其相对熵投影结构的条件下，引入一种更灵活的参考过程，使其能够感知并传递时变的波动率信息。
- **整体含义**：本文提出 **Triangular-Reference Schrödinger Bridges for Time Series (TR-SBTS)**，一种保守的扩展方法，将 SBTS 的布朗参考替换为一个在增广状态空间上呈三角分层结构、区间冻结且可能退化的高斯扩散参考。该框架保留了原有熵投影变分核心，并用相对熵的逐层分解（hierarchical disintegration）来施加跨时间和潜在波动率层级的联合约束。

## 2. 论文提出的方法论

- **核心思想**：
  - 在增广状态空间 \((Z^0, Z^1, \dots, Z^L)\) 上定义参考过程，其中 \(Z^0\) 是主时间序列（如价格），下层 \(Z^1, \dots, Z^L\) 表示由粗观测网格构造的波动率或协方差描述子。
  - 参考过程在每个粗区间 \((t_i, t_{i+1}]\) 内是冻结的高斯鞅（条件期望不变，协方差由区间起始时刻的历史和下层状态决定），即使在协方差秩亏时，动力学也限制在活跃的仿射叶面（affine leaves）上。
  - 目标依旧是寻找与参考律的相对熵最小，且粗网格上的联合律匹配经验数据的路径测度。通过熵的链式法则，该单一增广投影可以分层自底向上实现。

- **关键技术细节与公式**：
  - **变分识别**：最优路径测度 \(P^*\) 是参考测度 \(R\) 的 h-变换，密度为 \(d P^* / d R = G(\Pi)\)，其中 \(G = d\mu / d\mu_{RT}\) 是目标粗网格律 \(\mu\) 与参考粗网格律 \(\mu_{RT}\) 的 Radon-Nikodym 导数。投影的最优值为 \(H(\mu | \mu_{RT})\)。
  - **局部漂移公式**：在冻结区间上，给定状态和波动率描述子，最优受控扩散的漂移由逆向热势在该区间的活动协方差方向上的对数梯度给出：
    \[
    b_i^*(t, x) = \sum_{j=1}^{r_i} \lambda_{i,j} \, D_{q_{i,j}} \log H_i(t, x) \, q_{i,j}
    \]
    其中 \(H_i\) 是以区间终点倾斜（terminal tilt）\(f_i\) 作为终端条件的向后热传导方程的解，\(q_{i,j}\) 和 \(\lambda_{i,j}\) 为活跃余切协方差矩阵 \(A_i\) 的特征向量和特征值。
  - **冻结近似稳定性**：证明用分片冻结协方差逼近连续变化的波动率时，误差仅依赖于累积协方差的插值误差，且其桥梁的弱收敛性得到保证。
  - **统计估计**：对秩亏协方差采用谱阈值正则化（\(A_i^\varepsilon\)）得到全秩核，然后用条件核回归（如 Nadaraya-Watson 光滑器）从历史样本中估计终端倾斜律，并证明所得经验漂移和转移核的收敛性。
  - **实现架构**：通过三种互补的过去信息约化构建有限维条件变量——块主成分回归（PCR）降维、参考感知的马氏距离核、过去窗口的加权最小二乘漂移回归（WLS），并与一个动态更新的协方差描述符数据库耦合，逐层投射生成下一个粗网格状态。

## 3. 实验设计

- **数据集/场景**：
  - **低秩维度应力测试**：在逐渐增大的环境维度（\(d=4,8,16,\dots,512\)）中嵌入一个二维非线性霍普夫振荡加独立的快 OU 过程，检验维度灾难下的预测能力。
  - **Heston 随机波动率恢复**：从经典的 Heston 模型生成状态（对数价格与方差），用三层 TR-SBTS（主层 X 为状态、中层 Y 为累计平方变差、底层 Z 为协方差几何方向）进行闭环比生成，并基于生成路径恢复 Heston 参数分布。

- **基准与对比方法**：
  - 在低秩测试中，对比了 **classic_no_pcr**（在全维度上计算核权重，无降维）和 **classic_pcr**（通过 PCA 截断到信号子空间，即约化到二维）。
  - 在 Heston 恢复中，与 **SBTS-Classic**（单层、固定布朗参考的 SBTS）进行比较，对比基线为从真实路径恢复的参数分布。

## 4. 资源与算力

- 文中明确给出了实验的硬件环境：Intel Core i7-8565U CPU（1.80GHz），16 GB RAM，集成 Intel UHD Graphics 620，**无独立 GPU**。
- 所有算法均使用 Julia（1.12.3 版）实现，仅在 CPU 上运行，未使用任何 GPU 加速。论文中未提及具体的训练时长。整体看，该方法对算力要求极低。

## 5. 实验数量与充分性

- **实验组数**：
  - 共设计了 **两大组实验**：一组针对低秩泛化能力进行了跨 8 个维度（每种 3 个随机种子）的扫描；另一组针对随机波动率建模进行了 Heston 参数恢复的三层 vs 单层对比。
- **充分性与客观性**：
  - **低秩实验**严谨展现了无降维方法随环境维度增大预测能量的退化，而 PCR 变体的预测超额误差始终保持低位，有力地验证了降维策略抵抗维数灾难的能力。
  - **Heston 实验**公平地从相同数据集拟合模型，并基于闭合路径的 Quasi-Maximum Likelihood 分布进行对比。它直观地展示了 TR-SBTS 能够恢复波动率波动（\(\xi\)）和杠杆效应（\(\rho\)），而固定布朗参考的经典 SBTS 只能恢复长期均值（\(\theta\)），其余参数则坍缩到先验。
  - 虽然实验场景均为合成数据，但设计直指方法的核心理论优势（低秩、随机波动率），并给出了清晰的对比，实验具有针对性和充分的说服力。

## 6. 论文的主要结论与发现

- TR-SBTS 成功地将 SBTS 推广到了具有三角分层结构、区间冻结且可退化的扩散参考上。
- 该框架在理论上保持了熵投影的可解释性，并在秩亏协方差下给出了显式的对数梯度漂移公式。
- 理论证明了冻结近似的稳定性以及正则化核估计的收敛性。
- 实验证明，基于 PCR 的条件降维能有效抵抗高维灾难，整个分层生成框架能够可靠地捕获并传递随机波动率和资产间的依赖结构，其生成样本在统计上比经典 SBTS 更接近真实数据的随机过程属性。

## 7. 优点

- **结构优雅的扩展**：严格保留了原始 SBTS 的变分和 h-变换核心，仅通过改变参考过程来引入灵活性，概念清晰。
- **处理秩亏与波动率**：能够自然且显式地处理秩亏协方差和时变的波动率/相关结构，对金融时间序列等场景有重要的实际价值。
- **理论坚实**：给出了从变分识别、漂移形式、近似稳定性到统计估计收敛的完整理论链条。
- **计算高效**：可在无 GPU 的普通笔记本电脑上运行。
- **耦合与分层设计**：多层级的状态-协方差设计为信息在不同频率和抽象层级间的流动提供了机制。

## 8. 不足与局限

- **缺乏离散跳跃成分**：当前框架基于连续路径扩散，无法处理金融数据中常见的跳跃或体制突变。文中已将跳-扩散扩展列为未来工作。
- **核方法的限制**：尽管用降维缓解，作为条件近似的核回归机制仍对带宽选择敏感，并可能在高维场景下失效。
- **实验数据均为合成**：所有验证都针对人造模型（Hopf 振荡与 Heston 模型）生成的数据，尚未在真实、复杂噪声的现实世界时间序列上进行评估。
- **未与深度生成模型直接比较**：基准对比的是经典 SBTS，但没有涉及当前主流的扩散概率模型或基于得分的方法等生成式 AI 基线。
- **潜在的计算瓶颈**：尽管单机算力低，但随着分层数量或历史记忆窗口的增大，存储与核回归的计算量可能会显著上升。

（完）
